[package]
name = "assistant-protocol"
description = "Protocol types (Single source of truth from JSON schema)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
# No tests in this card; tests live in xtask's verify-schemas

[lints]
workspace = true