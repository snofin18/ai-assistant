{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.invalid/protocol/tool-schema-1.0.json",
  "title": "ToolSchema",
  "version": "1.0",
  "description": "TASK-011: Meta-schema for individual tool schemas. Every tool must have a JSON Schema conforming to this shape.",
  "type": "object",
  "required": ["version", "name", "input", "output"],
  "properties": {
    "version": { "type": "string", "const": "1.0" },
    "name": { "type": "string", "pattern": "^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$", "description": "Tool name = `<app>.<domain>.<action>` per naming § controlled vocabulary." },
    "description": { "type": "string", "minLength": 1 },
    "input": { "type": "object", "description": "JSON Schema for the tool input (draft-07)." },
    "output": { "$ref": "envelope-1.0.json", "description": "Every tool returns the unified envelope." },
    "risk_level": { "type": "string", "enum": ["low", "medium", "high", "critical"], "description": "Per ADR-0021; drives policy gating." },
    "requires_approval": { "type": "boolean", "default": false, "description": "If true, runtime MUST pause for human approval before execution." },
    "idempotent": { "type": "boolean", "default": false },
    "tags": { "type": "array", "items": { "type": "string" } }
  },
  "additionalProperties": false
}