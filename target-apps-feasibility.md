# 被控应用可行性分级与试点方案

> 状态：设计讨论稿（文档阶段）
> 版本：1.1　日期：2026-09-16（v1.1：修订试点执行顺序、补版本支持矩阵、Windows 10 结论、股票交易闸门预留）
> 配套文档：`cross-platform-ai-assistant-architecture-v2.md`（架构 v2）、`docs/governance-ai-agent-execution.md`（AI 协作治理）、`AGENTS.md`
> 适用范围：**Windows 平台**（阶段 1~3 的纵深验证）；macOS/Linux 的应用分级待阶段 4/5 补做

---

## 0. 本文档的作用与使用方式

架构 v2 的开放问题 D2 是「主程序是什么、能不能改」。现已明确：

> **被控对象不是某一个自研主程序，而是一组通用的 Windows 常用应用**（记事本、计算器、画图、浏览器、Office、Adobe、股票软件……），**我们无法修改它们的源码**。

这个答案对架构的影响是根本性的，必须先说清楚：

| 影响面 | 变化 |
|---|---|
| **L1 通道** | 不再是「推动主程序加接口」，而是「**发现并利用应用已有的官方接口**」（COM / CLI / 脚本 / URI scheme / 导出功能）。因此 **Adapter 的第一项工作是接口考古**，不是协商 |
| **可靠性上限** | 由**每个应用自身**决定，不由我们决定。因此必须按应用分级，**只对 T1/T2 承诺可靠性，T3 明确降级或拒绝** |
| **App Adapter** | 从「一个」变成「一族」，Adapter 规范（v2 §6.8）成为项目的**核心资产与主要工作量** |
| **可逆性** | 各应用差异极大（记事本有多级 undo，Excel 的程序化修改会**清空 undo 栈**，浏览器提交表单**不可逆**）→ v2 第 9 章的四级模型必须**逐应用声明** |
| **合规** | 出现真实的高风险类别（股票交易类软件的自动化下单）→ 必须进 Non-goals |
| **试点策略** | 需要一组**由易到难、各自验证不同架构机制**的代表性应用 → 本文档第 3 章 |

**使用方式**：
1. 第 1 章是**分级判据**（方法论），以后评估任何新应用都用它，不要凭感觉；
2. 第 2 章是**常见应用清单**（分 T1/T2/T3），可作为 Adapter 路线图；
3. 第 3 章是**5 个试点应用的深度档案**，直接决定阶段 1~3 的范围与 DoD；
4. 第 4 章是**试点顺序与修订后的路线**；
5. 第 5 章是**接口考古清单**（可直接照着做 Spike A）；
6. 标注 `【需实测】` 的条目是假设，必须在 Spike 中证伪或确认后才能升级为结论。

---

## 1. 分级判据（十维打分法）

### 1.1 十个维度

| # | 维度 | 0 分（最好） | 3 分（最差） |
|---|---|---|---|
| D1 | **官方接口可用性** | 有完整 COM/CLI/脚本 API，覆盖主要功能 | 无任何接口 |
| D2 | **UIA 覆盖度** | 控件树完整，关键控件都有 `AutomationId` | 只有一个大窗口（全自绘） |
| D3 | **UIA Pattern 支持** | Value/Text/Invoke/Selection/Table 齐全 | 无 Pattern，只能点击坐标 |
| D4 | **状态复杂度** | 单窗口、无模态、同步 | 多窗口 + 频繁模态 + 异步/后台任务 |
| D5 | **可验证性** | 结果可从 API/控件/文件直接读回 | 只能靠截图像素判断 |
| D6 | **可逆性** | 多级 undo 或可快照恢复 | 动作即生效且不可恢复 |
| D7 | **规模与性能** | 树 < 500 节点，遍历 < 200 ms | 树 > 5000 节点或遍历 > 2 s |
| D8 | **版本稳定性** | UI 与接口多年不变 | 频繁改版、控件 id 变化、多版本并存 |
| D9 | **合规与风控风险** | 无限制 | EULA 禁止自动化 / 涉及资金与账户安全 |
| D10 | **环境依赖** | 普通用户态即可 | 需提权 / 需登录态 / 需联网 / 需许可证 |

### 1.2 分级规则

```text
总分 = Σ(权重 × 得分)，权重：D1/D2/D5/D9 = 2.0，D3/D4/D6 = 1.5，D7/D8/D10 = 1.0
满分 45（加权后），归一化到 0~100

T1 易实现    ≤ 25 分　且 D9 ≤ 1　且 (D1 ≤ 1 或 D2 ≤ 1)
T2 部分实现  26~34 分　且 D9 ≤ 2
T3 难以实现  ≥ 35 分　或 D9 = 3　或 (D1 = 3 且 D2 = 3)
```

**一票否决项**（命中即降为 T3 或直接列入 Non-goals）：
- D9 = 3：涉及资金交易、账户凭据操作、EULA 明确禁止自动化、可能触发风控封号；
- D2 = 3 且 D1 = 3 且 D5 = 3：既无接口、又无控件树、又无法验证结果 → 只能盲操作，**违反「无静默失败」红线**（v2 §1.4）；
- 需要提权且无法隔离（v2 §13.2.4）；
- 运行在 RDP/虚拟机/云桌面内的应用（我们只能看到一张画面）。

### 1.3 分级对应的产品承诺

| 级别 | 承诺 | 策略 |
|---|---|---|
| **T1** | 可做，成功率目标 ≥ 90%，支持形态 A/B（有人/半自动） | 正常 Adapter，L1 或 L3 主通道 |
| **T2** | 可做**部分功能**，成功率目标 ≥ 75%，仅形态 A（有人在旁） | Adapter 明确列出「支持的能力子集」与「不支持清单」；高风险动作强制确认 |
| **T3** | **不承诺**。仅在有 L1 数据通道时提供「只读」能力 | 默认不接入；若必须接入，只允许只读 + 全动作人工确认 + 显著风险提示 |
| Non-goal | 拒绝接入 | 写入 v2 §1.3，UI 中明确告知「本助理不支持自动化操作此类应用」 |

> 这张表是**对用户的承诺边界**，必须写进产品文档。过度承诺是这类项目失去信任的最快方式。

---

## 2. 常见 Windows 应用清单与分级

### 2.1 T1　易实现

| 应用 | 主通道 | 关键说明 | 可逆性 | 备注 |
|---|---|---|---|---|
| **记事本 Notepad**（Win11 新版） | L3 UIA | WinUI 3 应用，编辑区支持 Value/Text Pattern；**多标签**（TabItem） | L0 多级 undo + L1 文件 | ⚠ Win10 旧版是单文档 Edit 控件，AutomationId 不同 → Adapter 需版本分支 |
| **计算器 Calculator** | L3 UIA | WinUI，按钮有稳定 AutomationId；显示区可读 | L1（读回显示值 / CE 清零） | 无 undo，但状态可完全重置 |
| **文件资源管理器 Explorer** | L1 `Shell.Application` COM + L2 CLI + L3 UIA | 三条通道都好；地址栏可直接输路径 | L2（删除走回收站） | 文件对话框是**独立进程**，需单独 Adapter |
| **Windows 设置 Settings** | L2 `ms-settings:` URI + L3 UIA | URI 可直达任意设置页 | L2（改前记录原值） | 部分设置改动需重启生效 |
| **截图工具 Snipping Tool** | L3 UIA | WinUI，控件规整 | L1（图片文件） | — |
| **7-Zip / WinRAR** | **L1 CLI**（`7z.exe`）+ L3 UIA | CLI 是首选，完全可脚本化、可验证 | L2（解压到新目录 / 备份原包） | GUI 只做兜底 |
| **写字板 WordPad** | — | **Windows 11 24H2 起已移除** | — | ❌ 不要选它做试点 |
| **录音机 / 时钟 / 天气 / 照片** 等内置 WinUI 应用 | L3 UIA | 覆盖良好 | 视应用 | 「照片」的编辑功能部分自绘 |
| **自建 WinForms/WPF 应用**（若设了 AutomationId） | L3 UIA | 最理想的 UIA 目标 | 视应用 | 我们的**靶机应用**属于此类（v2 §17.3） |
| **Windows Terminal / PowerShell / CMD** | **L1 PTY / CLI** | ⚠ **绝不做 UI 自动化**：文本可经 UIA TextPattern 读取，但输入必须走 PTY；且 `Ctrl+Z` 在 *nix shell 中是 SIGTSTP（v2 §9.2 陷阱 1） | L2 | 单独 Adapter，走 ConPTY |

### 2.2 T2　部分实现

| 应用 | 主通道 | 关键说明 | 可逆性 | 主要坑 |
|---|---|---|---|---|
| **Excel** | **L1 COM `Excel.Application`** + L3 UIA（状态与对话框） | COM 覆盖极全：工作簿/表/区域/公式/格式/图表/透视表 | ⚠ **L1 快照为主**：**任何程序化修改都会清空 Excel 的 undo 栈**，`Application.Undo` 只能撤销过程内最后一次修改 → **不能依赖 Ctrl+Z** | Protected View（网络来源文件只读沙箱，UI 树不同）、文件被占用/只读、AutoSave+OneDrive 冲突、模态对话框（另存为/覆盖/更新链接）、宏安全警告、大表 UIA 性能、**禁止 `DisplayAlerts=false`**（会静默覆盖） |
| **Word** | **L1 COM `Word.Application`** + L3 UIA | 同 Excel，另有修订(Track Changes)、批注、域 | L1 快照 + L0（**COM 修改同样不进 undo 栈**） | 修订模式下「改了什么」语义复杂；受保护文档；Normal.dotm 弹窗 |
| **PowerPoint** | L1 COM + L3 UIA | 覆盖良好 | L1 快照 | 幻灯片放映态是全屏独占，不可自动化 |
| **Outlook（经典版 outlook.exe）** | **L1 COM / MAPI** + L3 UIA | COM 支持完整（微软官方确认经典版继续支持 COM 与 VSTO） | ⚠ **发送邮件 = L3 不可逆** | 发送前必须人工确认（v2 §9.7）；安全弹窗（编程访问保护）；OST/PST 锁定 |
| **Outlook（新版 New Outlook, olk.exe）** | **L3 UIA（无 COM）** | 微软官方确认：**新版 Outlook 不支持 COM / VSTO / VBA 扩展模型** | L3（发送不可逆） | 基于 Web 技术，UIA 树大且不稳定；能力显著弱于经典版 → **建议只承诺只读，或明确列为 T3** |
| **Chrome / Edge** | **L1/L2 CDP（网页内容）** + L3 UIA（浏览器外壳）+ Win32（系统对话框） | 网页内容用 CDP 远比 UIA 可靠；Chrome 138+ 在 Windows 默认启用原生 UIA，外壳与网页都能读到 | **混合**：导航可后退(L2)、DOM 可脚本撤销、**表单提交/发消息 = L3** | ⚠ **Chrome/Edge 136 起 `--remote-debugging-port` 对默认用户数据目录失效，必须同时指定自定义 `--user-data-dir`** → **无法附加到用户日常浏览器实例**，登录态需重建；企业策略可能禁用远程调试；Cookie 同意横幅/权限弹窗/下载栏；**这是最大的提示注入面**（v2 场景 S5） |
| **VS Code** | **L1 CLI + 扩展 API** + L3 UIA | `code --goto/--diff/--install-extension`，扩展可提供稳定 API | L0（编辑器多级 undo） | Electron，UIA 需 a11y 开启；扩展开发有成本 |
| **Adobe Photoshop** | **L1 脚本：UXP Scripting / ExtendScript(JSX) / COM `Photoshop.Application` / Action Manager** | 脚本能力极强，可读写图层、执行动作、导出；**History 状态可编程回滚**（比 Ctrl+Z 可靠得多） | **L0（historyState 回滚）+ L1（PSD 副本）** | **UI 全自绘，UIA 覆盖差** → 只能走脚本；启动慢（10~30 s）；需登录/许可；版本差异大（2026 版为 v27）；**Adobe 正把 ExtendScript 逐步替换为 UXP**（Premiere Pro 的 ExtendScript 支持已于 2026-09 结束）→ Adapter 应**优先 UXP，保留 JSX/COM 兼容路径，并按版本记录** |
| **Adobe Illustrator** | L1 COM / JSX / UXP | 同 Photoshop | L0 + L1（AI 文件副本） | 同上 |
| **Acrobat / Foxit** | L1（部分 API）+ L3 UIA | Acrobat 有 COM/IAC | L1（PDF 副本） | 版本差异大 |
| **AutoCAD** | **L1 COM / AutoLISP / .NET API** | 脚本能力强 | L0（UNDO 命令 + 脚本可回滚） | 重型应用，启动慢 |
| **Teams / Slack / Discord**（Electron） | L3 UIA（需开启 a11y） | 覆盖中等 | ⚠ **发消息 = L3 不可逆** | 消息发送建议默认人工确认 |
| **浏览器内的 Web 业务系统** | **L1 CDP** | 与浏览器同一通道 | 混合 | 站点反自动化检测；注入风险最高 |
| **同花顺 / 通达信 / 东方财富（数据读取）** | **L1 官方数据 API（iFinD / Choice，付费）** + **L2 导出/复制到剪贴板** + L7 OCR | 见 2.3 专段 | 只读为主 | ⚠ 见下 |

### 2.3 股票类软件专段（同花顺 / 通达信 / 东方财富等）

这类软件是**特殊类别**，必须单独对待。

**技术特征（`【需实测】`，以下为基于同类软件的判断，须在 Spike A 中验证）**：

| 特征 | 影响 |
|---|---|
| 行情/交易界面大量使用 **GDI 自绘**（表格、K 线、盘口） | UIA 树里往往只有一个窗口 + 少量真控件，**D2/D3 得分极差** |
| 数据是**高频刷新**的实时流 | 指纹/断言会持续抖动，`settle` 几乎不可能成立 → 传统的「等 UI 静止」策略失效 |
| 普遍支持**键盘精灵**（输入代码 + 回车跳转）、**F1~F10 快捷视图**、**数据导出/复制到剪贴板** | ★ 这是**真正可行的 L2 通道**，比 OCR 可靠一个量级 |
| 部分产品有**官方数据 API**（同花顺 iFinD、东方财富 Choice，均为付费服务） | ★ 数据类需求应优先走官方 API（L1） |
| 通达信有**公式系统**，可编写指标/选股公式 | 可作为「计算」通道，避免 UI 操作 |
| 交易下单界面涉及**资金账户、密码、验证码、风控** | ⚠ D9 = 3，一票否决 |

**结论与建议**：

1. **数据读取**：分级为 **T2（部分实现）**，但**只允许走 L1 官方数据 API 与 L2 导出/剪贴板**，**禁止把 OCR 作为交易决策的唯一数据来源**（读错一个数字在交易场景是致命的）。若必须用 OCR，要求**双通道交叉校验**（OCR + 导出文本一致才采信）。
2. **交易操作（下单/撤单/改单/转账/开户）**：**列入 Non-goals**，理由：
   - 券商与软件 EULA 普遍明确禁止外挂与自动化交易；
   - 可能触发风控、限制账户甚至合规问题；
   - 不可逆（L3）且涉及资金，与 v2 §9.7「无人值守禁止 L3」冲突；
   - 一次误操作的损失远超自动化带来的收益。
   → 建议写入 v2 §1.3 Non-goals 第 11 条。
3. **辅助性只读场景**（例如「把自选股涨跌幅整理成表格写入 Excel」）：**可行且有价值**，路径是 `股票软件导出/剪贴板 或 官方数据 API → 本地处理 → 写入 Excel(COM)`，**全程不触碰交易界面**。这类任务应作为该类别的唯一样板。
4. **微信 / QQ / 其他社交类 PC 客户端**：同样因自绘 + 封号风险，建议 T3 / Non-goal。

**（v1.1 新增）「解读 + 图形展示」的数据流向**

按决策，股票类软件的目标是**只读数据 → 本地解读 → 在助理自己的 UI 中图形化展示**，而**不是操作股票软件去画图**。因此：

```text
股票软件
  └─ 只读通道：官方数据 API（iFinD / Choice）｜应用「导出/复制到剪贴板」｜（OCR 仅辅助，需双通道交叉校验）
       ↓
Core：结构化为标准行情/持仓数据模型（统一 schema，与来源解耦）
       ↓
本地分析（纯计算工具：analysis.* / chart.*，可内置 Rust 或 WASM）
       ↓
助理 UI 的「图表面」（第 6 个工作面，v2 §16.1）：React + 轻量图表库渲染
       ↓
（可选）导出为文件 / 写入 Excel（走 Excel COM 通道）
```

架构影响：
- 新增 **`analysis.*` 与 `chart.*` 两类纯计算工具**（不触碰任何应用 UI，因此风险级为 low、可逆性为 `none_readonly`）；
- UI 新增**图表面**，它是**只读展示**，不得成为操作被控应用的通道；
- 数据模型要与来源解耦（同花顺/通达信/东财/官方 API 各自一个 Adapter，但输出统一 schema），否则每换一个数据源就要改一遍分析逻辑。

**（v1.1 新增）交易闸门预留（TradingGate）**

未来若实现足够完善，可能开放「一定金额权限内的自动买卖」。现在就把闸门做出来，避免将来「为了能交易而绕过安全层」。完整设计见 v2 §9.9，要点：

- 当前 `TradingGate` **恒返回 Denied**，且交易类工具**根本不注册**；
- 预留三处：类型层（`TradingGate` trait + `TradingDecision`）、配置层（`[trading]` 段，`enabled=false` 为唯一总开关）、UI 层（策略面板显示「未开放」与启用条件）；
- **未来启用必须走厂商官方交易接口（L1），禁止用 UI 自动化点击下单**（后者无法可靠验证是否成交，违反「无静默失败」红线）；
- 启用前置条件（缺一不可）：新 ADR + 合规评估、单笔/单日限额、标的白名单、双重确认 + 确认词、冷静期可撤回、熔断、独立审计流、**永久禁止无人值守**、成交回报可验证。

### 2.4 T3　难以实现（不建议接入，或仅只读）

| 应用类别 | 原因 | 可能的降级路径 |
|---|---|---|
| **股票软件的交易界面** | D9 一票否决 | 无（Non-goal） |
| **微信 / QQ PC 客户端** | 自绘严重 + 封号与合规风险 | 无（Non-goal） |
| **老式 Delphi / VB6 / MFC 自绘应用**（部分进销存、ERP） | UIA 覆盖差 | Win32 消息 + OCR + 坐标，可靠性低 |
| **SAP GUI** | 自绘/Java 混合 | 若服务端开启 **SAP GUI Scripting** 则跃升为 T2；否则难 |
| **国产 ERP（金蝶/用友等）** | 版本碎片化、控件不规整 | 视版本，多数需 L1（数据库/API）而非 UI |
| **游戏 / Unity / 自绘 canvas 应用** | 无控件结构 | 仅视觉，Non-goal |
| **RDP / 虚拟机 / 云桌面内的应用** | 只能看到一张位图 | 仅视觉 + 坐标注入，可靠性低，建议 Non-goal |
| **未开启 Java Access Bridge 的 Java Swing 应用** | AT-SPI/UIA 均不可见 | 引导开启 JAB 后可升为 T2 |
| **需要提权且无法隔离的系统工具** | UIPI（v2 §13.2.4） | 提权 Host，风险高，逐个评估 |
| **New Outlook 的写操作** | 无 COM + UIA 不稳定 + 发送不可逆 | 只读可尝试，写操作建议 T3 |

### 2.5 版本支持矩阵（v1.1 新增，对应决策 Q4/Q5）

> 完整版见架构 v2 §13.6。此处只记两条会改变方案的结论。

**① Windows 10 不作为正式支持基线**

- **Windows 10 已于 2025-10-14 结束支持**（消费者 ESU 也仅延至 2026-10）；
- 更关键的是：**Win10 的记事本是经典单文档 `Edit` 控件、画图是经典版（无图层、自定义工具栏）**，与 Win11 的新版 WinUI 应用**结构完全不同**，等于要为两个试点应用各写一套 Adapter 分支；
- Win10 上 WebView2 可能未预装（影响助理自身 UI）。

结论：**基线 = Windows 11 24H2 / 25H2**。若内部确有 Win10 机器，采用**折中方案**：只支持在两个系统上行为一致的目标（Excel/Word 的 COM、Edge/Chrome 的 CDP、7-Zip 的 CLI），**不支持 Win10 上的记事本与画图**；Adapter 用 `os_version_range` 声明，Capability probe 自动禁用不支持项并给出可读原因。

**② Adobe 2020 无法使用推荐通道 UXP**

- **UXP 是 Photoshop v22.0（2021）及以后才有的脚本/插件平台**；Photoshop 2020 = v21，**没有 UXP**，只能走 ExtendScript(JSX) / COM / Action Manager；
- 而 Adobe 正在**逐步结束 ExtendScript 支持**（Premiere Pro 已于 2026-09 结束），JSX 属于退场中的技术；
- 另一现实约束：**Adobe 通常只支持最近 2~3 个大版本**，2020 版可能已无法通过 Creative Cloud 正常登录/激活 `【需实测】`。

结论：**建议最低正式支持版本调整为 Photoshop 2021 (v22)**，2020 (v21) 列为「尽力而为（仅 JSX/COM）」；Illustrator 建议最低 2022 (v26) `【需实测确认其 UXP 起始版本】`。Adapter 的通道探测顺序为 **UXP → JSX → COM**，并对 JSX 标注 `deprecated_upstream = true`。

**③ Office 2019+ 可用，但版本号不可用于区分**

- Office 2019 / 2021 / Microsoft 365 的 COM 自动化均完整，**2019 无 AutoSave**（反而少一个坑）；M365 有 **AutoSave + 云端版本历史**，版本历史可作为比 Ctrl+Z 更强的回滚锚点；
- ★ **坑**：Office 2016/2019/2021/365 的**内部版本号都是 16.0**，`Application.Version` 无法区分 → 必须用 `Application.Build` 或注册表 `ClickToRun\Configuration` 的 `DisplayVersion` / `ProductReleaseIds` `【需实测】`。

### 2.6 Adapter 路线图建议（按投入产出排序，v1.1 修订）

```text
第一批（阶段 1）  Notepad → Paint → Edge/Chrome   ← 覆盖 L3 UIA、L4 坐标+L5 视觉、L1 CDP、L1 文件契约；零许可零登录依赖
第二批（阶段 2）  Excel                           ← 企业级 L1 COM 通道、undo 栈清空问题、Adapter 抽象正式化
第三批（阶段 3）  Photoshop                       ← 纯脚本型应用、版本适配矩阵、长耗时与看门狗
第四批（阶段 4）  Explorer、Settings、7-Zip、Calculator、Word、PowerPoint、经典 Outlook（只读）、VS Code
                                                   ← 快速扩量，验证 Adapter 复用度（边际成本 ≤ 3 人日/个）
第五批（阶段 4）  股票类软件（只读 + 解读 + 图形展示）  ← 见 2.3；交易闸门预留但不启用
暂不接入          股票软件交易操作、微信/QQ、New Outlook 写操作、RDP/虚拟机内应用、游戏
```


---

## 3. 五个试点应用深度档案

> 选型原则：**由易到难，且每一个都负责验证一组不同的架构机制**。这不是「找 5 个应用练手」，而是「用 5 个应用把 v2 架构的每一条关键假设都压测一遍」。

| 顺序 | 应用 | 难度 | 主通道 | 负责验证的架构机制 |
|---|---|---|---|---|
| P1 | 记事本 Notepad | ★ | L3 UIA | UIA 读写、后置验证、L0 undo、多标签、**跨进程系统文件对话框** |
| P2 | Excel | ★★★ | **L1 COM** | L1 通道、**undo 栈被清空的处理**、L1 快照回滚、Protected View 与模态对话框状态机、大表性能 |
| P3 | 画图 Paint | ★★★ | L3 UIA + **L4 坐标** + **L7 视觉** | **坐标空间归一化（DPI/缩放）**、无结构画布、**容差断言（感知哈希）**、L0 undo 与像素快照 |
| P4 | Photoshop | ★★★★ | **L1 脚本（UXP/JSX/COM）** | 脚本通道优先、**长耗时操作与看门狗**、**版本适配矩阵**、historyState 回滚、重型应用的启动与许可态 |
| P5 | MS Edge | ★★★★★ | **L1 CDP** + L3 UIA + Win32 | **混合通道协同**、**不可逆动作后置**、**反提示注入（最真实场景）**、独立 profile 约束、下载/权限弹窗 |

---

### 3.0 执行顺序（v1.1 修订）

档案编号 P1~P5 保持不变（便于交叉引用），但**执行顺序已按决策修订**：

| 执行批次 | 应用 | 子阶段 | 为什么排在这里 |
|---|---|---|---|
| **第一批** | **P1 Notepad** | 阶段 1a | 最简单，用来把全部基础设施跑通；覆盖 L3 UIA + L1 文件契约 |
| **第一批** | **P3 Paint** | 阶段 1b | 覆盖 L4 坐标注入 + L5 视觉验证（与 Notepad 完全不同的技术路径） |
| **第一批** | **P5 Edge/Chrome** | 阶段 1c | 覆盖 L1 CDP + 混合通道；**把反注入与 DLP 底座提前到阶段 1** |
| 第二批 | **P2 Excel** | 阶段 2 | 覆盖企业级 L1 COM 通道与「undo 栈被清空」问题 |
| 第三批 | **P4 Photoshop** | 阶段 3 | 覆盖纯脚本型应用、版本适配、长耗时任务 |

**修订理由（相对 v1.0 的 Notepad → Excel 顺序）**：

1. **零许可、零登录依赖**：Notepad / Paint / Edge 都是 Windows 自带或免费浏览器，**agent 与 CI 可完全复现**。Excel 需要 Office 许可、Photoshop 需要订阅 + 登录 + 10~30 s 启动 —— 在「主要由 AI coding agent 执行」的模式下，这类依赖会让 agent 无法自测，只能靠人类手工验证，严重拖慢迭代。
2. **通道覆盖不减反增**：L3（Notepad）+ L4/L5（Paint）+ L1 CDP（Edge）+ L1 文件契约（Notepad 大文件降级）→ 阶段 1 就覆盖了 4 种通道，比原方案的 L3+L1 COM 更广。
3. **安全底座提前**：Edge 是提示注入最真实的来源，放进阶段 1 意味着 §12.4 四层反注入与 §12.6 DLP 在早期就被验收，而不是后期回头补。
4. **代价与补偿**：Excel 延后会使「COM 通道 + undo 栈清空」问题延后暴露 → 已在 **Spike F** 中用靶机应用模拟该行为提前验证。

---

### P1　记事本 Notepad（Windows 11 新版）

**技术栈**：WinUI 3 / MSIX 打包，进程 `Notepad.exe`；编辑区为富文本控件（支持 Value/Text Pattern）；**多标签**（`TabItem`）；自动保存草稿到 `%LocalAppData%\Packages\Microsoft.WindowsNotepad_*\LocalState\TabState`。

> ⚠ **版本分支**：Windows 10 的记事本是单文档 `Edit` 控件，AutomationId 与结构完全不同。Adapter 必须按 `app_version` 分支，或明确只支持 Win11 版本（建议后者，写进 `version_range`）。

**通道分析**

| 层 | 可用性 | 说明 |
|---|---|---|
| L1 API | ✗ | 无 COM/CLI（`notepad.exe 文件名` 只能打开文件） |
| L2 命令通道 | ✓ 部分 | `Ctrl+N/T/S/O/P/F/H`，`Ctrl+Tab` 切标签，`Alt+F4` |
| L3 UIA | **✓ 主通道** | 编辑区支持 Value/Text Pattern；标签、菜单、状态栏（行列号、缩放、编码）可读 |
| L4 合成输入 | ✓ 兜底 | 仅在 Pattern 不可用时；注意 IME（v2 §13.2.6） |
| L7 视觉 | 不需要 | — |

**定位要点**：编辑区、标签项、菜单项、状态栏（行/列、编码、缩放）、以及**「另存为」触发后出现的系统文件对话框**——该对话框属于 **Shell（Explorer）进程**，不是记事本进程，TargetDescriptor 必须能跨进程解析（这正是 v2 §3.2「element 不出 Host 进程、但 Host 要能解析多进程目标」的验证点）。

**可逆性档案**

| 动作 | 级别 | 策略 |
|---|---|---|
| 编辑文本 | **L0** | `Ctrl+Z` 多级 undo；**同时**做 L1 内容快照兜底 |
| 保存 `Ctrl+S` | **L1** | 保存前影子副本（v2 §9.4） |
| 另存为 | L1 | 目标文件若存在则先备份；**禁止静默覆盖** |
| 关闭未保存标签 | L3 | 会弹确认框 → 策略层禁止 Agent 选择「不保存」，除非用户显式批准 |

**坑清单**
1. 「另存为」是跨进程系统对话框，且**文件名输入框在对话框内的 AutomationId 稳定**（`1001`/`FileNameComboBox` 类），但不同 Windows 版本有差异 → 需版本记录；
2. 关闭未保存文档会弹「保存/不保存/取消」三态对话框 → 必须注册为 interrupt（v2 §8.6），且**默认选「取消」**；
3. 状态栏可被用户隐藏 → 不能用「状态栏存在」作为前置条件；
4. 编码（UTF-8/ANSI）与换行符（CRLF/LF）会影响文本读回的一致性 → 快照与断言要按字符而非字节；
5. 自动草稿恢复：重启记事本可能自动恢复上次标签 → 崩溃恢复场景（v2 S3）在此会表现得与预期不同，需实测；
6. 长文本（>1 MB）时 TextPattern 读取性能下降 → 大文件应改走 L1 文件通道（直接读磁盘），这正好验证 v2 §6.7「已知坑写进 App Map」的机制。

**建议的 3 个阶段 1 任务**
1. `T1.1` 打开指定文件 → 读取全文 → 报告行数与包含关键词的段落（**纯只读，验证定位与读取**）；
2. `T1.2` 把文档中所有「报表」替换为「报告」→ 保存（**验证 L0 undo + L1 快照 + 后置断言 + 审批与 diff**）；
3. `T1.3` 新建标签 → 写入内容 → 另存为到指定路径（**验证跨进程文件对话框 + 不覆盖已有文件 + 多标签身份管理**）。

**验收要点（postcondition 示例）**
```json
[
  { "kind": "text_not_contains", "value": "报表" },
  { "kind": "state_assert", "assert": "count(text,'报告') == expected" },
  { "kind": "file_changed", "path": "{{target_path}}", "hash_matches": "{{computed}}" },
  { "kind": "state_assert", "assert": "window.title not_matches '.*\\\\*.*'" }   // 保存后标题无未保存星号
]
```

---

### P2　Microsoft Excel

**技术栈**：原生 Win32/C++ 主程序 + COM 自动化服务器（`Excel.Application`）；UI 为自定义网格（UIA 有 Table/DataGrid 支持但**虚拟化**，只有可见区在树中）。

**通道分析**

| 层 | 可用性 | 说明 |
|---|---|---|
| **L1 COM** | **✓✓ 首选** | `Excel.Application` 覆盖几乎全部功能：工作簿/工作表/Range/公式/格式/图表/透视表/查询。可靠、可验证、可后台（不必前置窗口） |
| L2 命令 | ✓ | 快捷键与功能区；Ribbon 控件 UIA 可读 |
| L3 UIA | ✓ 辅助 | **用于两件事**：①验证 UI 状态（当前单元格、编辑栏、对话框）②处理 COM 覆盖不到的 UI（如某些向导） |
| L4/L7 | 兜底 | 一般不需要 |

**★ 最重要的架构结论（已核实）**：

> **任何触及工作表的程序化修改（VBA/COM）都会清空 Excel 的 undo 栈**；`Application.Undo` 只能撤销一个过程内的最后一次修改。
> **因此 Excel 的 COM 写入必须按 L1（快照可恢复）建模，绝不能声明为 L0。**

这直接验证了 v2 §9.1「L0 与 L1 不是互斥而是叠加，且必须以实测为准」以及 §9.2 陷阱 3/5 的必要性。Adapter 里 `undo_capability.level` 应写 `L1_snapshot`，并注明 `app_undo_unavailable_for_programmatic_changes = true`。

**可逆性档案**

| 动作 | 级别 | 策略 |
|---|---|---|
| 单元格/区域写入 | **L1** | 操作前读取原区域值 + 文件级影子副本；回滚 = 写回原值或还原副本 |
| 结构性改动（增删行列/表） | **L1** | 文件影子副本 + 反向脚本（记录插入位置） |
| 保存 | L1 | 保存前副本；**禁止 `DisplayAlerts = false`**（会静默覆盖） |
| 另存为 / 导出 | L2 | 新文件可直接删除（移入影子目录） |
| 发送邮件（Excel 无此功能，但 Outlook 联动有） | L3 | 强制人工确认 |

**坑清单**
1. **Protected View**：来自网络/邮件的文件以只读沙箱打开，UI 与 COM 行为都不同（`Workbook.ProtectedViewWindow`）→ 必须探测并显式处理（提示用户「启用编辑」需人工确认，因为这是安全边界）；
2. **文件被占用/只读**：另一进程打开时 COM `Workbooks.Open` 行为不同 → 前置探测 + 可读错误；
3. **AutoSave + OneDrive/SharePoint**：开启后每次修改立即上云，**影子副本回滚会与云端版本冲突** → 必须探测 AutoSave 状态并作为前置条件；
4. **模态对话框族**：另存为、覆盖确认、更新链接、宏安全警告、"文件未响应"→ 全部注册为 interrupt，且**只允许白名单动作**；
5. **大表性能**：UIA 全网格遍历不可行（虚拟化 + 数万单元格）→ **一律走 COM**，UIA 只读局部；
6. **公式 vs 值**：`Range.Value` 与 `.Formula` 与 `.Text`（显示文本）三者不同 → 断言必须指明读哪一个；
7. **`Application.ScreenUpdating` / `DisplayAlerts`**：性能优化常用，但 `DisplayAlerts=false` 会**静默覆盖文件**，违反「无静默失败」红线 → **架构层面禁用**，写进 Adapter 的 `forbidden_properties`；
8. **多工作簿/多窗口**：TargetDescriptor 必须包含 workbook + worksheet 身份，不能只到进程级；
9. **区域引用与命名区域**：优先用命名区域而非 `A1` 字符串（抗行列插入）；
10. **COM 生命周期**：必须显式释放（否则残留 EXCEL.EXE 进程）→ Host 需要 COM 对象生命周期管理与看门狗。

**建议的 3 个任务**
1. `T2.1` 打开指定工作簿 → 读取某表的某区域 → 汇总统计（**只读，验证 COM 通道与身份管理**）；
2. `T2.2` 向指定命名区域写入数据 → 重算 → 校验公式结果 → 保存（**验证 L1 快照 + 前置探测 Protected View/AutoSave + 后置断言用 `.Value` 而非 UI**）；
3. `T2.3` 生成一张图表并导出为 PNG（**验证 L2 补偿动作 + 导出路径校验 + 不覆盖已有文件**）。

---

### P3　画图 Paint（Windows 11 新版）

**技术栈**：WinUI 3；工具栏/面板/菜单为 XAML 控件（UIA 覆盖好，有 AutomationId）；**画布是单一自绘表面，无子控件结构**；新版支持**图层**、背景移除等 AI 功能。

**通道分析**

| 层 | 可用性 | 说明 |
|---|---|---|
| L1 API | ✗ | 无 |
| L2 命令 | ✓ | `Ctrl+N/O/S/Z/Y`、`Ctrl+滚轮` 缩放、工具快捷键（部分） |
| L3 UIA | ✓ **仅工具栏/面板/菜单** | 选工具、改颜色、切图层、读状态栏（缩放比例、坐标）可行 |
| **L4 坐标注入** | ✓ **画布操作唯一路径** | 画布内绘制只能靠坐标 + 拖拽 |
| **L7 视觉验证** | ✓ **必需** | 画布内容无法从 UIA 读回，只能截图比对 |

**这是本项目第一个「必须依赖视觉验证」的目标**，因此它的价值不在于画得好，而在于**逼我们把 v2 §6.9 坐标归一化、§7.4 断言体系、§7.6 视觉兜底全部做实**。

**可逆性档案**

| 动作 | 级别 | 策略 |
|---|---|---|
| 画布绘制 | **L0** | `Ctrl+Z` 多级；**同时**导出 PNG 作为像素快照（L1） |
| 图层操作 | L0 | 同上 |
| 保存/另存为 | L1 | 文件影子副本 |

**坑清单**
1. **画布坐标 ≠ 屏幕坐标**：受缩放比例（状态栏可读）与滚动偏移影响 → 必须做「画布坐标 ↔ 屏幕坐标」换算，并把缩放比例作为 TargetDescriptor 的一部分；
2. **DPI 与多屏**：正是 v2 §6.9 的验证点，混合缩放双屏下必须校准；
3. **抗锯齿/半透明导致像素不精确一致** → 断言不能用逐像素相等，必须用**容差比对或感知哈希（pHash/dHash）**，并声明 `confidence_min`；
4. **工具状态是隐式的**：当前选中的画笔/颜色/粗细必须先验证再画，否则「画了但用错工具」是典型静默失败；
5. **拖拽操作的原子性**：一次拖拽中途被打断会留下半成品笔画 → 拖拽期间必须独占租约并暂停接管检测的自动恢复；
6. **图层面板**：新版有图层，绘制落在哪个图层取决于当前选中图层 → 前置条件必须包含「当前图层已确认」；
7. **AI 功能（背景移除等）需要联网且耗时不可预测** → 列为不支持能力，或走 `NeedsHuman`。

**建议的 3 个任务**
1. `T3.1` 新建画布 → 选择矩形工具与指定颜色 → 在指定画布坐标画一个矩形 → **截图验证形状与颜色**（验证坐标归一化 + 容差断言）；
2. `T3.2` 打开一张 PNG → 读取尺寸与缩放 → 在某区域绘制标记 → 另存为新文件（验证文件对话框 + 不覆盖 + 像素快照回滚）；
3. `T3.3` 执行一次绘制 → 用户点「撤销」→ **验证像素回到快照**（验证 L0 undo + 像素级回滚确认，这是 v2 §9 的核心场景）。

---

### P4　Adobe Photoshop

**技术栈**：原生 C++ 应用，**UI 全自绘**（UIA 覆盖差）；提供多层脚本接口：**UXP Scripting（新，PS v22+）**、**ExtendScript/JSX（旧，Adobe 正在逐步退场）**、**Windows COM `Photoshop.Application`**、**Action Manager（底层，可录制/回放动作）**；2026 版为 v27。

**通道分析**

| 层 | 可用性 | 说明 |
|---|---|---|
| **L1 脚本** | **✓✓ 唯一可靠通道** | UXP / JSX / COM 均可：打开文档、读写图层、选区、滤镜、导出、读取直方图与像素 |
| L2 命令 | ✓ 部分 | 菜单项可通过脚本触发（比点击可靠） |
| L3 UIA | **✗ 基本不可用** | 面板/工具多为自绘；只能读到少量顶层结构 → **不要在此投入** |
| L4/L7 | ✗ 不建议 | 自绘 + 高 DPI + 面板可自定义布局 → 坐标极不稳定 |

**★ 架构价值**：Photoshop 是「**L1 优先原则**」（v2 §2.1）最极端的证明——**当 UI 完全不可自动化时，唯一的出路是官方脚本接口**。如果这个试点成功，说明 Adapter 规范必须支持「纯脚本型应用」（无 UIA 依赖），这对后续接入 AutoCAD、Illustrator、Blender 等同类应用是模板。

**可逆性档案（本试点最强的一点）**

| 动作 | 级别 | 策略 |
|---|---|---|
| 任何脚本修改 | **L0（强）** | **History 状态可编程访问**：记录操作前的 `historyState`，失败时 `doc.activeHistoryState = savedState` 精确回滚 —— **比 Ctrl+Z 计数可靠得多**，应作为 L0 的**首选实现**写入 v2 §9.3 方案 C（应用锚点） |
| 文件保存 | L1 | PSD/TIFF 影子副本 |
| 导出 | L2 | 导出文件可直接删除（移入影子目录） |
| 批处理动作（Action 回放） | L0/L1 | 取决于动作内容；含「存储为」的动作按 L1 |

**坑清单**
1. **启动慢（10~30 s）+ 首选项/登录/许可弹窗** → 连接阶段必须有长超时与「就绪探测」（不能只探测进程存在）；
2. **版本差异大**：UXP 与 JSX 的 API 面不同；Adobe 正在**逐步结束 ExtendScript 支持**（Premiere Pro 已于 2026-09 结束）→ Adapter 必须声明 `version_range` 并做**通道探测**（优先 UXP，回退 JSX/COM），把结果写进 Capability Matrix；
3. **脚本执行上下文**：部分操作要求文档为活动文档；多文档打开时需显式激活 → 激活会改变用户前台，需租约与接管协调；
4. **内存与大文件**：大 PSD 操作耗时不可预测 → 必须走看门狗 + 可取消（v2 §8.9），且**取消后状态不确定 → 按 v2 §7.7 幂等判定处理**；
5. **颜色模式/位深/配置文件**会影响像素级断言 → 视觉验证需在同一色彩空间下比较；
6. **「最大化兼容性」保存对话框**、**字体缺失对话框**、**插件冲突** → 注册为 interrupt；
7. **无头/后台运行受限**：Photoshop 需要有图形会话，不能服务化 → 无人值守形态（v2 §10.7）在此不适用。

**建议的 3 个任务**
1. `T4.1` 打开指定 PSD → 列出图层树（名称/可见性/尺寸）→ 导出为 PNG（**只读 + 导出，验证脚本通道与就绪探测**）；
2. `T4.2` 在指定图层上执行一次可逆修改（如调整色阶）→ 校验直方图变化 → **失败时用 historyState 精确回滚**（验证 L0 强回滚 + 后置断言用数据而非像素）；
3. `T4.3` 批量：对一个目录内 N 个文件执行「缩放到指定宽度并导出」→ 逐个校验尺寸 → 生成报告（验证长任务、预算、看门狗、部分失败处理与 L2 补偿）。


---

### P5　Microsoft Edge（浏览器）

**技术栈**：Chromium 内核；浏览器外壳（地址栏、标签、下载栏、权限弹窗）为原生 + Chromium UI；网页内容为 Blink 渲染树；支持 **CDP（Chrome DevTools Protocol）** 与 **WebDriver BiDi**；Windows 上 Chrome 138+ 默认启用原生 UIA。

**通道分析**

| 层 | 可用性 | 适用对象 |
|---|---|---|
| **L1/L2 CDP** | **✓✓ 网页内容首选** | DOM 读写、点击、导航、截图、网络拦截、执行 JS、下载控制 |
| L3 UIA | ✓ | **浏览器外壳**：地址栏、标签页、书签栏、下载栏、权限/密码弹窗；也可读网页内容（树巨大，仅在 CDP 不可用时用） |
| Win32/UIA | ✓ | **系统级对话框**：文件上传/下载/打印（属于系统或 Edge 的原生对话框） |
| L4/L7 | 兜底 | 极少数 canvas 类页面 |

**★ 最大的现实约束（已核实）**：

> **Chrome / Edge 136 起，`--remote-debugging-port` 对默认用户数据目录不再生效**（微软与 Google 均确认此安全变更，用于防止 Cookie 窃取），**必须同时指定自定义 `--user-data-dir`**。

后果与对策：

| 后果 | 对策 |
|---|---|
| **无法附加到用户日常使用的 Edge 实例** | 必须由助理启动一个**专用自动化 profile** 的实例；这在产品上是可接受的，但要向用户解释 |
| 专用 profile **没有用户的登录态** | 三条路径：①首次由用户在专用 profile 中登录（推荐，最安全）②任务限定在无需登录的站点 ③**禁止**从用户 profile 复制 Cookie（安全红线，等同于凭据窃取） |
| 企业策略可能禁用远程调试 | capability probe 必须探测并如实上报；不可用时降级到 UIA + 明示可靠性下降 |
| 两个 Edge 实例并存会让用户困惑 | UI 中显著标注「自动化窗口」，并给专用 profile 设置独立主题色/窗口标题前缀 |

**可逆性档案（最复杂的混合体）**

| 动作 | 级别 | 策略 |
|---|---|---|
| 导航 | **L2** | 可后退；记录 history 栈 |
| 表单填写（未提交） | **L1** | 记录原值，可脚本还原 |
| DOM 修改（本地） | L1 | 可脚本还原，但刷新即失效 |
| 下载文件 | L2 | 移入影子目录 |
| **提交表单 / 下单 / 发消息 / 发帖** | **L3 不可逆** | 强制人工确认 + 不可逆动作后置（v2 §9.5） |
| 输入凭据 | **禁止** | 策略层拒绝（凭据只能由用户手工输入，v2 §12.5） |

**坑清单**
1. **提示注入面最大**：网页内容是不可信输入的典型来源（v2 场景 S5）→ 本试点是**四层反注入机制（v2 §12.4）的验收场**；
2. **Cookie 同意横幅 / 登录墙 / 验证码 / 人机验证** → 需要 interrupt 处理与 `NeedsHuman` 升级，**禁止尝试绕过验证码**（合规红线）；
3. **反自动化检测**：部分站点检测 `navigator.webdriver` / CDP 特征 → 不应对抗（合规），而应如实报告「该站点不支持自动化」；
4. **iframe / shadow DOM / canvas 页面** → CDP 定位复杂度上升，需要在 Adapter 中声明站点级已知坑；
5. **下载行为**：下载目录、「保留/放弃」安全提示、多文件下载 → 需要显式配置下载目录到影子区并校验；
6. **多标签/多窗口身份**：TargetDescriptor 必须能稳定标识「哪个标签」（CDP 的 targetId 会变）→ 用 URL + 标题 + 自定义标记（如注入的 `data-*` 或 `window.name`）组合，**不能只用 tab index**；
7. **页面异步加载** → `settle` 判定要用 CDP 的 load/networkIdle 信号，而不是像素静止；
8. **企业策略/扩展**：用户已装的扩展可能干扰 → 专用 profile 默认禁用扩展；
9. **凭据与支付页面** → 策略层默认**黑名单域名/页面类型**（银行、支付、密码修改），命中即拒绝。

**建议的 3 个任务**
1. `T5.1` 打开指定站点 → 提取某列表页的结构化数据 → 写入 Excel/CSV（**只读，验证 CDP 通道 + 专用 profile + 数据落地，且是「股票软件数据 → Excel」样板的替代验证**）；
2. `T5.2` 在某表单站点填写表单 → **停在提交前** → 展示 diff 与来源归因 → 用户确认后提交（**验证不可逆动作后置 + HITL + L3 处理**）；
3. `T5.3` 打开一个**含隐藏指令的测试页面**（我们自己造的注入靶页）→ 验证 Agent **不执行**页面中的指令，且审批卡片正确显示来源归因为 `app_content`（**验证反注入四层机制，这是安全验收，不是功能验收**）。

> `T5.3` 应当被当作**安全回归测试**长期保留在 CI 中（v2 §17.5），每次改动策略层都必须重跑。

---

## 4. 修订后的开发路线（覆盖 v2 §20）

原 v2 §20 的阶段划分假设「1 个主程序」。现在被控对象是**一族不可修改的通用应用**，因此路线调整为「**先 Windows 纵深，再跨平台横扩**」：

| 阶段 | 周期 | 内容 | DoD（关键项） |
|---|---|---|---|
| **0　Spike** | 2~3 周 | A~G 七个 Spike（A：Notepad UIA + Paint 坐标实测与接口考古；B：跨进程 Host；C：工具选择准确率；E：Tauri 审批卡/拾取器原型；F：撤销闭环含「undo 栈被清空」模拟；G：Edge CDP 专用 profile + 注入靶页）+ **H：存储方案 PoC（SQLite-WAL + blob 去重 + 性能预算实测）** + **D-lite：Linux/Wayland 早期侦察（不阻塞主线）** | 每个 Spike 有量化 go/no-go；产出 `SPIKE_REPORT.md` |
| **1　Notepad → Paint → Edge 闭环** | 10~12 周 | 1a Notepad（4 周，基础设施 + L3 UIA + L1 文件契约）→ 1b Paint（3 周，L4 坐标 + L5 视觉 + 容差断言）→ 1c Edge（3~4 周，L1 CDP + 反注入四层 + DLP 三档）。含策略引擎、状态机、后置验证、指纹、租约、撤销（L0/L1/L2）、审计、存储四层、时间线、审批卡片、拾取器 v0、靶机应用 v0、录制回放 v0 | 9 个任务连续 10 次成功率：Notepad ≥ 90%、Paint ≥ 75%、Edge ≥ 75%；**静默失败 = 0**；撤销成功率 ≥ 95%；**注入靶页 0 次执行页面指令**；四类异常可恢复 |
| **2　Excel + Adapter 抽象正式化** | 6~8 周 | 在 3 个真实 Adapter 之后抽象 Adapter 规范；L1 COM 通道；「undo 栈被清空」的 L1 快照策略；Protected View/AutoSave 前置探测；自愈定位；App Map 生成器 | Excel 3 个任务成功率 ≥ 85%；**COM 写入 100% 走快照、0 次误报可 undo**；新增一个 T1 应用的边际成本 ≤ 3 人日 |
| **3　Photoshop + 评测体系** | 6~8 周 | 纯脚本型应用（UXP → JSX → COM 探测）；版本适配矩阵；长耗时与看门狗；historyState 精确回滚；基准任务集与指标看板 | PS 3 个任务成功率 ≥ 75%；historyState 回滚 100% 可靠；启动就绪探测无误判 |
| **4　扩量 + 股票只读 + 图表面** | 持续 | Explorer / Settings / 7-Zip / Calculator / Word / PowerPoint / 经典 Outlook（只读）/ VS Code；股票类只读通道（官方 API / 导出 / 剪贴板）+ `analysis.*` `chart.*` 工具 + UI 图表面 | 每新增一个应用的边际成本 ≤ 3 人日；股票数据双通道交叉校验一致率 ≥ 99% |
| **5　macOS** | 8~10 周 | AX 通道；TCC 权限引导；同族应用（TextEdit/Numbers/Preview/Safari） | — |
| **6　Linux（Wayland-first）** | 10~12 周 | 按 v2 §13.4；先 KWin 后 GNOME；只支持 T1/T2 的 Linux 对应物（gedit/LibreOffice/GIMP/Firefox） | Spike D 通过项才承诺 |
| **7　开放生态** | 可选 | 外部 MCP、插件治理、OS 原生 agent 通道、无人值守 | 可以永远不做 |

**排期变化的理由**：
- 阶段 1 从「1 个应用」扩为「2 个应用」，因为 **L1 与 L3 是两条完全不同的通道**，只验证一条会导致抽象错误（v2 §20.3 的「不要过早抽象」原则要求至少两个真实 Adapter）；
- Photoshop 放在阶段 2 而非更后，因为它是**纯脚本型应用**的唯一代表，会显著影响 Adapter 规范设计；
- Edge 放在阶段 3，因为它是**难度最高且引入新安全面**的目标，需要前面的机制都已稳定。

---

## 5. 接口考古清单（Spike A 的可执行操作步骤）

对每一个待接入应用，按此顺序做，**每步都要记录结果**（结果直接填进 Adapter 的 `connect.toml` 与 App Map）：

```text
① 版本与形态
   · 记录精确版本、安装路径、是否 MSIX/Store 打包、是否需登录/许可
   · 进程名、是否多进程（浏览器/Electron 会有多个）

② L1：官方接口
   · COM：注册表 HKCR 下搜 ProgID（如 Excel.Application / Photoshop.Application /
          Shell.Application / Outlook.Application）；或用 OLE/COM Object Viewer
   · CLI：安装目录与 PATH 中是否有可执行工具（7z.exe / code / msedge --help）
   · 脚本：是否有 VBA / JSX / UXP / AutoLISP / Python 插件机制
   · URI scheme：注册表 HKCR\<scheme>\shell\open\command（如 ms-settings: / microsoft-edge:）
   · DDE / 命名管道（老应用）
   · 文件契约：应用的工作文件是否可直接读写（如 .txt/.csv/.png）——★ 最容易被忽略但最可靠

③ L2：命令通道
   · 快捷键清单（官方文档 + 实测）
   · 命令面板 / 菜单加速键

④ L3：UIA 覆盖度实测（用 Accessibility Insights for Windows 或 SDK 的 inspect.exe）
   · 悬停关键控件，记录：ControlType、AutomationId、Name、支持的 Pattern
   · 统计：可见控件数、有 AutomationId 的比例、有可调用 Pattern 的比例
   · 大树性能：全窗口遍历耗时（多次取中位数）
   · 虚拟化：列表/表格是否只有可见项

⑤ 状态与对话框普查
   · 枚举所有会出现的模态对话框（首启动、更新、保存冲突、许可、字体缺失、Protected View…）
   · 每个对话框记录：触发条件、标题、按钮、安全默认选项

⑥ 可逆性实测
   · 手工执行典型动作 → 测 Ctrl+Z 是否可逆、粒度、深度
   · ★ 程序化修改后测 undo 栈是否仍存在（Excel 的答案是"不存在"）
   · 是否有更强的锚点机制（Photoshop historyState、Office 版本历史、IDE Local History）

⑦ 环境依赖
   · 是否需提权（右键"以管理员身份运行"才能操作的目标）
   · 是否需联网/登录/许可
   · 是否会弹 UAC / 安全警告

⑧ 合规
   · 阅读 EULA / 服务条款，确认是否禁止自动化
   · 涉及账户、资金、个人数据的，直接评估是否列入 Non-goals
```

**产出物**：每个应用一份 `adapters/<app_id>/SPIKE_REPORT.md`，包含以上 8 项结果 + 十维打分 + 分级结论 + 建议任务集。这份报告是 Adapter 的输入，也是 v2 §17.5 基准的基线。

---

## 6. 对架构 v2 的回填决策

本次输入解决了 v2 §23 的以下开放问题：

| 编号 | 决策项 | 结论 |
|---|---|---|
| **D2** | 主程序能否修改 | **不能**。被控对象为通用 Windows 常用应用 → L1 通道改为「接口考古」而非「协商加接口」；Adapter 成为核心资产与主要工作量 |
| **D5** | 首个平台 | ✅ **Windows 11 24H2+**（阶段 1~4）；Win10 仅对跨版本一致的目标提供 C 级尽力支持（§2.5）；macOS 阶段 5，Linux 阶段 6 |
| **D6** | 是否含浏览器 | ✅ **是**（Edge 为主，Chrome 为同 Adapter 变体）→ **提前到阶段 1c**；必须处理 Chrome/Edge 136+ 的 `--user-data-dir` 约束与专用 profile 登录态问题（P5） |
| **D9** | 第三方技能 | 远期（阶段 7），字段预留 |
| — | 新增 Non-goal | **股票/交易类软件的下单、撤单、改单、转账、开户等资金操作**；社交客户端（微信/QQ）消息发送；绕过验证码/风控 |
| — | 新增 Non-goal | **禁止从用户浏览器 profile 复制 Cookie/凭据**到自动化 profile |
| — | 新增 Non-goal | **禁止 `DisplayAlerts=false` 类「静默跳过确认」的应用属性设置**（违反无静默失败红线） |

**v1.1：以上问题已全部回答**，结论汇总：

| # | 问题 | 结论 | 落点 |
|---|---|---|---|
| 1 | D1 无人值守 | **暂不支持，但三处预留接口**（类型/契约/能力/UI），未来仅在白名单应用上开放 | v2 §1.2、§9.7 |
| 2 | D7 数据出域 | **由用户选择且随时可改**：`local_only / redacted / full` 三档 + 逐应用与逐内容类型覆盖；默认 `redacted`；降级不静默、升级需显式说明 | v2 §12.6.1 |
| 3 | D3 核心语言 | **Rust**（人类通过阅读逐步熟悉）→ 配套要求：命名一眼可懂、注释密度偏高、受控词汇表 | gov §6 |
| 4 | Office / Adobe 版本 | Office **2019+**（2016 技术可用但不承诺）；Adobe 用户设定 2020+ → **修正为 Photoshop 最低 2021(v22)**，因 2020 无 UXP | §2.5、v2 §13.6 |
| 5 | Windows 基线 | **Windows 11 24H2/25H2 为唯一正式基线**；Win10 已 EOL（2025-10-14）且记事本/画图结构不同 → 仅对 Excel/Edge/7-Zip 提供 C 级尽力支持 | §2.5、v2 §13.6.1 |
| 6 | 分发形态 | 当前内部使用，但**按开源规范建设**，未来脱敏上 GitHub | v2 §18.1 |
| 7 | 股票软件 | **只读 + 解读 + 图形展示**（不操作股票软件 UI）；保留 `TradingGate` 闸门与接口占位，未来经厂商官方接口有限开放 | §2.3、v2 §9.9 |

**仍需在阶段 0 实测确定的技术细节**：Office 各版本的准确区分方式（M1）、Adobe 2020 能否登录激活与 Illustrator 的 UXP 起始版本（M2）、本地模型选型（M3）、内部是否存在 Win10 机器（M4）、开源许可证最终选择（M5，建议 `MIT OR Apache-2.0`）。

