# 自动化轮次 2026-09-26 / round-3

## 运行上下文

- automation：`ai-assistant-two-hour-0000`
- 计划时刻：2026-09-27 00:00 +08；实际开始：2026-09-27 00:01:30 +08
- 运行日：按章程 §11.3，00:00~11:59 归属 **2026-09-26**
- 分支：`automation/2026-09-26`
- 仓库锁：开始时不存在；本轮建立 `.automation.lock`，收尾释放。
- 轮次：`main` 上已有 `2026-09-26-round-1*.md` 两个文件；为避免覆盖，本轮使用 **round-3**。

## 本轮完成

- 从 `origin/main` 新建 `automation/2026-09-26`。
- 选择下一张 Ready 卡 **TASK-207**：`crates/core` Planner。
- 新增 `Planner` / `PlannerRequest`：调用方持有 plan/task identity、goal、预算与工具目录；模型只返回单一 `steps` JSON 数组。
- Provider 流按事件校验、传播取消、单次 poll ≤ 500 ms；拒绝不可解析 / 缺字段 / 类型错误 / 空 steps / tool-call / 非 Stop finish / 无 finish。
- 解析结果直接复用 `assistant-task-engine::PlanStep`，先 `Plan::validate()`，再拒绝目录外工具。
- 新增 14 个 Planner 测试与 3 个 ADR-0053 workspace 依赖白名单 arch 测试。

## 依据

- `AGENTS.md`、`PLAN.md`、`plans/stage-1-pilots.md`、`tasks/TASK-207-core-planner-plan-step-dag.md`
- `docs/spec/core-orchestration.md`、`docs/adr/0053-core-orchestration-layer-interface.md`
- `crates/core/README.md`、`crates/task-engine/src/{lib,plan,error}.rs`、`crates/model-gateway/src/{model,provider,error}.rs`
- `docs/memory/rejected.md`、`MEMORY.md`、`LEDGER.md` 末 10 行
- 自动化章程 §11.2 / §11.3 / §11.4 与操作手册 §4.1.b / §4.1.c

## 验收

- G1：`cargo 1.98.1` / `rustc 1.98.1`。
- G2 基线：`cargo fmt --all --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test --workspace` 全绿。
- G3：工作区干净。G4：从 `origin/main` 新建 `automation/2026-09-26`。
- G5：TASK-207 为 Ready。G6：`xtask hygiene` 可运行。G7：磁盘剩余 > 2 GB，开始时间 < 05:00。G8：仓库锁获取成功。
- 卡内 14 条命令：`assistant-core` 44 tests（5 arch layering + 3 arch dependencies + 10 context + 14 planner + 12 session）；`arch::` 8 tests；`hygiene` 0E/4W、`docscan` 0E/468W、`card-check` 0E/27W；schema / codegen / memory / ADR / ledger / deny 全 PASSED。
- `cargo llvm-cov -p assistant-core --fail-under-lines 85`：PASS，行覆盖 **88.60%**。

## 遗留

- Planner 按卡面契约不承担路由、重试、降级或工具执行；这些仍由 `model-gateway` 与后续装配层负责。
- 下一张 Ready 卡：**TASK-206**（storage `memory_fts` FTS5），随后 TASK-208（Core Memory）。
- 本轮未操作真实 GUI、未读取凭据、未引入第三方依赖、未放宽 lint。

## 分支与提交

- 分支：`automation/2026-09-26`
- 实现提交：pending（写本文件时尚未提交；最终提交哈希由后续报告/PR 记录）
