# 自动化轮次 2026-09-26 / round-1

## 运行上下文

- automation：`ai-assistant-1615-next-card`
- 运行开始：2026-09-26 16:57 +08
- 仓库锁：开始时不存在；本 run 建立 owner `ai-assistant-1615-next-card` 的 `.automation.lock`，收尾自删。
- 当前 checkout 初始为上一轮遗留分支 `codex/task-206-storage-memory-fts5-search`（仅本地，未 push）。

## 判定与执行

- 上一轮把 TASK-206 停在 `DRIFT-206-1`（全局迁移登记表不在 write scope），该偏差不阻塞 TASK-028；按 ADR-0046 D8 跳过 206。
- 从 `main` 新建 `codex/task-028-core-session-context`，实现拆卡后的 Core 会话 + 上下文主卡。
- 交付 `SessionManager` / `SessionSnapshot` / `SessionStore` / `MemorySessionStore` / `ContextManager` / `ContextBudget`，并把 store、clock、compressor 全部改为注入边界。
- 发现 storage 无 conversation/session 公开记录 API，登记非阻塞 `DRIFT-028-6` 与 PL-092；未越界改 `crates/storage`。

## 验收

- `cargo fmt --all --check` PASS。
- `cargo clippy --all-targets -- -D warnings` PASS。
- `cargo test --workspace` PASS；`assistant-core` 27 个测试（5 arch + 10 context + 12 session）全绿。
- `verify-schemas` / `codegen --check` / `hygiene` / `docscan` / `card-check` / `memory-counts` / `adr-index` / `check-ledger` / `cargo deny check` PASS。
- `cargo llvm-cov -p assistant-core --fail-under-lines 85` PASS，行覆盖 92.62%。

## 遗留

- `TASK-206` 仍受 `DRIFT-206-1` 阻塞。
- PL-092：storage conversation/session 记录 API 缺失，生产 `SessionStore` adapter 必须在 TASK-029 装配前补齐。
- 下一张可推进卡：`TASK-207` Planner。
