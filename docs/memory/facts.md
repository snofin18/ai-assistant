# facts.md — 已验证事实（FACT）

> 标签 `[FACT]` = 有来源或实测支撑、当前仍然成立的结论。
> 若某条日后被推翻：不要删，追加一条 `[FACT][supersedes:日期]` 说明。

---

> **本文件是 `MEMORY.md` 分层结构（ADR-0021）的 L1 层之一。**
> 写入规则见 `MEMORY.md`「条目格式」与「更新职责」；路由规则见 `docs/memory/README.md`。
> **只追加，不改写他人条目**；更正用新条目 + `[supersedes:日期]` 标注。
> 应用专属的条目**不进本文件**，进 `docs/memory/apps/<app>.md`。

### 平台与生态

- [2026-09-16][FACT][src:GNOME 50 发布说明] **GNOME 50（2026-03）已从 Mutter 完全移除 X11 后端**，成为 Wayland-only（X11 应用仍可经 XWayland 运行）→ Linux 侧必须 Wayland-first，不得再按 X11 会话设计。
- [2026-09-16][FACT][src:Fedora 43 / Ubuntu 25.10 发布说明] Fedora 43（2025-11）移除 GNOME on Xorg 会话；Ubuntu 25.10（2025-10）移除 Xorg 会话，26.04 LTS 延续。
- [2026-09-16][FACT][src:Microsoft 支持文档] **Windows 10 已于 2025-10-14 结束支持**（消费者 ESU 仅延至 2026-10）→ 不作为正式基线。
- [2026-09-16][FACT][src:Qt 6 文档 QAccessible] Qt 的 AT-SPI 桥在 DBus 属性 `org.a11y.Status.ScreenReaderEnabled` 为 true 时激活；替代方式是环境变量 `QT_LINUX_ACCESSIBILITY_ALWAYS_ON=1` → Linux 上 Qt 应用的 a11y **可以不重启应用就激活**。
- [2026-09-16][FACT][src:at-spi2-core bus/README + cua blog] `at-spi-bus-launcher` 读取 gsettings `org.gnome.desktop.interface toolkit-accessibility` 并据此设置 `org.a11y.Status` → Tier 0 激活开关就是这条 gsettings。
- [2026-09-16][FACT][src:at-spi2-core devel docs] **GTK4 直接实现 AT-SPI DBus（不经 ATK）**；GTK3 仍走 ATK 桥；ATK 自 2.46 起弃用 → GTK4 应用的 a11y 覆盖度需逐应用实测，"有节点但无 action"是已知形态。
- [2026-09-16][FACT][src:AT-SPI 接口定义] `org.a11y.atspi.EditableText` 提供 `SetTextContents` / `InsertText` / `DeleteText`；`Action.DoAction` 可触发点击类动作 → **Wayland 下无需注入输入即可读文本、写文本、点控件**（走 DBus，与合成器无关）。
- [2026-09-16][FACT][src:Wayland 协议现状] Wayland **无全局窗口列表 API**（社区仍在讨论 Top Level Tag）；无任意输入注入与任意截屏 → 窗口枚举改用 AT-SPI 根节点（APPLICATION → FRAME），身份主键改用 `app_id`。
- [2026-09-16][FACT][src:wlr 协议采用情况] `wlr-virtual-pointer` / `wlr-screencopy` **KWin 与 Mutter 均不实现** → Linux 必须按**合成器家族**（Mutter / KWin / wlroots / 其他）分流，不能按发行版。
- [2026-09-16][FACT][src:KDE bug 497778、input-leap issue 2258] KDE 的 portal `restore_token` 持久化存在已知 bug；**锁屏会移除 libei 输入设备** → 长任务必须监听会话状态并在锁屏时暂停。
- [2026-09-16][FACT][src:cua blog 2026-06-18] 业界开源 Linux computer-use 驱动（cua-driver 0.5.7）**正式支持的仍是 X11/XWayland，原生 Wayland 仍是 preview**（`CUA_DRIVER_RS_ENABLE_WAYLAND=1`），且原生 Wayland-only 应用可能完全不可见 → Wayland-first 是可行但需自建的方向，不能指望现成驱动。
- [2026-09-16][FACT][src:MCP 规范与 rust-sdk] MCP 最新规范为 **2026-07-28**（stateless-first、Extensions、多轮往返请求取代 elicitation、Tasks 移出核心）；官方 Rust SDK 为 **`rmcp`**，兼容 2025-11-25 及更早。规范把更多安全责任交给实现方 → 权限/审批/审计必须自持。
- [2026-09-16][FACT][src:Chrome/Edge 136 变更] **Chrome 与 Edge 136 起，`--remote-debugging-port` 对默认用户数据目录失效**，必须同时指定自定义 `--user-data-dir`（防 Cookie 窃取）→ 无法附加用户日常浏览器实例，必须专用 profile。
- [2026-09-16][FACT][src:Chrome 开发者博客] Chrome 138 起 Windows 上**默认启用原生 UI Automation**（此前需 `--force-renderer-accessibility`）。
- [2026-09-16][FACT][src:Microsoft Q&A] **New Outlook 不支持 COM / VSTO / VBA 扩展模型**；经典 Outlook 支持 → 两者是两个完全不同的目标，New Outlook 写操作列为 T3。
- [2026-09-16][FACT][src:Excel VBA/Undo 文档与社区一致结论] **任何触及工作表的程序化修改都会清空 Excel 的 undo 栈**；`Application.Undo` 只能撤销一个过程内的最后一次修改 → Excel 的 COM 写入必须按 **L1 快照**建模，禁止声明 L0。
- [2026-09-16][FACT][src:Adobe 开发者文档] **UXP 是 Photoshop v22.0（2021）及以后才有的插件/脚本平台**；Photoshop 2026 = v27。Adobe 正逐步结束 ExtendScript 支持（Premiere Pro 已于 2026-09 结束）→ 通道探测顺序 UXP → JSX → COM，JSX 标注 `deprecated_upstream`。
- [2026-09-16][FACT][src:Office 版本体系] Office 2016 / 2019 / 2021 / Microsoft 365 的**内部版本号都是 16.0**，`Application.Version` 无法区分 → 需用 `Application.Build` 或注册表 `ClickToRun\Configuration` 的 `DisplayVersion`/`ProductReleaseIds`【具体方式待实测 M1】。
- [2026-09-16][FACT][src:Windows 平台动向] 微软在 Build 2026 将 Windows 定位为 agent 平台（identity/isolation/containment/governance），Agent Workspace 与 Copilot Actions 自 2025-11 起 Insider 灰度；Microsoft Agent Framework 1.0 已发布但**无 Rust SDK** → 预留 `PlatformAgentChannel`，把资产沉淀在 Adapter 而非平台胶水。
- [2026-09-16][FACT][src:Windows 安全机制] UIPI 阻止低完整性进程操作提权窗口 → 操作管理员身份运行的目标需要独立提权 Host；Session 0 隔离使服务无法直接访问交互桌面。
- [2026-09-16][FACT][src:macOS TCC 行为] 签名身份变化 / 重装 / 移动位置会**重置 TCC 授权**（辅助功能、屏幕录制等）→ 必须用稳定 Developer ID 签名，并在更新流程中检测与引导重新授权。

### 项目自身

- [2026-09-16][FACT][src:本项目定位] 被控对象是**不可修改源码的通用 Windows 应用** → L1 通道的工作是「接口考古」而非「协商加接口」；App Adapter 是核心资产与主要工作量。
- [2026-09-16][FACT][src:行业观察 2026 中] computer-use agent 在 OSWorld 类基准可达约 85%，但真实长流程业务失败率仍高 → **基准分 ≠ 生产可用性**；可靠性来自 App Adapter 契约，不来自模型更聪明。
- [2026-09-16][FACT][src:v2 §21] 25 个真实场景推演中，**没有一个能靠"模型更聪明"解决**，全部依赖确定性工程机制（后置验证、租约、指纹、幂等、HITL）。
- [2026-09-16][FACT][src:本机实测 TASK-001] Rust 工具链 1.98.1（stable-x86_64-pc-windows-msvc）+ clippy/rustfmt/llvm-tools 已就位。`winget install Rustlang.Rustup` 下载 rustup-init 耗时约 **14 分钟**（static.rust-lang.org 慢；DeliveryOptimization 先超时失败、WinINet 重试成功）→ 装工具链要预留时间，别以为卡死了。
- [2026-09-16][FACT][src:Codex 桌面 automation 实测] `automation_update` 的 `kind="cron"`（独立项目任务、每次全新会话）在本机**创建失败**（返回 `Failed to create automation.`；日志显示非 ChatGPT 鉴权，本机用自定义 model provider）。只有 `kind="heartbeat"` 可用，且**一个 thread 只允许一个 heartbeat**、必须挂在已存在的 thread 上。
- [2026-09-16][FACT][src:xtask 实测] `rustfmt` 的 `wrap_comments` / `comment_width` / `format_code_in_doc_comments` / `normalize_comments` 在 **stable 通道不生效**，只打印一行 warning。
- [2026-09-16][FACT][src:xtask 实测] clippy `pedantic`+`nursery` 全开 + `-D warnings`，在约 2800 行 Rust 上产生 **12 条**必须处理的告警（`option_if_let_else`、`format_collect`、`needless_pass_by_value`、`missing_const_for_fn`、`doc_markdown`、`useless_let_if_seq`、bool→int）→ 可行，但要边写边按这些习惯来，事后收拾成本高。
- [2026-09-16][FACT][src:xtask 实测] `cargo test` 的工作目录是 **package 根**（`xtask/`）而不是 workspace 根 → 测试里不能用相对路径假设 cwd；要仓库根就用 `env!("CARGO_MANIFEST_DIR")` 的父目录推导。
- [2026-09-17][FACT][src:探针实测 2026-09-17][supersedes:2026-09-16] `automation_update` 的 `kind="cron"` 在本机**能创建也能触发**（旧条目"创建失败"的记载有误）。真实原因：cron 强制要求显式提供 `model` + `projectId` + `executionEnvironment="local"`，缺任一只回一句 `Failed to create automation.` 而不指明是哪个字段。实测两次触发（约定 11:16/11:24 → 实际 11:17:54/11:25:54，约 **+2 分钟 jitter**），各新建独立会话。
- [2026-09-17][FACT][src:探针实测 2026-09-17] **cron 与 heartbeat 共用同一投递机制**：automation 的 prompt 一律被包装成合成 `FunctionCallOutput{id:None, call_id:None, name:"automation_update", namespace:"codex_app"}` 提交，**不是** user message。→ 换 `kind` 无法规避畸形条目问题，"每轮全新会话"能拿到、"干净投递"拿不到。
- [2026-09-17][FACT][src:CLI 实测 2026-09-17] `codex.exe exec --skip-git-repo-check "<prompt>"`（CLI v0.154.0-alpha.6.2）走**标准 user message** 投递：实测会话 `01a0adcd-2b86-…` 的 rollout 中 `function_call_output` 计数 **0**、无 `automation_update`、无 `at_` 前缀 id、assistant 正常回复、退出码 0、19422 tokens。→ 本机唯一可行的无人值守投递方式，应由 Windows 任务计划程序调用（外部调度无 jitter，时间更准）。
- [2026-09-17][FACT][src:实测 2026-09-17] 百炼 `/models` 返回 `{"first_id","data":[…]}`，缺 Codex 解码器要求的 `models` 字段 → `list_models` 永久失败、`models_cache.json` 解析出 0 个 id。推理不受影响（`model` 硬写在 config.toml），仅模型选择器无法在线刷新。另 CLI 警告 `Model metadata for qwen3.8-max not found`；fallback 元数据下 cron 探针会话 `task_started` 里 `model_context_window=828400`，而 config.toml 配的是 `1000000` → **fallback 可能覆盖显式配置**，需核实，否则 auto-compact 会提前触发。
- [2026-09-17][FACT][src:阿里云官方文档 help.aliyun.com/zh/model-studio/text-generation-model] `qwen3.8-max` 官方上下文窗口 = **1M**（推荐模型表：`qwen3.8-max | 1M | 思考模式支持 | Function Calling 支持 | 内置工具支持 | 结构化输出支持`；快照版 `qwen3.8-max-0902` 同为 1M）。同档 1M 的还有 `qwen3.8-flash`/`qwen3.7-plus`/`deepseek-v4-pro`/`glm-5.2`；`kimi-k2.7-code` 256k、`MiniMax-M3` 192k。
- [2026-09-17][FACT][src:本机实测] Codex 的 `task_started.model_context_window` 记的是 **`max_context_window × effective_context_window_percent%`**（即当轮**可用**窗口），**不是** config.toml 里的 `model_context_window`；后者在桌面 app 路径下**不会覆盖** catalog / fallback 元数据。实测两组值可精确反推：catalog 内 `qwen3.8-max` = `max_context_window 983616 × 95% = 934435`；fallback 元数据 = `272000 × 95% = 258400`。
- [2026-09-17][FACT][src:本机实测] `~/.codex/cc-switch-model-catalog.json` 提供 `qwen3.8-max` 的元数据，靠 config.toml 的 `model_catalog_json = "cc-switch-model-catalog.json"` 挂载。该键**一旦丢失**，模型落入 fallback → 可用窗口从 **934435 骤降到 258400（损失 72%）** → 长会话立刻撞 ContextLimit 并触发 auto-compact。2026-09-17 10:48:57 config 被重写时该键被丢弃，10:49:52 起本 thread 即从 934435 变 258400（rollout 内 13 条 `task_started` 可逐条对上），11:52 已加回。
- [2026-09-17][FACT][src:本机网络实测] `github.com` 的 **release 资源下载与 `git clone` 均被重置**（`objects.githubusercontent.com` 亦不通），但 **`api.github.com` 直连可用**（可取 release 的 `digest` 字段做校验）。可用镜像：**`ghproxy.net`（同时支持 HTTP 下载与 git clone）**、`gh-proxy.com`（仅 HTTP）；`gitclone.com` 返回 502、`github.moeyy.xyz` 不可达。`index.crates.io` / `static.crates.io` 直连可用（后者根路径 403 属正常）。
- [2026-09-17][FACT][src:本机实测] `cargo-deny 0.20.2` 的 `[advisories] db-path` **只接受字符串**；写成数组会让整个 deny.toml 解析失败（`expected a string`）→ deny 门禁形同虚设。`cargo deny check` 需 clone `RustSec/advisory-db`，本机可用**进程级** git 环境变量重定向到镜像，无需改全局 git 配置、也无需把镜像写进仓库：`GIT_CONFIG_COUNT=1`、`GIT_CONFIG_KEY_0=url.https://ghproxy.net/https://github.com/.insteadOf`、`GIT_CONFIG_VALUE_0=https://github.com/`。
- [2026-09-17][FACT][src:本机实测] 本地工具链已补齐：`cargo-deny 0.20.2`、`cargo-llvm-cov 0.9.1`（均取 GitHub 官方 release 预编译包，经 api.github.com 的 `digest` 逐个 sha256 校验通过）+ `llvm-tools` 组件已装。**TASK-001 此前缺的两条验收命令现已全绿**：`cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`（退出码 0）；`cargo llvm-cov --workspace --fail-under-lines 75` → 99 测试通过、**行覆盖 96.31%** / 函数 94.26% / 区域 95.18%（退出码 0）。
- [2026-09-17][FACT][src:人类裁决 2026-09-17] **DRIFT-001-1 已裁决：接受**「`hygiene` 实现 gov §5.4 的 **3/11 项** + 其余 8 项规则与 `verify-schemas`/`codegen`/`replay` 三个子命令在 `deferred.rs` 显式登记并运行时失败（退出码 3）」这一产出边界；§4 中 2026-09-16 那条 REJECTED 的「待人类确认」到此解除。卡面已同步修订：`plans/stage-0-spikes.md` TASK-001 的 In scope #1 加裁决注记，验收命令第 5 条拆为两条独立命令并注明**判定以 `-- deferred-rules` 摘要行为准、`verdict=PASSED` 不等于 11 项全过**。**PL-001 / PL-011 未一并裁决**；若采纳 PL-011（新增 CRLF 规则）则 `3/11` 需改述为 `3/12`。
- [2026-09-17][FACT][src:本机实测] **本机 `http://127.0.0.1:30000` 有可用 HTTP 代理，github.com 经它完全正常** —— 实测：`git clone --depth=1 https://github.com/RustSec/advisory-db` 成功取得 1265 个文件；release 资源可下载，且经代理取到的 `.sha256` 内容与直连 `api.github.com` 的 `digest` **逐字一致**；`cargo deny check` 仅靠该代理即通过（无需 `ghproxy.net` 镜像）。→ **优先用本地代理而非第三方镜像**（镜像有被替换风险，代理是直连语义）。仅代理 github 的持久写法：`git config --global http.https://github.com/.proxy http://127.0.0.1:30000`；进程级临时写法（不落盘）：`GIT_CONFIG_COUNT=1` + `GIT_CONFIG_KEY_0=http.https://github.com/.proxy` + `GIT_CONFIG_VALUE_0=http://127.0.0.1:30000`。注意本机原先**没有任何 global gitconfig**（PL-008），首次 `--global` 写入会新建该文件。
- [2026-09-17][FACT][src:本机实测] TASK-001 的验收已从 **5/6 变为 6/6**：`cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`（退出码 0）；`cargo llvm-cov --workspace --fail-under-lines 75` → 99 passed、**行覆盖 96.31%** / 函数 94.26% / 区域 95.18%（退出码 0，阈值 75）。逐文件行覆盖：`cli.rs` 100%、`deferred.rs` 100%、`hygiene.rs` 99.17%、`report.rs` 98.70%、`rustscan.rs` 95.27%、`repowalk.rs` 93.64%、`main.rs` 90.39%。→ `tasks/TASK-001-repo-skeleton.md` §7.4 标注的「安装工具后应回填实测数字」可用此数据回填。
### 工程护栏与仓库治理

- [2026-09-17][FACT][src:人类裁决 2026-09-17] **DRIFT-001-2 已裁决：接受**，a/b/c 照建议执行 —— **a** CI 硬门禁确认为 **6 项**（fmt、clippy `-D warnings` 含 `[workspace.lints]` 禁用项、test、deny、build、`xtask hygiene`），计数口径澄清为「gov §5.1 共 **16** 行，#3 由 #2 覆盖 → **6 硬 + 9 软 = 15 个 CI 步骤 ↔ 16 行清单**」（卡面原文 5+9=14 是重复计了 #3、又漏掉了已就绪的 hygiene）；**b** `plans/stage-0-spikes.md` TASK-001 In scope #4 已改为 6 项并补口径注记；**c** 采纳**元门禁** → **ADR-0019（Accepted）**「硬门禁必须配负向验证」，定义 N1 单元负向用例 / N2 CI 显式失败步骤 / N3 canary 工作流三种形式，并规定**软门禁转硬的那张卡必须同时提交负向验证并在登记表补一行**（自 TASK-015 起适用）。**未一并处理**：PL-001（gov §5.1 行数 vs stage-1「14 项门禁」表述）、PL-011（第 12 项 CRLF 规则）、fmt/clippy/build 三项 canary 缺失 → 新登记 **PL-018**。
- [2026-09-17][FACT][src:本机实测 cargo-deny 0.20.2] **ADR-0019 的 deny canary 双向均已验证**：正向 `cargo deny check licenses bans sources` → `bans ok, licenses ok, sources ok`，**exit 0**，且**不需要联网**（只有 `advisories` 要 clone advisory-db）；负向 `cargo deny --config <把 db-path 写成数组的坏配置> check licenses bans sources` → `error[wanted]: expected a string` + `failed to deserialize config`，**exit 1**。→ 这两条构成 `gate-selftest.yml` 的正/负断言。负向断言**必须同时校验 stderr 含 `expected a string`**：只断言"非零退出"的话，任何无关错误（网络、路径、权限）都会让 canary **假绿**。
- [2026-09-17][FACT][src:api.github.com 实测] `EmbarkStudios/cargo-deny-action` 的 **release `v2.1.1` 标题即 "Release 2.1.1 - cargo-deny 0.20.2"**，且该 action **没有 `version` 输入**（inputs 仅 command / arguments / command-arguments / manifest-path / log-level / rust-version / credentials / ssh-key / ssh-known-hosts / use-git-cli）→ **钉 action 的 release tag 就等于钉 cargo-deny 版本**，不需要在 CI 里 `cargo install`（省 5~8 分钟）。`ci.yml` 已由 `@v1` 改为 `@v2.1.1`，与本机同版本 → **PL-016 根因关闭**。
- [2026-09-17][FACT][src:本机 git 实测] **远端已建立、提交身份已重写（PL-008 关闭）**：`origin = https://github.com/snofin18/ai-assistant.git`（公开库，2026-09-17T07:36Z 建，建库时为空）；`git filter-branch --env-filter` 把全部 **7** 个提交的 author+committer 从占位身份 `Codex (ai-assistant agent) <codex@localhost.invalid>` 重写为 **`snofin18 (via Codex) <snofin@gmail.com>`**（人类裁决方案 B：保留 AI 溯源 + 邮箱合法），**原始 author date 全部保留**，工作树逐字节未变（`git diff backup/pre-author-rewrite-20260917 main` 为空）。哈希映射：`58fed3d→9d0fbee`、`c30c502→a03d2f9`、`bcc890e→3a90c7c`、`5cbab04→2cab794`、`d56ba6b→da3b9b1`、`2b31301→ab1068d`、`2788edd→dbbb3a0`。旧提交保留在**本地**分支 `backup/pre-author-rewrite-20260917`（不推送，确认远端无误后可删）。仓库本地 `user.name` 已设为 `snofin18 (via Codex)`（global 仍为 `snofin18`）以保持后续提交一致。
- [2026-09-17][FACT][src:GitHub Actions run 35198879508] **CI 首次运行即三平台全绿**（08:17:23→08:20:42Z，约 3m19s）：`cargo deny`（用钉好的 `cargo-deny-action@v2.1.1`）、`check (ubuntu/windows/macos-latest)`、`xtask deferred inventory` 五个 job 全部 `success`。→ **TASK-001 的 DoD「CI 在空 workspace 上三平台全绿」到此才真正被机器验证**（此前仓库从未推送、CI 一次都没跑过，正是 ADR-0019 背景②所指）；`deny.toml` 的 `db-path` 修复与 action 钉版本也得到端到端确认。附带观察：软门禁 #9（覆盖率）在 CI 里用 `cargo install cargo-llvm-cov --locked` 从源码编译，是三平台 job 的主要耗时来源 → TASK-015 转硬时应改为下载预编译二进制。
- [2026-09-17][FACT][src:本机只读侦察，TASK-002 步骤 1] **靶机环境基线（Spike A 的实测前提）**：OS = **Windows 11 25H2，build 26200.9457**（真值取自 `DisplayVersion`+`CurrentBuild`；`ProductName` 谎报 "Windows 10 Pro"，见 §5）；记事本 = **`Microsoft.WindowsNotepad` 11.2607.14.0 x64**，打包版，装于 `C:\Program Files\WindowsApps\Microsoft.WindowsNotepad_11.2607.14.0_x64__8wekyb3d8bbwe`（即含标签页的新版 Notepad，非 Win32 老版）；显示 = **3200×2000、AppliedDPI=192（200% 缩放）**、NVIDIA RTX 5060 Laptop GPU；输入法 = `zh-Hans-CN` 首位、TIP `0804:{81D4E9C9-1D3B-41BC-9E6C-4B40BF79E35E}{FA550B04-5AD7-411F-A5AC-CA038EC515D7}`（微软拼音），另有 `en-US`/`en-GB` → 卡面要求的「IME 开/关两态测中文写入」**本机可测**；`inspect.exe` **已就绪**（`C:\Program Files (x86)\Windows Kits\10\bin\10.0.26100.0\x64\inspect.exe`，SDK 10.0.26100.0，另有 x86/arm64）；**Accessibility Insights for Windows 未安装**（`%LOCALAPPDATA%\Programs\Accessibility Insights for Windows` 不存在）→ 卡面步骤 2 需人类裁决：补装，或以 `inspect.exe` + 自写 UIA 树导出替代。
- [2026-09-17][FACT][src:Spike A 先导实测 probe-01/02，详见 docs/spike-reports/SPIKE-A.md（状态 PARTIAL）] **新版记事本 11.2607.14.0 的 UIA 形状与性能已实测**：整棵控件树仅 **32 节点**；全树遍历中位数 **17.0 ms**（冷启动首次 74.2 ms，min 15.6 / max 30.0）、编辑区 `Descendants` 搜索 **1.5 ms**、`ValuePattern.GetValue` **0.11 ms**（59 字符）、`SetValue` 写 57 个中英混排字符 **4.79 ms** 且读回归一化后完全一致；记事本 WorkingSet ≈ 118.8 MB。→ 相对 go 判据（全树 ≤800 ms、局部 ≤200 ms）**余量 47× / 133×**。编辑区 = `ControlType.Document` + ClassName **`RichEditD2DPT`** + Name `文本编辑器`，**AutomationId 为空**，父级 `Pane` ClassName **`NotepadTextBox`** 可作锚点；`ValuePattern` 可读写（IsReadOnly=False）且 **`TextPattern` 也支持、返回文本逐字符相同** → 优先 TextPattern（能做范围/选区），ValuePattern 作降级。**go/no-go 尚未给出**：跨进程「另存为」对话框、1 MB 大文件、失败注入、以及 **Rust/COM 路径**都还没测（PowerShell 用托管封装，Rust 走 COM，批量取属性行为不同，耗时可能差一个数量级）。
- [2026-09-17][FACT][src:Spike A 先导实测] **新版记事本是「单窗口 + 多标签 + 多进程」结构，PID 不可用于定位**：`Start-Process notepad <file>` 返回的 PID 与真正持有窗口的 PID **不是同一个**（实测 30464 vs 20304，**事先杀掉所有 Notepad 仍然如此**）；打开第二个文件后系统里有 **3 个 Notepad 进程、但只有 1 个窗口、TabItem 变成 2 个**。→ Adapter 必须**按 ClassName `Notepad` + Name 后缀 `- Notepad` 定位窗口、按 `Tab aid='Tabs'` → `List aid='TabListView'` → `TabItem` 寻址内容**，绝不能按"我启动的那个 PID"找窗口；"打开文件"的真实语义是**在当前窗口新增标签页**。TabItem 的 Name 形如 `<文件名>. 未修改。`，**自带脏标记**，但属本地化可见文本，按 AGENTS.md 只能作辅助信号、不能作主 selector。
- [2026-09-17][FACT][src:Spike A 先导实测] **记事本的 AutomationId 是英文、Name 是本地化文本，且 AutomationId 不唯一**：`aid='File'`↔`name='文件'`、`aid='CloseButton'`↔`name='关闭标签页'`、`aid='AddButton'`/`'MenuBar'`/`'SettingsButton'`/`'FREButton'` 同理 → 实证支持 AGENTS.md「禁止用控件可见文本作主 selector」。状态栏有 **6 个** `Text aid='ContentTextBlock'`（行/列、字符数、空、缩放、` Windows (CRLF)`、` UTF-8`）→ 状态栏只能靠**索引 + Name 模式**区分。另有**瞬态** `TeachingTip` 节点（`PrivacyTeachingTip`、`PsDownloadDetailTeachingTip`、`PsDownloadLightTeachingTip`，后者提到"本地 AI 模型必须完成下载"）→ **UIA 树不稳定**，架构 v2 §17.4 的录制回放 fixture 必须在 UI settled 后采集、且比对要容忍瞬态节点。

---

---

## 2026-09-18 追加：`windows` crate 的 UIA 绑定 与 Rust/COM 路径实证

> 全部由本机实测产生（`spikes/spike-a-notepad/src/bin/uia_dep_proof.rs`，E1~E6 全 PASS，exit 0），
> 详情与逐条 feature 理由见 **ADR-0024 D1a**。

- [2026-09-18][FACT][src:`windows-0.62.2.crate` 原包 Cargo.toml 实测] **`windows` crate 里不存在 `Win32_UI_UIAutomation` 这个 feature**。0.62.2 共 26 个 `Win32_UI_*` feature，没有任何 UIAutomation 项；全包里唯一带该字样的是 **WinRT** 命名空间的 `UI_UIAutomation` / `UI_UIAutomation_Core`（`Windows.UI.UIAutomation`，主要是 provider 侧）。→ 任何文档/代码里出现 `Win32_UI_UIAutomation` 一律视为笔误。
- [2026-09-18][FACT][src:crates.io 稀疏索引 `index.crates.io/wi/nd/windows`] `windows` crate 共 81 个版本，**最新即 0.62.2** → 「换个版本就有那个 feature」不成立。
- [2026-09-18][FACT][src:`windows-0.62.2/src/Windows/Win32/UI/Accessibility/mod.rs` 实测] **UI Automation 的客户端 COM 绑定确实存在**，与 MSAA（`IAccessible`）、provider 侧接口（`IRawElementProviderSimple`、各 `I*Provider`）**同住 `Win32::UI::Accessibility` 模块**（约 2 万行、1381 个 trait/struct/fn）。含 `CUIAutomation`(CLSID, L904)、`IUIAutomation`(L6806)、`IUIAutomationElement`、`IUIAutomationElementArray`、`IUIAutomationTreeWalker`、`IUIAutomationCondition`、`IUIAutomationValuePattern`，以及 `UIA_*PropertyId` / `UIA_*ControlTypeId` / `UIA_*PatternId` / `TreeScope_*` / `PropertyConditionFlags_*` 全套常量。→ **正确 feature 名 = `Win32_UI_Accessibility`**。
- [2026-09-18][FACT][src:同上，实测编译报错] **`VARIANT` 被 `#[cfg(all(feature = "Win32_System_Com", feature = "Win32_System_Ole"))]` 双重 gate** → 只开 `Win32_System_Com` 会报 `no VARIANT in Win32::System::Variant`；而 `IUIAutomation::CreatePropertyCondition` 的签名需要 `VARIANT`。→ 用 UIA 就必须同时开 `Win32_System_Ole` + `Win32_System_Variant`。
- [2026-09-18][FACT][src:`uia_dep_proof` E1/E2，本机 exit 0] `CoCreateInstance(&CUIAutomation, None, CLSCTX_INPROC_SERVER)` → `IUIAutomation` 成功；`GetRootElement()` 返回 `name="桌面 1" class="#32769"`。→ `windows` crate 的 UIA 绑定**不是空壳**，COM 调用真的打到 UIAutomationCore。**ADR-0024 D1（用 `windows`、否决 `uiautomation` crate）的前提从"推测"升级为"实证"。**
- [2026-09-18][FACT][src:`uia_dep_proof` E3，Rust/COM 独立复现] **启动 PID ≠ 窗口属主 PID 在 Rust 路径同样成立**：`launched_pid=2632` / `owner_pid=13168` / `same_pid=false`；且窗口在第 **3** 次枚举扫描（间隔 150 ms）后才出现 ≈ **450 ms**。→ ADR-0022 D1 得到第二条独立证据；且「启动后立刻查窗口」必然失败，**必须轮询**。
- [2026-09-18][FACT][src:`uia_dep_proof` E5，n=10] **Rust + `windows` crate 的 COM 定位中位数 = 2.349 ms**（min 1.976 / max 3.049），PowerShell 托管封装同一操作 = 1.5 ms → **同一数量级，比值 ≈1.6×**。→ **解除 `SPIKE-A.md` §6 点名的最大不确定性**（"Rust 侧是否与 PowerShell 侧表现一致"）；架构 v2「Host 进程内完成定位与执行」的批量取属性策略**无需重估**。
- [2026-09-18][FACT][src:`uia_dep_proof` E4] 记事本文档区实测：命中候选链第 1 项 `Document + RichEditD2DPT`，**`AutomationId` 为空串**；磁盘 32 字节 / 28 字符 / CRLF → UIA 返回 **25 字符、3 个裸 CR、0 个 LF**（28−3=25，与"3 个 CRLF 各丢 1 字符"精确吻合），归一化后**逐字符相等**，**CJK 无损**。→ 在 Rust/COM 路径独立复现了 **ADR-0023** 与 **ADR-0022 D4/D5**。
- [2026-09-18][FACT][src:`Win32/UI/Accessibility/mod.rs` grep] **`IUIAutomationCacheRequest` 与全部 `*BuildCache` 方法均在**（`ElementFromHandleBuildCache` / `FindFirstBuildCache` / `GetChildrenBuildCache` / `GetRootElementBuildCache` …）→ ADR-0024「重新评估触发条件」中的"缺少 CacheRequest 支持"**未命中**，批量取属性策略可实现。
- [2026-09-18][FACT][src:本机 `cargo deny` 实测] `spikes/spike-a-notepad`（真实 `windows =0.62.2` 依赖）→ `licenses ok, sources ok`，**exit 0**；负向 GPL fixture → **exit 4**，输出含 `error[rejected]` / `license is not explicitly allowed` / `GPL-3.0-only`。→ ADR-0024 D2 的 spike-deny 门禁双向都已实测，且**从此有真实检查对象**（不再走"无 manifest 显式通过"分支）。

## 2026-09-18 追加：EOL 矩阵的 CJK 用例已补测（probe-03 改纯 ASCII 后重跑）

- [2026-09-18][FACT][src:`D:\csart\eol-probe\RESULT.txt`，重跑于 04:08:23Z] **probe-03 五个用例的完整矩阵（修正后）**：CRLF `15/15→uia 12`；LF `12/12→12`；CR `12/12→12`（唯一 `raw_eq=True`）；MIXED `13/13→12`；**CRLF-CJK `10 字符 / 22 字节 → uia 8 字符`**。五个用例的 `uia_eol` **全部是 `code13=N`、`code10=0`** → 结论 R1（UIA 一律返回裸 CR）**对 CJK 内容同样成立**；`norm_eq` 5/5 True。状态栏依次报 ` Windows (CRLF)` / ` Unix (LF)` / ` Macintosh (CR)` / ` Windows (CRLF)` / ` Windows (CRLF)` —— 与磁盘真值 5/5 一致。
- [2026-09-18][FACT][src:同一次重跑的前后对比] **除 CJK 行外，其余 4 行与 2026-09-18 首次运行逐字节相同** → 把脚本从含中文注释改为纯 ASCII 英文注释 **未改变探针逻辑**（这是「修编码不修语义」的验证方法，以后改探针可复用）。旧输出留存 `RESULT-pre-ascii-fix-20260918.txt`。

## 2026-09-18 追加：Codex `create_thread` 三次被拒的根因（人类指示 #9）

- [2026-09-18][FACT][src:本机 rollout `rollout-2026-09-16T10-43-52-01a0a819-….jsonl` L3293/L3300/L3307/L3296/L3303/L3310 直接取证] **`create_thread` 三次被拒的根因不是权限，而是参数结构错误。** 三次的 `function_call` 实参依次为：①②**只有 `prompt`，缺 `target`**；③`target` 被序列化成了 **JSON 字符串**而非对象 —— 原文：`{"prompt": "…", "target": "{\"type\": \"project\", \"projectId\": \"5b628eec-…\", \"environment\": {\"type\": \"worktree\"}}"}`。三次的返回都是 `create_thread received invalid arguments.`，**wall time 0.048~0.056 s** → 是**本地 schema 校验**当场拒绝，根本没走到服务端。
- [2026-09-18][FACT][src:子代理 Gibbs 检索 GitHub openai/codex issue] 上游存在两条**仍 open** 的相关条目：**#36315**「Codex Desktop `create_thread` rejects valid project worktree requests as invalid arguments」（labels: bug / app / app-server）、**#36250**「Codex Desktop: atomic project-aware `create_thread` with runtime roots and idempotency」（labels: enhancement / windows-os / app / app-server）。→ 即使把 `target` 写成对象，project+worktree 这条路径**也可能**仍有上游缺陷；**本机未验证过"写对结构就能成功"**（因为验证会在用户侧边栏里真的建出一个任务）。
- [2026-09-18][DECISION][src:人类指示 #9 + 上两条 FACT] **项目不依赖 `create_thread` 派生任务卡会话。** 正确结构是 `target` 必须是**对象**：`{"type":"project","projectId":"<list_projects 取>","environment":{"type":"worktree"|"local"}}`。但在上游 #36315 关闭前，**开工 TASK-002 及后续任务卡由人类手工新建会话**（或 `fork_thread`），会话第一句贴 AGENTS.md §3 的**约束回执**模板。→ 已写入 `docs/subagent-orchestration.md` 的前置条件与 `docs/PARKING_LOT.md`。

## 2026-09-18 追加：护栏工具 `xtask` 的第 3 / 第 4 个能力（ADR-0028 / ADR-0030）

- [2026-09-18][FACT][src:ADR-0030 + `cargo run -p xtask -- memory-counts` / `adr-index` 本机实测] **`xtask` 现有 4 个子命令族**：`hygiene`（gov §5.4 的 3/13 项）、`memory-counts`（**8** 条规则）、`adr-index`（**11** 条规则）、`guard`（`acquire` / `release` / `status` / `reap`）。退出码：0 通过 / 1 有 Error / 2 用法 / 3 未实现 / 4 IO / **5 锁超时放弃**。两个文档一致性子命令都是**只读**的，发现不一致时打印「可直接粘贴的正确值」而**不自动改写**（ADR-0030 选项 1 已否决自动写回：违反 xtask 只读不变量，且会与 `guard` 的锁语义打架）。→ 因此「`MEMORY.md` 的规模计数」与「ADR 编号登记表」从 2026-09-18 起**不再靠人记**。
- [2026-09-18][FACT][src:本机负向实证（注入 → 跑 → 复原），2026-09-18] **两道新门禁在「该红的时候」真的会红**（满足 ADR-0019 元门禁：单测 N1 + 真仓库实证）：① 把 `MEMORY.md` 规模表里 `facts.md` 的行数改成 `1` → `memory-counts` 退出码 **1**，输出 `memory/line-count-mismatch … 表中 1 ≠ 实测 101`；② 往 `docs/adr/README.md` §2 临时注入一行 `0019` → `adr-index` 退出码 **1**，**同时**命中 `adr/number-collision`（§1 ∩ §2 ≠ ∅ —— 就是 0019 双重占用事故的机器判据）与 `adr/retired-number-reallocated`（退役号被重新分配）。两次复原后均 **PASSED**（`memory-counts` scanned=7 / `adr-index` scanned=13，errors=0）。
- [2026-09-18][FACT][src:`.github/workflows/ci.yml` + gov §5.1 / §10] **CI 硬门禁由 7 项增为 8 项**：新增独立 job `doc-consistency`，步骤名 `[HARD #12b] xtask memory-counts + adr-index`。用**子编号** `#12b`（沿用 `#8b` 先例）而不是一级行号，因为 gov §5.1 的主编号被 `tasks/TASK-001-repo-skeleton.md`、`xtask/src/deferred.rs`、`ci.yml` 头部注释与 `plans/stage-1-pilots.md` 交叉引用。gov §10 口径同步为「**18 行清单 ↔ 17 个步骤（8 硬 + 9 软）**」。**不并入 #12（hygiene）** 的理由：hygiene 的规则总数已被 ADR-0025 钉死为 13（gov §5.4 表格行数就是 SSOT），塞进 19 条会立刻作废那个口径并牵连 `deferred.rs` 的数量自洽测试。该 job 刻意**不用 `set -e`**：两个子命令都跑完再判失败，一次给审阅者全部发现项。
- [2026-09-18][FACT][src:`xtask/src/guard.rs` 的 `slug_for` + ADR-0028 D2] **`guard` 的锁名（slug）按小写折叠**：先把仓库相对路径转小写，再把 `/` 换成 `__`、非字母数字换成 `-`、截断到 **64** 个可见字符，最后拼 **8 位 FNV-1a** 十六进制摘要（防重名与超长）；锁文件在 `target/locks/<slug>.lock`（不入库，`/target/` 已覆盖）。理由：Windows / macOS 默认文件系统**大小写不敏感**，`MEMORY.md` 与 `memory.md` 是同一个文件，分别派生两把锁会让互斥形同虚设；代价是 Linux（大小写敏感）上两个**真的**只差大小写的文件会共用一把锁 —— **刻意接受的过锁**（宁可少并行，不可 lost update）。锁记录里的 `target` 字段**不做大小写归一**（要能原样回显给人看），折叠只发生在派生锁名时。
- [2026-09-18][FACT][src:`xtask/src/adr_registry.rs` + 本轮新增 3 条单测] **`xtask` 的文档解析有两个「顺序 vs 集合」语义，不能混用**：`four_digit_numbers_in_text_order`（按出现顺序、不去重）与 `all_adr_numbers`（升序去重，集合语义）。`first_adr_number` 必须建立在**前者**上 —— 建立在后者上会让「第一个」退化成「**最小的**」。`all_adr_numbers` 供退役清单与待建号集合使用（它们要的确实是集合）。


## 2026-09-20 追加（更正：line 105 「xtask 现有 4 个子命令族」过期）

- [2026-09-20][FACT][src:TASK-070 audit cleanup] **`xtask` 现已扩到 7 个子命令**（不是 line 105 写的「4 个」）：`hygiene` + `memory-counts` + `adr-index` + `guard`（= line 105 的「4 个」）+ 2026-09-19 落地的 `refscan` + `docscan` + `card-check` = 共 **7** 个（+ `exemptions` helper module 未暴露）。line 105 是 2026-09-18 当天的中间状态快照（ADR-0030 D1 落地后的次日）。**本卡 supersede**：line 105 仍保留 = 历史记录，但下方此条作为「当前 SSOT」指针 —— 实际个数由 `cargo run -p xtask -- --list` 输出为准（PL-022 根因）。xtask card-check 未来应把「子命令数 = 7 + 1 helper」作为 hard-coded assertion 写入（避免手抄） = 归 TASK-015（card-check 改造）。
- [2026-09-21][FACT][src:TASK-075 B1.4, 本机实测 2026-09-21] **Win11 25H2 modern Notepad（11.2607.14.0）的"另存为"是 in-window WinUI3 FileExplorer-like panel** —— 不是跨进程 Explorer.exe dialog。**触发方式**：menu InvokePattern "File > 另存为"（中文：文件 > 另存为；通过 `[char]0x6587+0x4EF6` 和 `[char]0x53E6+0x5B58+0x4E3A` 构造）；`Ctrl+Shift+S` 键盘快捷键不可靠。**panel 位置**：完全在 Notepad 进程内，作为 in-window UI 元素。**识别 AID**：`SaveDialogLabel` 出现 2 次、+89 新 descendants 在 menu invoke 之后。**对 v2 §3.2 的影响**：架构原假设"element 不跨进程"对 WinUI3 应用不再成立——被控应用 = Win32 vs WinUI3/UWP 两类的 Adapter 边界差异**必须走 ADR 重新审视**
- [2026-09-21][FACT][src:TASK-075 B1.4 + user feedback 2026-09-21, 本机实测 2026-09-21] **[supersedes:2026-09-21] 之前错把 Save As 认成 in-window WinUI3 FileExplorer-like panel** —— Win11 25H2 modern Notepad 的"另存为"**实际上是 Win32 #32770 dialog 跨进程**（pid=4828/16992，独立于 Notepad 进程）。之前误判的根源：probe-07 用 `RootElement.FindAll(TreeScope.Children)` 枚举顶层窗口，**UIA1 API 自身不返回 Win32 标准 dialog 类**（虽然 Win32 EnumWindows 看得到 hwnd=1641050, pid=4828, class=#32770, title=另存为）。**+89 descendants 之前误读为"in-window"**：实际上那是 dialog 内的 file list + toolbar 等子元素，全部归属跨进程的 explorer 子进程（不是 Notepad）。**正确触发链**：`File > 另存为` menu Invoke（`[char]0x53E6+0x5B58+0x4E3A` 构造）→ 弹出 Win32 #32770 dialog（独立进程 pid=4828/16992）→ 跨进程。可通过 UIA `AutomationElement.FromHandle(hwnd)` 从 Win32 HWND 桥接回 UIA element → 找 descendants。**FileName input 路径**：`FileNameControlHost` pane（aid=`FileNameControlHost`）→ 内部 `aid=1001` Pane（class=Edit）→ **Win32 SendMessage WM_SETTEXT 直接写入**（UIA ValuePattern 在 Pane wrapper 上 Unsupported Pattern）。**Save 按钮路径**：dialog 直接子元素（不是 descendants），`name=保存(S) aid=1 class=Button`（Win32 标准 IDOK=1）→ **Win32 SendMessage BM_CLICK 直接触发**。**v2 §3.2 隐含假设实际成立**：Save As dialog 确实是跨进程；之前的"in-window"判断完全错误

- [2026-09-21][FACT][src:TASK-075 B1.4 empirical test (probe-07 v2 with set_filename actually attempted + verified via file existence), 本机实测 2026-09-21 10:51-10:52] **Win11 25H2 modern Notepad FileName Edit 的 set_filename 失败模式已 empirical 验证** —— 之前猜测"WM_SETTEXT 被 DirectUI 忽略"，**实测**：WM_SETTEXT **API 调用返回 success (1)**（API 层接受），**但 DirectUI 自绘层**完全忽略写入 = 文本从未被设置到内部 buffer。**经验性验证路径**（这比 UIA GetWindowText/UIA ValuePattern 都更可靠——后两者在 DirectUI 自绘控件上也被屏蔽）：设目标文件路径 = probe07-tgt-NEW.txt（与源文件名不同），WM_SETTEXT 改名为 NEW → PostMessage BM_CLICK → 检查 NEW 文件是否被创建。结果 = NEW 文件**永远不存在**（10/10）+ 源文件被覆盖 = 100% set_filename 失败 + 100% 文件保存到原路径。**关键洞察**：DirectUI 自绘 Edit 控件对 **所有**写入路径都不响应（Win32 WM_SETTEXT、UIA ValuePattern、UIA 从句柄桥接的 ValuePattern），但**读路径返回过期缓存**（这就是 4 项 100% 中 edit_access 假阳性的来源——FindChild 递归能找到 hwnd 不代表能操作）。**经验性测试模式是 ground truth**：未来所有 Win32 dialog 的 set_text 测试都应该用"目标文件是否存在"作为成功判据，不要依赖 GetWindowText/UIA ValuePattern 的返回值。**对 stage-1 Adapter 设计的实际影响**：Notepad Save-As 路径 = "保存到原文件名" 可用（set_filename=0 不影响基本 save 流程）；要 rename 必须用 SendInput API 模拟键盘输入（v2 §3.2 假设仍成立；不要 ADR 修订）

- [2026-09-21][FACT][src:TASK-077 B1.3 probe-06 = uia_dep_proof E7, 本机实测 2026-09-21] **Rust production path SetValue = WORKING (2 ms)** = E7 在 uia_dep_proof.rs 中追加并 PASS：SetValue ok=true (2 ms) roundtrip chars=32 contains_cjk=true。**对比 PowerShell UIA1（probe-02）** SetValue 中英混排 57 字符 = 4.79 ms；Rust production path **快约 2.4×**。**关键结论**：Rust SetValue 不存在 DirectUI Edit 限制（因为 Rust 走 `windows` crate 直接 COM 调用，绕过了 UIA1 的 ValuePattern 抽象层；UIA ValuePattern 在 DirectUI 自绘 Edit 上 "Unsupported Pattern" 但底层 COM IUIAutomationValuePattern SetValue **工作**）。**对 stage-1 Notepad Adapter 设计的意义**：set_filename 路径 = **Rust via windows crate 直接 COM**（不要走 UIA ValuePattern）。这是经验性确认：B1.4 probe-07 (PowerShell UIA1) 测 set_filename 失败 ≠ Adapter 实现必然失败，只是 UIA1 抽象层在 DirectUI 上限制。**stage-1 Adapter 可直接用 Rust SetValue 跳过 UIA1 限制**
- [2026-09-21][FACT][src:TASK-075 B1.4 v3 set_filename fix via SendKeys, 本机实测 2026-09-21 11:19-11:21] **[supersedes:2026-09-21] 之前错判 set_filename 永远 0%** -- 之前用 Win32 WM_SETTEXT (API 成功但 DirectUI 屏蔽) + UIA ValuePattern (Unsupported Pattern) 失败. **实测 v3 fix**: 用 Win32 SendKeys (Ctrl+A + char-by-char 键入) = **100% 成功**. 原因: DirectUI Edit 忽略 WM_SETTEXT (合成 API), 但响应真实键盘事件. SendKeys 底层走 SendInput API = 走键盘事件管道 = DirectUI 必须接受. **RESULT-07.txt v3 (2026-09-21T03:20:43Z)**: dialog_found 10/10 + edit_access 10/10 + **set_filename 10/10** + save_clicked 10/10 + file_on_disk 10/10 = 综合 5/5 = 100% = GO (go 判据 #5 实际达标). **对 stage-1 Adapter 设计的实际影响**: set_filename 不需要 SendInput API (更复杂), 用 SendKeys 即可 (PowerShell + .NET WinForms 都可用) = stage-1 Notepad Adapter Save-As rename 路径 = PowerShell + Win32 SendKeys

- [2026-09-23][FACT][src:TASK-103 stage-1 启动 重新实施] **`protocol` crate 是 stage-1 1a 批次 A1 的第一张** = 5 份 JSON schema 单一事实源 + Rust 类型由 `xtask codegen` 生成。**`v2 §5.3` envelope 必填字段（task_id / step_id / tool / source / truncated-detail / evidence / metrics）已全部补齐**。**`v2 §8.7` ErrorCode 13 类已对齐**（ModelInvalidOutput / ModelNetworkFailure / ToolInvalidArgs / PolicyDenied / TargetNotFound / TargetAmbiguous / TargetUnresponsive / CapabilityMissing / VerifyFailed / PlatformPermission / Transient / UserInteraction / Fatal）。**`ErrorDefinition` 由 codegen 从 schema 的 categories 数组生成** = schema 是事实源。**`verify-schemas` 验证**: 5 份 schema 存在 + JSON 合法 + version 匹配 + error-codes 恰好 13 个 category。**`codegen --check` 检测 drift** = hand-edit generated/*.rs → CI 红灯。**新依赖 serde + serde_json 已登记** = 见 DEPENDENCIES.md。

- [2026-09-23][FACT][src:TASK-011 修订硬化 335e1ad 实测] **`xtask codegen --check` 现在是真门禁**：`--check` 已进 `xtask/src/cli.rs::BOOLEAN_FLAGS`；`codegen::run` 有 drift 返回 `Ok(1)`；`main.rs::run_codegen` **原样透传**退出码。**负向验证实测**：手改 `crates/protocol/src/generated/tool_schema.rs`（`pub struct ToolSchema` → `ToolSchemaXX`）→ `codegen --check` **exit 1** + `[DRIFT] ... first difference at line 23` + generated/on-disk 两行差异；还原 → exit 0。**生成物天生 rustfmt 稳定**：`codegen`（写盘）→ `cargo fmt --all --check`（0 diff）→ `codegen --check`（0 drift）三步闭环，无需 fmt 后处理。**`verify-schemas` 三条不变量**：① 5 份 schema 存在 + JSON 合法 + 顶层 `version` 匹配；② `error-codes` 的 `categories` 恰好 13 项；③ `categories` 数据数组 ↔ `properties.categories.items.properties.category.enum` **同序逐项一致**；另有 capability ≥1 项 + id 不重复（`uniqueItems` 我们不实现，故显式查）。**协议侧当前事实**：envelope `required = [version, tool, task_id, step_id, ok, data]`（`error` **不在** required，Rust 侧 `Option` 无 `skip_serializing_if` → 恒序列化为 `null`）；`audit-event` `required` **缺 `self_hash`**；`protocol/{tool-schema,audit-event}/*.json` 的 `$ref: "envelope-1.0.json"` 是**断链**（同级目录无该文件，真身在 `protocol/envelope/`）。**测试现状**：`crates/protocol` 7 个单测；`xtask/src/{codegen,render,verify_schemas,serde_json_lite}.rs` 合计约 1100 行 **0 单测**。**唯一残留 allow**：`xtask/src/serde_json_lite.rs:8` 的 21 条 file-level `#![allow(...)]`。

- [2026-09-23][FACT][src:TASK-011 修复轮] **上一轮复核发现的 4 项已修**：(1) `xtask/src/serde_json_lite.rs` 的非 ASCII **静默 mojibake** 已修（按 UTF-8 字符宽度整体解码；实测 `中文§` 现在原样进生成物；+9 条解析器单测，含「`\uXXXX` 仍显式报错」这条**故意锁住**的已知限制用例）；(2) `xtask/src/render.rs` 对 schema 文本做 **Rust 字面量转义**（铁律 2：schema = 被读的文档 = 不可信输入；实测把 `\"quotes\"` + `\\` 写进 schema 的 `hint` 后，生成物仍是**合法** Rust 且 `cargo check -p assistant-protocol` 通过），另加 `//!` 注释折行清洗 + 6 条渲染单测；(3) `protocol/{tool-schema,audit-event}/*.json` 的 `$ref` 从断链的 `envelope-1.0.json` 改成 `../envelope/envelope-1.0.json`（**实测目标文件存在**）；(4) `audit-event-1.0.json` 的 `required` 补 `self_hash`（与 Rust 侧非 Option 字段对齐）；另新建 `crates/protocol/README.md`（TASK-011 DoD 明写项 + AGENTS.md §3 要求启动时读的 crate 不变量）。**测试基线**：`xtask` 304 → **319** passed；`assistant-protocol` 7 passed。
- [2026-09-23][FACT][src:probe-11/12/13 单测, Win11 25H2 modern Notepad, PS 5.1] **Win32-Input.psm1 模块导出 4 函数 API 契约 100% 满足**（Send-SendInputVk / Send-SendInputUnicode / Set-Win32ForegroundFocus / Get-VkFromChar）：9 + 5 + 2 = 16 个次项 0 异常 + 返回预期类型。non-interactive PS 下 SendInput 调用本身**结构正确**（构造 INPUT 数组 + 调 SendInput + LastError=87 ERROR_INVALID_PARAMETER）→ 框架可被 stage-1 Notepad Adapter 直接 Import-Module 调用，无需 wrapper 改造。
- [2026-09-23][FACT][src:probe-11 Phase B/C, Win11 25H2 modern Notepad, PS 5.1 non-interactive] **后台 PS 下 SendInput 字符投递 = 不可行**：即便 SetForegroundWindow + SetFocus + UIA doc.SetFocus 全套做完，GetFocus() 仍返回 0x0，SendInput 返回 0，GetLastWin32Error()=87（ERROR_INVALID_PARAMETER）。**UIA ValuePattern.SetValue 不受 session 限制**：5/5 成功（控制基线）。→ **stage-1 Adapter 写路径必须优先 UIA SetValue**（不依赖 console foreground），只在需要仿真键盘（如 IME / 修饰键组合）时切到 SendInput 且必须确认 foreground 锁定。
- [2026-09-23][FACT][src:probe-11 Phase C + probe-12 Phase C, Win11 25H2 modern Notepad, PS 5.1] **UIA ValuePattern.SetValue 写 CJK roundtrip 100% 成功**（5/5 实测）：读回字符串与写入字符串**逐字符相等**（包含 [char]0x4E2D + [char]0x6587 + [char]0x4E2D = 中文中）。佐证 probe-02 / probe-04 的 UIA 写路径稳定，可作为 stage-1 Adapter 主写通道。

- [2026-09-23][FACT][src:TASK-101 集成回归用户实测 2026-09-23 11:00, Win11 25H2 modern Notepad, interactive PS console] **SendInput 在 interactive PS console 下**对 Notepad 主窗口 + 标准 UIA 控件**真能工作**：probe-04 Send-SendInputVk -Vk 0x53 -Modifier 0xA2 (Ctrl+S) **触发真保存 5/5 100% PASS**（save=SAVED）。这是 SendInput 路径在 interactive session 工作的首例实测证据，**打破了 2026-09-23 之前认为"interactive session 就一定 100%"的隐含假设**。**关键 caveat**：仅在 probe-04 的"Notepad 主窗口 + 触发菜单/键盘加速键"场景下工作；probe-07 的 SaveAs FileName Edit / probe-08 的 unsaved_dialog / probe-10 的 CJK 输入在同样 interactive session 下仍 0% = SendInput 是**上下文敏感型** API，**不能简单地把"interactive session = 100%"作为通用假设**。→ **stage-1 Notepad Adapter 写路径优先级明确**：UIA ValuePattern.SetValue > SendInput (主窗口 Ctrl+S 类) > SendKeys (deprecated, 0% 同等效果)
- [2026-09-23][FACT][src:TASK-101 集成回归用户实测 2026-09-23 11:00, Win11 25H2 modern Notepad] **TASK-101 SendKeys → Win32-Input 替换无 regression**：probe-04 / 07 / 08 / 10 实测结果 = pre-TASK-101 baseline（详 probe-04 之前 RESULT-04.txt + probe-07 之前 RESULT-07.txt + probe-08 之前 RESULT-08.txt + probe-10 之前 RESULT-10.txt，均在 git history f3a96a0 前）：(a) probe-04 Ctrl+S 5/5 = 5/5 (相同);(b) probe-07 set_filename 0/10 = 0/10 (相同 = DirectUI 平台限制，SendInput 不工作);(c) probe-08 unsaved_dialog setup 0/10 = 0/10 (相同);(d) probe-10 SK 0/10 = 0/10 (相同 = SendKeys 在原版本同样不工作)。→ **SendKeys 路径在 spike 内可正式弃用**，TASK-101 替换等价。
- [2026-09-24][FACT][src:merge 5257dc6 实测] **本分支（TASK-011 硬化版）与 main 是平行实现，merge-base = `d56b2eb`（真分叉 4/6）**。合并取舍（用户裁决「以本版为准」）：实现类文件取本分支；`LEDGER.md` / `docs/memory/{facts,pitfalls}.md` 双方条目**全保留**（只追加，不改写他人条目）；`MEMORY.md` / `Cargo.lock` 取本分支；`.github/workflows/ci.yml` 取 main 的步骤名。**保留 main 的 TASK-100/101 产物**：`spikes/spike-a-notepad/Win32-Input.psm1` + `probe-11/12/13/14*.ps1` + `tasks/TASK-101-*.md` + probe-04/07/08/10 的集成改动 + `docs/memory/apps/notepad.md` + spikes README。**删除 main 的 2 个孤儿数据文件** `protocol/capability-matrix/capability-values.json` 与 `protocol/error-codes/error-codes-values.json`：`git grep` 在 origin/main 全树 **0 命中**（连它自己的 codegen/verify-schemas 都不读），且与 schema 内嵌的 `capabilities` / `categories` 数组**逐条重复** = 第二个事实源。**教训**：合并"取哪边"要先跑 `git diff --name-status origin/main HEAD` 看清 `D`（只在对方存在）的文件，否则会静默删掉对方整批交付物。
- [2026-09-24][FACT][src:TASK-011 收尾轮 实测] **CI 软门禁 #6（verify-schemas）/ #7（codegen --check）已转硬**，且按 ADR-0019 §规则 同步补齐负向验证：**N1 = 12 条单测**（`verify_schemas.rs` 8 条：13 类同序一致→零发现项 / 12 类 / 数据数组与 enum 不同序 / 缺 `categories` / capability 空 / id 重复 / 仓库根不存在→`Ok(1)`；`codegen.rs` 4 条：两侧一致→无差异 / 第 2 行手改 / 追加尾行→`<missing line>` / schema 读不到→`Err`）；**N2 = `ci.yml` 的 `gate-negative` job** 2 步注入式验证。**关键设计**：N2 断言的是 **`code == 1`（具体阻塞级退出码）而不是 `code != 0`** —— 因为"用法错误 exit 2"也是非零，那不叫拦住漂移（这正是 TASK-011 复核里发现的假绿通道之一）；且 #6 步在断言前先 `grep` 自检"坏样本真的注入进去了"，避免 `sed` 没匹配时把"门禁没红"误判成"门禁坏了"。**实测**：error-codes schema `version`→`9.9` → `verify-schemas` exit 1；生成物追加一行 → `codegen --check` exit 1；还原后均 exit 0。**同时删除 main 的孤儿文件后 `verify-schemas`/`codegen --check` 仍 5/5 OK**（证明它们不被引用）。**xtask 测试基线 319 → 331（+12）**。
- [2026-09-24][FACT][src:TASK-011 收尾轮 实测] **`xtask/src/serde_json_lite.rs` 的 22 行模块级 `#![allow(...)]`（21 条 lint）已彻底清除，改为改代码**：`Value::X` → `Self::X`（use_self ×10）、`Value::{as_array,as_bool,as_object}` + `Parser::new` + `skip_ws` 转 `const fn`（×5）、`self.input[i]` / `self.input[a..b]` 全部改 `slice::get`（消掉 4 处 `clippy::indexing_slicing` deny 级错误）、`(b'0'..=b'9').contains(&b)` → `b.is_ascii_digit()`、并把 `parse_null`/`parse_bool` 的两处未检查切片合并成带 `slice::get` 的 `parse_literal` 辅助函数。**唯一保留**：`Value::Number(f64)` 上 1 条 per-line `#[allow(dead_code)]` + 原因注释（数字载荷只有单测读）。**行为等价性证据**：12 条解析器单测全绿 + `codegen --check` **0 drift**（生成物字节未变）。**成本对照**：上一轮把这条记为"待人类裁决"，本轮走 ADR-0035 §替代路径「首选：改代码」，实际只有 ~20 处机械修改。
- [2026-09-24][FACT][src:gate-selftest run_number=1 实测 + 本机 Git Bash 复现] **GitHub Actions 在 Linux runner 上执行 `run:` 的默认 shell 是 `bash -e {0}`（隐式 `-e`；`pipefail` 要脚本自己 `set -o`）**，因此 `var="$(可能失败的命令)"` 这种「赋值 + 命令替换」在命令非零退出时会**直接终止脚本** —— `$?` 取不到、后续断言不执行；而**条件位置**的命令（`if cmd; then` / `while cmd; do` / `cmd || ...`）不受 `-e` 影响。实测：`bash --noprofile --norc -eo pipefail` 下 `out="$(cargo deny --config <坏配置> check licenses bans sources 2>&1)"; code=$?` → exit 1 且连下一行 `echo MARKER` 都不打印；改成 `if out="$(...)"; then code=0; else code=$?; fi` → exit 0 且正常打印 `exit=1`。**同一事实的另一半**：本地 shell 默认**没有** `-e`，所以同一脚本在本地裸跑是通过的 —— 「本地验证过」会把这类 bug 完全掩盖。
- [2026-09-24][FACT][src:TASK-012 实测 本机 Windows] **`crates/storage` 落地**：SQLite(WAL) 主库 + **只前进不回滚**的迁移框架（`migrations/000N_*.sql` 用 `include_str!` 编译期内嵌，按 sha256 记账在 `schema_migrations.checksum`；事后改文件 → 启动 `migration_checksum_mismatch` 拒绝）+ 6 张表（`tasks` / `task_steps` / `checkpoints` / `blobs` / `blob_refs` / `usage_records`）+ 内容寻址 blob 池（zstd level 3 + sha256 + 去重 + 「一行一引用」计数 + GC + 一致性扫描）。**依赖解析实测**：`rusqlite 0.37.0`（`bundled` → `libsqlite3-sys 0.35.0` + `cc 1.4.7`，首次编译 C 源码约 14 s）/ `zstd 0.13.3` / `sha2 0.10.9`。**验收全绿**：`fmt` / `clippy --all-targets -D warnings` / `test -p assistant-storage`（18 集成 + 2 records + 6 单测 + 1 doctest = **27**）/ `test --workspace`（331 xtask + protocol + storage）/ `build --release` / `hygiene`（0 error；2 warning 是 xtask 既有文件 >600 行）/ `docscan`（0/0）/ `memory-counts`（0/0）/ `adr-index`（0/0）/ `verify-schemas`（0 error）/ `codegen --check`（0 drift）/ `cargo deny check licenses bans sources`（全 ok）。**表名取舍**：卡面 In scope 写的是单数简称（`task`/`step`/`usage`），落地用架构 v2 §15.1 的权威复数名 → 已登记 DRIFT-012-1。**`deny.toml` 不在本卡 write scope**，所以卡住 bans 的那条通配依赖是靠给 path 依赖补 `version = "0.1.0"` 解决的，不是改 deny 配置。**`assistant-core` 尚不存在**（PL-037 要提前的骨架），故 `cargo test -p assistant-core arch::` 本卡无法执行（N/A，见卡 §7）。
- [2026-09-24][FACT][src:TASK-012 本机实测] **本机 `cargo fetch` 直连 crates.io 可用，不需要任何 proxy 环境变量**：在 `%TEMP%` 新建 probe crate 依赖 `tinyvec = "1.6"`，**清掉 `CARGO_HTTP_PROXY` / `HTTPS_PROXY` / `HTTP_PROXY` 后 `cargo fetch` exit 0 且真的 `Downloaded tinyvec v1.13.3`**；再加上 `CARGO_HTTP_PROXY=http://127.0.0.1:30000` 同样 exit 0。→ 上一会话交接里「crates.io 直连超时、必须设 `CARGO_HTTP_PROXY`」在本机当前状态下**不可复现**；两条路都能走，遇到超时再设 `CARGO_HTTP_PROXY` 即可（`~/.cargo/config.toml` 与仓库 `.cargo/config.toml` **都不存在**，所以不是配置文件在起作用）。
- [2026-09-24][FACT][src:TASK-201 落地 本机 Windows] **`crates/core` 骨架提前落地 = PL-037 闭环（人类 chat 2026-09-24 裁决选项 ③）**：新增 `crates/core/{Cargo.toml,README.md,src/lib.rs}`（`assistant-core`，**零第三方依赖**、`[dependencies]` 为空、`#![deny(unsafe_code)]`、**不含任何 `pub` 项** —— 骨架期不发明接口）。**为什么提前**：CI 软门禁 #5 是 `cargo test -p assistant-core arch::`，而 `crates/core` 原本要到 TASK-028 才存在 → TASK-015 的验收要点（arch test 能拦 `core → platform/windows`）在 028 之前无法满足 = 与「015 必须早做（护栏晚于代码 = 漂移已发生）」死锁。**实测**：`cargo test -p assistant-core` 与 `cargo test -p assistant-core arch::` 均 exit 0，输出 `running 0 tests`。**关键提醒**：本卡之后该门禁的语义是「0 个测试通过」而**不是**「分层规则已生效」—— 真正的 arch 断言归 TASK-015（它拥有 `crates/core/tests/arch*`），`.github/workflows/ci.yml` 的 `[SOFT #5]` **仍是软门禁**，转硬是 TASK-015 的动作。
- [2026-09-24][FACT][src:TASK-013 落地 本机 Windows] **`crates/audit` 落地**：`audit_logs`（列名/顺序 = 架构 v2 §15.1）+ `idx_audit_ts` + **数据库侧 append-only 触发器**（`BEFORE UPDATE` / `BEFORE DELETE` → `RAISE(ABORT)`）由**迁移 `0002_audit_logs.sql`** 建，`SCHEMA_VERSION` 由 **1 → 2**；`AuditLog` 提供 `append`（串链 → ring buffer → 单事务批量 flush）/ `flush` / `verify_chain` / `verify_chain_strict`，`Durability` 三档（`batched` 默认 **100 条 / 200 ms**、`immediate`、`separate_db_full` 未落地 → 显式报错）。**实测**：batched 摊销 **0.0347 ms/条**（2000 条 / 69.3 ms，阈值 < 1 ms）；25 个测试（8 unit + 11 integration + 6 tamper）+ 1 doctest 全绿。**跨 crate 加表的口径**：storage 的迁移链是 `crates/storage/src/schema.rs` 的编译期内嵌常量，**没有**外部注册入口 ⇒ 任何跨 crate 加表都必须改 storage 的源码（见 PL-046）。
- [2026-09-24][FACT][src:TASK-202 落地 本机 Windows] **存储迁移注册表落地（ADR-0038）= PL-046 闭环**：`crates/storage` 不再拥有「全部表的迁移清单」，改为**只提供机制** —— 新增 `crates/storage/src/migrations.rs`（`Migration` / `MigrationSet` / `MigrationSetError`；`register()` 拦重号、`validate()` 拦缺号、`expected_version()` = 最高版本），`Database::open(paths, clock, &MigrationSet)` 的**迁移集是必填参数**（不存在「默认只开 storage 那部分」的静默路径）；每个**拥有表**的 crate 用 `include_str!` 内嵌自己的迁移 SQL 并公开 `pub const MIGRATIONS`（0001 → `assistant_storage::MIGRATIONS`；0002 → `assistant_audit::MIGRATIONS`，文件已 `git mv` 到 `crates/audit/migrations/0002_audit_logs.sql`）。**关键实测**：① 移动前后 git blob 逐字节相同（`4d9ffbb25f47de30c2ed577edeeb478d0cfc9b98`）⇒ sha256 checksum 不变 ⇒ 已有库仍可打开；② `pub const SCHEMA_VERSION` **已删除**（注册表下它必然说谎），替代 = `MigrationSet::expected_version()`；③ 「漏注册 audit」现在**拒绝启动**（`schema_version_mismatch`），不再静默少建表；④ `crates/storage` 的集成测试只装配**自己**的集合 ⇒ 别的 crate 加表**不再**需要改 storage 的测试（TASK-013 那 3 处断言爆炸是 PL-046 的症状，现已从机制上消除）。

- [2026-09-24][FACT][src:TASK-203 落地 本机 Windows] **`audit_logs` 列语义去重 + 显式链序落地（ADR-0040）= PL-043 + PL-045 双闭环**：迁移 `crates/audit/migrations/0003_audit_logs_semantics.sql` **重建表**，列形状由 `(id, prev_hash, ts, actor, task_id, step_id, event_type, detail_json, hash)` 变为 `(sequence, id, prev_hash, ts, actor, task_id, step_id, event_type, detail_json)` —— 删 `hash`（与 `id` 完全同义）、加 `sequence INTEGER PRIMARY KEY AUTOINCREMENT`。运行期 SQL 侧：`INSERT` 只写 8 列（`sequence` 由 SQLite 分配）、`SELECT_CHAIN_TAIL_SQL` = `SELECT id FROM audit_logs ORDER BY sequence DESC LIMIT 1`、`SELECT_RECORDS_SQL` / `SELECT_RAW_ROWS_SQL` 均 `ORDER BY sequence`；`RawAuditRow` 删 `hash` 字段、`verify_rows` 只剩「重算 == `id`」一条权威判据。**`0002_audit_logs.sql` 一字未改**（git blob 不变 → checksum 记账不破）。**实测**：v2 库（storage 0001 + audit 0002，3 行数据）升到 v3 后行数不变、`sequence` = 1/2/3、链尾不变、新行接得上旧链尾、`verify_chain` 自洽；`VACUUM` 后链尾不变（`test_vacuum_does_not_change_chain_tail`）。

- [2026-09-24][FACT][src:TASK-014 落地 本机 Windows] **`crates/secrets` 落地（OS keychain 封装）**：新增 crate，依赖 `keyring` 4.2（默认 feature `v1` = apple keychain + windows-native + zbus-secret-service）+ `zeroize` 1.9（`alloc` 是默认 feature，故 `Zeroizing<String>` 开箱可用）。结构：`SecretName`（构造即校验：非空 / ≤128 字节 / `[A-Za-z0-9._-]`）→ `SecretStore` trait（**缺省 = `Ok(None)`，与 `Err` 严格区分**）→ 两个后端（`KeyringSecretStore` 真 / `InMemorySecretStore` fake）→ `AuditedSecretStore` 装饰器（每次访问记一条 `SecretAccessRecord`；**审计写不进去 ⇒ 拒绝该次访问**，fail-closed）。`keyring::Entry::store_status()` 返回 `&'static Result<()>`，是"后端是否就绪"的唯一查询点（store 未就绪时 `Entry::new` 报 `NoDefaultStore`）。**CI 三平台都没有可用的真实密钥库**（ubuntu / macos runner 无 Secret Service / 无登录 Keychain）→ 单测一律走内存 fake，真后端只做"能构造 + 能编译"。
- [2026-09-24][FACT][src:TASK-014 本机实测] **`keyring` 4.2 的依赖树不小，首次 `cargo fetch` 很慢但不是卡死**：Windows 上拉入 `regex` / `windows-sys` 等，Linux 上还会拉入 `zbus`（纯 Rust，**不需要 libdbus**，故 ubuntu CI 不必装系统包）。本机实测首次 `cargo fetch` 约 **40 分钟**（169 个 crate / 29.6 MB），`static.crates.io` 单文件下载偶发到 10 s 量级。**后续会话若见 `cargo fetch` 长时间无输出**：先看 `~/.cargo/registry/cache` 的文件数是否在涨，再决定是否重试 —— 别急着 kill。
- [2026-09-24][FACT][src:GATE-0 实测 本机 Windows + `codex-cli 0.155.0-alpha.16.3`] **automation 的投递形态已变，ADR-0018 的 E1 根因消失**：prompt 现在以**正常 user message** 投递（`role = "user"`、`msg_…` 前缀的合法 id，正文前缀 `Automation: <name>` / `Automation ID:` / `Automation memory:` / `Last run:`），**不再**注入 `call_id: None` 的合成 `function_call_output`。cron 探针 2 个 + heartbeat 探针 5 个 `function_call_output` **全部**带合法 `call_id`，`at_` 前缀 id **0 个**，无 400 → 「毒项沉入长驻会话」的失效模式**未复现**（heartbeat 跑完后同一 thread 仍能正常回答）。
- [2026-09-24][FACT][src:GATE-0 实测] **`automation_update` 的必填字段口径（实测）**：cron = 8 个（`kind` / `name` / `prompt` / `rrule` / `status` / `projectId` / `model` / `reasoningEffort` / `executionEnvironment`，`destination` **不需要**）；heartbeat = 只要 `kind = "heartbeat"` + **`targetThreadId`** + **`rrule`**（落盘字段名 `target_thread_id`）。取值：`status` ∈ `ACTIVE` / `PAUSED`；`executionEnvironment` **只接受 `"local"`**（经工具选 worktree 的写法未找到）。
- [2026-09-24][FACT][src:GATE-0 实测] **`projectId` 必须用 legacy project id**：本项目 `ai-assistant` = `5b628eec-9ca4-4768-8891-c7744a9dab5e`（可用）；app-server id `01a0a80a-d1e7-7551-9d27-6518a58b78ed` **会被拒**，且只回 `Failed to create automation.`（不指字段）。落盘形态为 `target = { type = "project", project_id = "<legacy id>" }`。
- [2026-09-24][FACT][src:GATE-0 实测] **automation 机制的三条可用能力**：① `~/.codex/automations/<id>/memory.md` 自动创建、agent 每轮可读写（跨轮记忆，**不得**拿它承载项目结论 —— 项目结论只进仓库内 `docs/memory/*`）；② `::inbox-item{title="…" summary="…"}` 可把「有发现」推进 **Scheduled 收件箱**（章程 §11.6 依赖的未读通道）；③ 心跳 run 的 agent **能调 `automation_update(mode = "delete")` 把自己删掉**（GATE-0.4 的清理即由此完成）。
- [2026-09-24][FACT][src:GATE-0 实测] **本机环境已换，旧结论不可照抄**：CLI `codex-cli 0.154.0-alpha.6.2` → **`0.155.0-alpha.16.3`**；端点/模型 阿里云百炼 `qwen3.8-max` → **`opencode_go` / `deepseek-v4.1-flash`**；桌面应用 `26.917.51856`。`model_context_window` 实测 **950000**（≠ `config.toml` 写的 1000000）。cron 抖动 **+46 s**（17:40:00 → 17:40:46）、heartbeat **约 +1 s**；cron 每次 run 开全新 chat，产物落在 `cwd = D:\csart\ai-assistant`（主 checkout，非 worktree）。

- [2026-09-24][FACT][src:TASK-015 实测] **`docscan` 4 条新结构规则上线时的存量（全仓 158 个 `.md`）**：`doc/nul-byte` **2**（= PL-051 的 `docs/memory/{facts,pitfalls}.md`，同批修掉 → 0）；`doc/duplicate-section-number` **33**；`doc/empty-section` **492**；`doc/duplicate-heading` **47**。前三条之外**没有任何 Error 级发现项**（破表 / setext / 编码形状仍是 0）→ `xtask docscan` PASSED（**0 error**）。⚠ warning **总数是派生值**（任何一次文档编辑都会改动它，本卡记录区填完即下降），按 ADR-0030 D2 **不写进记忆** —— 只记上面 4 条规则各自的存量。存量里既有**真债**（stage-0 的 8 张 spike 卡记录区整节未填）也有**按定义合法的形态**（`Ready` 卡的 9 节空骨架、TASK-011 那种「每轮追加一个 UPDATE 块」的重复节标题）→ 所以后三条**先 Warning**，清扫与判据收紧归 `docs/PARKING_LOT.md` **PL-055**。
- [2026-09-24][FACT][src:TASK-015 实测] **两个新子命令上线即绿**（ADR-0025 D1 的「上线即全绿则可直接 Error」口径因此成立）：① `xtask check-ledger` → `plan_date=2026-09-24`（`PLAN.md:10`）= `ledger_last_date=2026-09-24`（`LEDGER.md:132`），`current_stage=阶段 1` → 0 error；② `xtask check-migrations` → `scanned_migration_files=3` / `registry_entries=3` / `crates_with_migrations=2` → 0 error（0001 `crates/storage`、0002/0003 `crates/audit` 与 `docs/storage-design.md` §3.4 逐行一致）。
- [2026-09-24][FACT][src:TASK-015 实测] **gov §5.1 #5 从「0 个测试的假绿」变成真断言**：`cargo test -p assistant-core arch::` 由 `running 0 tests` → **`running 5 tests`**（`crates/core/tests/arch_layering.rs`：正向 2 + 负向 3）。同批把 #5 与 #16 两条 CI 门禁由 `continue-on-error: true` 转硬（ADR-0039「验证方式 2」原文要求 check-ledger 由 CI 硬拦；ci.yml 的 #5 注释原本就写着「启用卡号：TASK-015」）。
- [2026-09-24][FACT][src:TASK-015 实测] **PL-051 的 4 个裸 NUL 的真身是被 NUL 顶掉的数字 `0`**：`facts.md` 偏移 46886/46909 与 `pitfalls.md` 偏移 42266/42289 处的字节序列是 `<NUL>x0` / `<NUL>，`，而同仓库另外 3 处同一条记录（`docs/memory/win32-input-research.md:85`、`tasks/TASK-100-*.md:120`、`docs/memory/apps/notepad.md:254`）写的是 **`GetFocus() = 0x0`** 与 **`SendInput 返回 0`** → 正确修复 = 把这 4 个字节还原成 ASCII `0`（0x30），**不是** PL-051 建议的「改成字面量 `\0` 文本」（那会得到 `\0x0` 这种无意义文本）。修完 `git diff` 是「1 行改动/文件」，不是二进制 diff。
