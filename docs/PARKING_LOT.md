# PARKING_LOT.md — 停车位（不相关但值得记住的事）

> **用途**：执行任务卡时发现"这也有问题"，但**不属于本卡范围** —— 一行记在这里，本卡不动手。
> 这是 gov §3.3 禁止 drive-by refactor 的配套设施：没有停车位，agent 只能选择
> "顺手改掉"（漂移）或"假装没看见"（丢失信息），两者都更糟。
> **处置时机**：阶段末评审（gov §7.2）。模板见 gov §9.5。

| 日期 | 提出者 | 事项 | 相关卡 | 处置（阶段末评审） |
|---|---|---|---|---|
| 2026-09-16 | TASK-001 | PL-001 `gov §5.4` 表格实际是 **11 行**，但 `plans/stage-1-pilots.md` 的 TASK-015 DoD 写"hygiene **12** 项检查全部生效"。数字不一致，需人类裁决以哪个为准（`plans/*` 不在 TASK-001 write scope 内，故未改） | TASK-015 | 待评审 |
| 2026-09-16 | TASK-001 | PL-002 `check-comments` / `check-ledger` / `card-check` 三个护栏子命令**没有任何任务卡认领**（gov §5.1 的 #15 #16 门禁因此无人负责）。需补卡或并入 TASK-015 | TASK-015 | 待评审 |
| 2026-09-16 | TASK-001 | PL-003 `xtask/src/report.rs` 的模块注释引用了 `docs/spec/testing.md` §4.3，但该 spec 属于"后续卡写"，目前不存在（悬空引用）。写 spec 的卡需要回填这一节 | 写 docs/spec/testing.md 的卡 | 待评审 |
| 2026-09-16 | TASK-001 | PL-004 「文档注释中用反引号引用的标签字样」（例如在 `///` 里解释什么是被禁标签）是否应豁免 `hygiene/banned-comment-tag` 与 `hygiene/missing-card-reference`？当前实现**不豁免**，因此写规则说明时必须绕开字面量。TASK-001 采取"改措辞"绕过，未改规则语义 | TASK-015（check-comments 设计） | 待评审 |
| 2026-09-16 | TASK-001 | PL-005 `docs/OPEN_SOURCE_CHECKLIST.md` 在 gov §10 清单里被要求，但不在 TASK-001 的 write scope 内，故未创建。开源前（M5 之后）需要补 | 开源准备 | 待评审 |
| 2026-09-16 | TASK-001 | PL-006 `cargo deny` 未在本机安装，CI 里装了但本地跑不了 → 本地验收与 CI 验收不等价。建议在 TASK-015 前把 `cargo-deny` 写进开发环境准备清单 | TASK-015 | 待评审 |
| 2026-09-16 | TASK-001 | PL-007 `.github/workflows/.gitkeep` 在 `ci.yml` 就位后已冗余，可删（未删：删除文件不在本卡必要动作内） | — | 待评审 |
| 2026-09-16 | TASK-001 | PL-008 git 提交身份目前是**仓库本地占位**（`Codex (ai-assistant agent)` / `codex@localhost.invalid`），本机没有任何 global gitconfig。推 GitHub 前必须设成真实身份，否则历史里全是占位作者 | 开源准备 | 待评审 |
| 2026-09-16 | TASK-001 | PL-009 仓库根同时存在 `cross-platform-ai-assistant-architecture.md`（v1 原始规划）与 `...-v2.md`（SSOT）。AGENTS.md 只指向 v2，但两者并排放在根目录容易让新 agent 读错版本。建议把 v1 移到 `docs/history/` 并加"已被 v2 取代"横幅（v1 不在任何卡的 write scope 内，故未动） | — | 待评审 |
| 2026-09-16 | TASK-001 | PL-010 `docs/governance-ai-agent-execution.md` §9.2 的 LEDGER 模板没有"commit 哈希在建库前无法预知"的处理约定，导致首条台账只能先写占位再回填（违反"只追加不改写"的字面要求）。建议在模板里明确：commit 列允许写 `pending`，并在下一次提交中**追加一行**回填而非改写 | TASK-015（card-check） | 待评审 |
| 2026-09-16 | TASK-001 | PL-011 建议给 gov §5.4 增加第 12 项卫生规则：**文件不得含 CRLF**（`hygiene/crlf-line-endings`，Error 级）。本次就真实踩到了 —— 编辑脚本按旧约定写回 CRLF，`.gitattributes` 只能保证入库形态、管不住工作区，而 `cargo fmt --check` 会在 Linux runner 上因此变红。**顺带**：这可能正是 PL-001 里"hygiene 12 项"与"表格 11 行"数字不一致的来源，两条可一并裁决 | TASK-015 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-012 **夜间 heartbeat 自动化与第三方 Responses 端点不兼容（夜间自动化实际从未成功跑起来）**：Codex 的 heartbeat 会往所附着 thread 注入一条合成的 `function_call_output`（`name=automation_update`、`namespace=codex_app`），该项**没有 `call_id`**；阿里云百炼 compatible-mode 与 MiniMax 的 `/responses` 都会 400 拒绝（`Invalid 'call_id': call_id is required for function_call_output.`）。又因 `disable_response_storage=true` 每轮重放全量历史，该 thread 之后**每次**提问都失败（新开 thread 正常）。已用 `D:\csart\fix_codex_callid.py` 对 rollout 做等长度原地修补恢复问答，但只要 heartbeat 继续注入就会再次损坏。建议：把章程 §11 的 heartbeat 机制改为**独立 cron 工作单**（每次唤醒新建 thread、prompt 走 user message，不产生 function_call_output），或夜间任务改用官方 OpenAI 端点 | docs/overnight-automation-charter.md §11 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-013 **更正 PL-012 的建议：改 cron 无效，已实测否决；正解是 `codex exec` CLI + 外部调度**。2026-09-17 探针实测（临时 cron `probe-call-id`，约定 11:16/11:24，实际 11:17:54/11:25:54 触发，各新建独立会话 `01a0ad5e-…`/`01a0ad66-…`）证明：① cron **能**在本机创建并触发 —— 章程 §11.1「cron 需 ChatGPT 鉴权、本环境不可用」的论断**错误**，真实原因是 cron 强制要求显式提供 `model` + `projectId` + `executionEnvironment="local"`，缺任一只回一句含糊的 `Failed to create automation.`；② 但 cron 与 heartbeat **共用同一投递机制** —— prompt 一律被包装成合成 `FunctionCallOutput{id:None, call_id:None, name:"automation_update", namespace:"codex_app"}`，**不是** user message，故 PL-012「cron 走 user message、结构上不产生毒项」的推断不成立；③ 两次运行均在 **2.6s 内失败、零产出**（`last_agent_message=null`），错误变为 `Invalid 'id': message id must be a string starting with 'msg_', got 'at_<uuid>'`，且该 `at_` id 由 Codex **序列化请求时临时生成、从不落盘**（rollout 内只有 `msg_`/`fco_` 两种合法前缀）→ **等长补丁法对它完全无效**。对比：老 heartbeat 会话里的毒项落盘 id 是合法的 `fco_…`、只缺 `call_id`，故补 `hb_…` 即恢复问答；④ **已实测可行的替代**：`codex.exe exec --skip-git-repo-check "<prompt>"`（CLI v0.154.0-alpha.6.2，实测会话 `01a0adcd-2b86-…`）—— prompt 走标准 `user` message（id `msg_…`）、`function_call_output` 计数 **0**、无 `automation_update`、无 `at_` 前缀、模型正常回复、退出码 0、19422 tokens。**建议**：夜间自动化改为 Windows 任务计划程序 + 包装脚本调 `codex exec`，并据此重写章程 §11 全章。附带发现：自动化调度存在约 **+2 分钟 jitter**（`.run-jitter-salt`），CLI 路径无 jitter，定时更准 | docs/overnight-automation-charter.md §11 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-014 **百炼端点两处环境噪音（均不影响推理，但影响可观测性与上下文窗口估算）**：① `list_models` 永久失败 —— 百炼 `/models` 返回 `{"first_id":…,"data":[…]}`，缺 Codex 解码器要求的 `models` 字段，故报 `failed to decode models response: missing field 'models'`，`models_cache.json` 解析出 0 个 id；因 `model` 在 config.toml 硬写，推理不受影响，仅模型选择器无法在线刷新。② CLI 运行时警告 `Model metadata for 'qwen3.8-max' not found. Defaulting to fallback metadata; this can degrade performance` —— 该模型不在 Codex 内置元数据表，实测 cron 探针会话 `task_started` 里 `model_context_window=828400`，而 config.toml 配的是 `1000000` → **fallback 元数据可能覆盖显式配置**，导致 auto-compact 提前触发（本 thread 已在 10:49:52 撞 ContextLimit）。建议核实 `model_context_window` 在 CLI/自动化路径是否生效，必要时在包装脚本里用 `-c model_context_window=1000000` 显式覆盖 | 开发环境准备清单 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-015 **MEMORY.md §1 快照与 §3 DECISION 中有两条已被本次实测推翻、但本会话无权更正的条目**：① §1 快照「执行方式」行写「夜间由 heartbeat automation 推进（每日 23:30 与 02:30，见章程 §11）」—— heartbeat 已删除，当前夜间自动化**处于停摆状态**；② §3 `[2026-09-16][DECISION][ADR:待建 0018]`「夜间自动化用 heartbeat」—— 该决策已失效，且 ADR 0018 **尚未建立**。按 AGENTS.md §8，Implementer 对 MEMORY.md 只有 §2/§5 的追加权；§1 属 Orchestrator（阶段末更新），§3 属 DECISION、按铁律 10「契约先行」必须先有 ADR 才能改。故本次只在 §2/§4/§5 追加了带 `[supersedes:2026-09-16]` 的更正条目，**未动 §1/§3**。需要人类：建 ADR 0018（正式否决 heartbeat、采纳 `codex exec` + 外部调度），随后由 Orchestrator 覆写 §1 快照 | MEMORY.md §1/§3、ADR 0018 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-016 **`deny.toml` 的 `db-path` 写成数组导致整条 deny 门禁从未真正跑起来（TASK-001 遗留缺陷，本次已修）**：`db-path = ["target/deny-db"]` 在 cargo-deny 0.20.2 下报 `expected a string`，配置文件直接解析失败。CI 之所以没发现，是因为它用 `EmbarkStudios/cargo-deny-action@v1`（该 action 最新已 v2.1.1）**且未钉 cargo-deny 版本**，与本地的 0.20.2 不是同一个解析器 —— 正是 PL-006「本地验收与 CI 验收不等价」的实例。已改为字符串形式 `db-path = "target/deny-db"`（新旧版本均兼容）。**建议**：① CI 显式钉 cargo-deny 版本；② 把「deny.toml 能否被解析」纳入本地验收清单；③ 与 PL-006 合并处置 | TASK-015 / CI | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-017 **缺一份《开发环境准备清单》**（PL-006 已提过，本次再次被卡）：`cargo-deny` / `cargo-llvm-cov` 的安装方式、版本、来源与校验方法目前只存在于聊天记录里，新机器或新 agent 无法复现。本次实测可用的配方：从 GitHub 官方 release 取预编译包（**注意本机 github.com 的 release 下载与 git clone 被重置，需走 `ghproxy.net` 镜像**），并用**直连可达的** `api.github.com` 的 `digest` 字段逐个校验 sha256 后装入 `~/.cargo/bin`；`cargo deny check` 另需经进程级 `GIT_CONFIG_*` 环境变量把 `RustSec/advisory-db` 的 clone 重定向到镜像。建议落成 `docs/dev-env-setup.md` 并写进 gov §10 清单 | 开发环境准备 / 开源准备 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-018 **三项硬门禁缺「负向验证」**（ADR-0019 登记表的 ❌ 项）：gov §5.1 的 #1 `cargo fmt --all --check`、#2/#3 `cargo clippy -D warnings`（含 `[workspace.lints]` 禁用项）、#10 `cargo build --release` 目前只有"绿灯"证据，没有"该红时会红"的证据。建议在 TASK-015 把它们并入 `gate-selftest.yml` 的 canary（各自注入一个已知坏样本：格式错的 `.rs` / 含 `unwrap()`+`dbg!` 的 `.rs` / 编译不过的 `.rs`，断言 exit ≠ 0）。**规则已生效**：自 ADR-0019 起，任何"软门禁转硬"的卡必须同时提交该门禁的负向验证并在 ADR-0019 登记表补一行，否则不得转硬。附带建议：TASK-015 的 `card-check` 落地后，把"转硬必须改登记表"做成机器检查 | TASK-015 / ADR-0019 | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-019 **`spikes/` 被 workspace `exclude`，四道硬门禁（deny / fmt / clippy / test）全都覆盖不到 spike 代码**（`Cargo.toml`：`exclude = ["spikes", "fixtures/apps", "tools"]`）。阶段 0 这是**设计意图**（spike 是一次性验证代码，不该拖慢主线门禁），但后果是：spike 里 `cargo add` 任何依赖都**只有政策约束（DEPENDENCIES.md 规则 1）、没有机器约束**。TASK-002 即将引入 `windows` crate + `uiautomation`，正好撞上该缺口。**建议（择一，需人类裁决）**：① 给 `spikes/` 单独一个 deny job（`--manifest-path spikes/<name>/Cargo.toml`，只查 licenses + sources，跳过 advisories 省时间）；② 在 `xtask hygiene` 加规则「`spikes/**/Cargo.toml` 的依赖必须能在 DEPENDENCIES.md 找到对应行」（归属 TASK-015）；③ 维持现状，但在每张 spike 卡的 DoD 里显式写「依赖已登记」并要求人工核对。**附带**：`uiautomation` 未列入 DEPENDENCIES.md 的「计划首批依赖」清单，引入前需补行 | TASK-002 / TASK-015 / CI | 待评审 |
| 2026-09-17 | 人类会话（非任务卡） | PL-020 **建议给 `xtask hygiene` 增加一条规则：「文本文件必须以单个 `\n` 结尾，且不得以空行结尾」**（`hygiene/missing-final-newline`，Warning 级）。本次真实踩到：`docs/DEPENDENCIES.md` 末行无换行，导致以「整行+`\n`」为锚点的编辑脚本 `count==0` 断言失败，而报错信息与真因毫无关系。该规则与 PL-011（CRLF）同属 gov §5.4 家族，**建议三条一并裁决**：PL-001（11 行 vs 12 项的口径）、PL-011（禁 CRLF）、PL-020（必须有末行换行）—— 若同时采纳 PL-011 与 PL-020，gov §5.4 将变成 13 项，各处 `3/11` 需改述为 `3/13` | TASK-015 / gov §5.4 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-021 **建议给 `xtask hygiene` 增加第 14 项规则：所有 `Cargo.toml` 必须有 `license` 字段**。本机实测踩到：`spikes/spike-a-notepad/Cargo.toml` 初版没写 `license`，`cargo deny check licenses` 直接报 `error[unlicensed]` 并 **exit 4** —— 也就是说新建的 `spike-deny` 门禁（gov §5.1 #8b）会因一个**与依赖无关的字段缺失**而变红，而报错文字不会提醒你「这是新 crate 忘了填字段」。已当场补 `license = "MIT"` 并在行内注释写明原因；ADR-0025 「考虑过的选项 5」已把它**推迟到本条**（因为当时未走人类裁决）。归 TASK-015 | TASK-015 / gov §5.4 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-022 **根治「同一数字手写多处」：让 `xtask` 直接从事实源派生计数**。具体：① 解析 `gov §5.4` 的表格行数 → 得到 `TOTAL_HYGIENE_RULE_COUNT`（不再硬编码）；② 校验 `gov §5.1` 的清单行数 ↔ `ci.yml` 的步骤数；③（可选）校验 `DEFERRED` 条数 = TOTAL − IMPLEMENTED。ADR-0025 D4 已把它点名为**唯一的长期解**；本次落地时又找出 **7 处**漏改（含 **SSOT `architecture-v2` 自己的 2 处**），正是这个问题的实证。归 TASK-015（需先让 `xtask` 能读到 `docs/`，而它目前声明「只读、零第三方依赖」，解析 Markdown 表格要自己写） | TASK-015 / xtask | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-023 **夜间自动化的两个未定事项**：① `docs/nightly/logs/` 的轮次日志**是否入库**待裁决（入库 = 可回溯但仓库会被机器生成物污染；不入库 = 夜间失败无证据可查）；② `scripts/` 顶层目录与 `scripts/nightly-run.ps1` 的归属 —— ADR-0018 已授权该目录，但 hygiene 规则「新增顶层目录必须在 ADR 白名单中」**尚未实现**，需在 TASK-015 前确认白名单已含 `scripts/`，否则规则一上线就会报自己。附：验收文档 `docs/nightly/scheduler-acceptance-test.md` §6 已列明这些前置物**尚未创建** | 夜间自动化 / TASK-015 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-024 **`create_thread` 上游跟踪**：openai/codex **#36315** / **#36250** 关闭后，重测一次「把 `target` 写成对象能否成功」；成功则把「人类手工建会话」从**强制前置**降级为备选，并回写 `docs/subagent-orchestration.md` §10.1、`docs/memory/open.md` N3、`docs/memory/rejected.md` 2026-09-18 末条。（本条只作跟踪占位，根因与证据已入 `docs/memory/facts.md` 2026-09-18 节） | 多 agent 编排 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-025 **`gov` §10「落地检查清单」已与事实脱节，且没有「谁负责勾选」的约定**：清单里 `docs/spec/naming.md`、`LEDGER.md`、`docs/PARKING_LOT.md`、`docs/DEPENDENCIES.md`、`tasks/` 目录均**已实际存在**但仍为未勾选。本次只改了其中「14 项门禁」的数字（属 ADR-0025 D4 口径统一），**未勾选任何框**（避免 drive-by）。建议：归阶段 0 末的对齐审计（或 TASK-015）一次性回填，并在清单表头写明「由审计 agent 在阶段末核对」 | 阶段 0 末审计 / TASK-015 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-026 **`probe-01` / `probe-02` 仍含非 ASCII 字节（788 / 941 个），违反 ADR-0024 D4「仓库内 `.ps1` 一律纯 ASCII」**。它们是 D4 采纳**之前**提交的（`bc08a7e`），本次只修了未提交的 `probe-03`。**不在本会话里改**的理由：它们已产出被 ADR/SPIKE-A 引用的证据，改写就必须重跑验证「修编码不修语义」（probe-03 花了一轮 5 次启动记事本才证明），属独立工作量。**建议**：归 TASK-002 正式开工时顺手做（那时本就要重跑全部探针），或由 TASK-015 加一条 hygiene 规则（候选第 14/15 项，与 PL-021 一并裁决）。校验命令：`[IO.File]::ReadAllBytes($f) \| ? { $_ -gt 127 } \| Measure-Object` 应为 0 | TASK-002 / TASK-015 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-027 **hygiene 第 13 项（末行换行）的待清扫清单已变，且它有两种违反形态**。2026-09-18 全仓重新扫描（按 ADR-0025 D1 的扩展名白名单，排除 `.git`/`target`）：**形态 A：缺末行换行 = 10 个**（`.gitattributes`、`.gitignore`、`Cargo.toml`、`CLAUDE.md`、`clippy.toml`、`deny.toml`、`LICENSE`、`rust-toolchain.toml`、`rustfmt.toml`、`xtask/Cargo.toml`）；**形态 B：以 `\n\n` 结尾（末尾多一个空行）= 1 个**（`target-apps-feasibility.md`）。合计 **11 个**，不再是 PL-020 里写的 15 个（差额：5 个被本次文档编辑顺手修好、另 2 个形态 B 本次已修）。**含义**：① TASK-015 的规则实现**必须同时报两种形态**（只查「不以 `\n` 结尾」会漏掉形态 B）；② DoD 写「清扫 11 个文件 + 升 Error」；③ 扫描命令已存为 `D:\csart\scan_eol.py`（**未入库**，建议 TASK-015 改写成 `xtask` 子命令而不是外部脚本） | TASK-015 / gov §5.4 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-028 **两处「裸 ADR 引用」违反 ADR-0026 D3**：`docs/adr/0021-*.md:4` 写 `ADR-0017（显式登记）`、`docs/adr/0023-*.md:179` 写 `（ADR-0016）`，但 0016/0017 **没有对应文件**（只存在于 `docs/memory/decisions.md` 的 `[ADR:待建 NNNN]` 条目）。裸写法会让下个 agent 去 `cat` 一个不存在的文件，进而怀疑决策丢失、甚至重新做一遍决定。**未当场改**：ADR 文件「只增不改」，改写正文需要人类授权 → 建议留到阶段末评审，或等这两份 ADR 下次被 supersede 时顺带修正 | ADR-0026 D3 / `docs/adr/README.md` §4 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-029 **章程 §13 W4 的 write scope 已过期**（**已关闭（ADR-0026 D4 执行，章程 v1.4）**：章程 §13 W4 的 write scope 由「用范围写法表示的 0016 至 0020（新建 5 个文件）」改为**逐个列出**的 **0016 / 0017 / 0020 / 0027** 四个号；「禁止」项由「新建 0021 及以后的编号」改为「新建 **0028 及以后**的编号」，并新增「不得复用**已退役编号 0019**」；验收由「5 个文件」改为「**4 个文件**」，并新增两条机器验收（`xtask hygiene` 与 `xtask adr-index` 均须 0 Error）。**write scope 额外含 `docs/adr/README.md`（仅回填）**。并新增一条通用要求：ADR 编号禁止写范围形式） | ADR-0026 D4 / 章程 §13 W4 | **已关闭（ADR-0026 D4）** |
| 2026-09-18 | 人类会话（非任务卡） | PL-030 **建议给 `xtask` 加 `adr-index` 子命令，把「ADR 编号登记表」从手工维护变成机器校验**：扫 `docs/adr/*.md` 的编号集合 ↔ `docs/adr/README.md` §1 表格，不一致即失败；顺带扫「裸引用」（`ADR-00NN` 出现在 `docs/adr/` 之外且不带「待建」字样 → Warning，可解 PL-028 复发）。**本次刻意未实现**：加子命令是漂移触发器 ②，且不在任何卡的 write scope 内 → 归 TASK-015（与 PL-002 / PL-021 / PL-022 同一批护栏增强）。落地后 ADR-0026 D1 的「手工维护」应被取代（见 ADR-0026「重新评估触发条件」②） | ADR-0026 风险表 / TASK-015 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-031 **3 个表格单元格里有未转义的竖线，GitHub 渲染会破表**（GFM 规定：表格单元格内的竖线即使在代码跨度里也必须写成反斜杠+竖线）。实测（按「逐字符扫描代码跨度状态」的准确判据，全仓 3 处命中）：① `LEDGER.md:32` —— 开源脱敏预检那行里的密钥扫描正则含 **8** 个裸竖线；② `docs/PARKING_LOT.md:57` —— 同一条正则的 **9** 个裸竖线；③ `docs/nightly/scheduler-acceptance-test.md:84` —— 断言 C1 的判定方法是一条 PowerShell 管道命令，**1** 个裸竖线，破表后从渲染页复制该命令会得到跑不通的半截命令（这条最影响使用）。**未当场改**：①②在只追加文件里（LEDGER 明写「已写入的行不得修改或删除」，PARKING_LOT 明写「不改写原行」），改转义虽不改语义但要动既有行 → 需人类授权；③ 属夜间自动化文档，与 ADR-0018 的验收一并处理更合适。**建议**：给 `xtask hygiene` 增一条规则「表格行的代码跨度内不得出现裸竖线」（Warning 起步，清单同上 3 处），与 PL-021/PL-022/PL-030 同批归 TASK-015 | ADR-0026 / gov §5.4 / TASK-015 | 待评审 |
| 2026-09-18 | 人类会话（非任务卡） | PL-032 **「裸 ADR 引用」的机器检查仍未实现（PL-028 / PL-030 的这一半被显式推迟）**。ADR-0030 **D4 刻意不在本轮实现**，理由是：现存 2 处违规（`0021-*.md:4` 的 `ADR-0017`、`0023-*.md:179` 的 `ADR-0016`）**都在「只增不改」的 ADR 正文里**，先实现规则就等于造一条**永久红灯**，而永久红灯会让人学会忽略 CI（`ci.yml` 头部注释已把这条写成设计原则）。**正确顺序**：先决定豁免机制（可参照 ADR-0030 D3 的「已退役编号」清单，例如给 `xtask` 一份「已知的历史裸引用」白名单），再实现规则。规则语义已在 ADR-0026 风险表描述：裸写 `ADR-00NN`（NN 属登记表 §2 的待建号集合）且出现在 `docs/adr/` 之外 → Warning。⚠ 实现时注意：该规则的正则含竖线字符，**直接写进 Markdown 表格会破表**（PL-031 的教训），只在代码里落地 | PL-028 / ADR-0030 D4 | 归 **TASK-015**（护栏增强） |
| 2026-09-18 | 人类会话（非任务卡） | PL-033 **`gov` 内部对「单文件行数」有两套互相矛盾的口径，且与 `AGENTS.md` 也不一致**。实测：`gov §5.4` 表格行写「**> 600 行警告，> 900 行失败**」，而 `gov` 另一处「模块规模」写「**单文件 ≤ 400 行（软）/ 600 行（硬）**」，`AGENTS.md §5.3` 抄的是后者；`xtask hygiene` 的 `hygiene/file-too-long` **实现的是前者**（>600 Warning、>900 Error）。后果：按 `AGENTS.md` 读的人会以为 601 行就该拆（硬），按工具跑却只得到 Warning —— 两种理解都能自圆其说，于是「拆不拆」变成看谁在场。**本轮不改门禁语义**（放宽/收紧护栏都是漂移触发器），改用**拆文件**把矛盾消解在事实上：`adr_index` / `adr_registry` / `guard` / `guard_runner` 四个模块的测试用 `#[path]` 外置后，全部文件在**两种口径下都不再超限**。**待裁决**：① 统一为一套阈值（哪一套？改哪份文档？）；② 或者明确「400/600 是**写作规范**、600/900 是**CI 门禁**，两者本来就不该相同」并把这句话写进 gov 与 `AGENTS.md`。任一方案都需要 ADR | gov §5.4 / `AGENTS.md` §5.3 / TASK-015 | 待裁决（需 ADR） |
| 2026-09-18 | 人类会话（非任务卡） | PL-034 **「ADR 编号禁止写范围形式」这条新规矩与仓库现状冲突：既有文件里仍有 9 处范围写法**。本轮全仓扫描（判据 = 两个 4 位编号之间夹一个波浪号或连字符）命中三类：**① 只追加 / 已提交因而不能改的 5 处** —— `LEDGER.md` 的三行历史事件（含上一会话写入的一处）、`docs/memory/decisions.md` 的 2026-09-16 条目、`docs/adr/0026-*.md` 正文两处；**② 可改但本轮未动的 2 处** —— `docs/wbs-overview.md:147` 与 `plans/stage-0-spikes.md:30`（两处都还引用了 ADR-0021 分层之后**已不存在**的 `MEMORY.md §3`，属悬空引用）；**③ 刻意保留的 2 处规则引文** —— 章程 §13 W4 的修正说明与本文件 PL-029 关闭行（不写出违规形态就无法说明规矩是什么）。→ 建议：① 归 TASK-015 一并清扫（顺带修 `MEMORY.md §3` 的悬空引用）；② 进豁免清单；③ 第 ③ 类需要一个**机器可判**的豁免标记（例如同一行出现「禁止写范围形式」字样即豁免），否则未来的 `check-comments` 规则一上线就是永久红灯 —— 这与 **PL-032** 是**同一个**豁免机制问题，建议合并裁决 | PL-029 / PL-032 / ADR-0026 D3 | 待评审（需 ADR） |
| 2026-09-18 | 人类会话（非任务卡） | PL-035 **三份文档把 `AGENTS.md` 的行数手抄了一遍，且三个数字互不相同、全部已过期**。实测：`docs/subagent-orchestration.md` §5.2 派单包写「AGENTS.md 全文（**~164 行**）」、`docs/governance-ai-agent-execution.md` §4.1 启动协议写「① 读 AGENTS.md（全文，**~164 行**）」、架构 v2 §派单包写「AGENTS.md（全文，**~140 行**）」；而 `AGENTS.md` 实际是 **185** 行。**这是 PL-022 / ADR-0030 同一根因的第三次发作**（派生值被手抄进文档 → 过期 → 三处还互相矛盾：164 vs 140）；§5.2 的「总量 ≤ 800 行」预算是**按这些行数算的**，行数假了预算就假了。**本轮刻意不改**：修它属 drive-by refactor（gov §3.3），正确解不是「改对数字」而是**删掉数字**——按 ADR-0030 D2 改为指向事实源（例如「AGENTS.md 全文」，不写行数；行数看 `wc -l`），并重新校准 800 行预算。→ 归 **TASK-015**（与 PL-022 / PL-032 / PL-033 / PL-034 同批护栏增强）；建议顺便给 `xtask` 加一条规则：文档里禁止出现「~NNN 行」形式的派生行数 | §5.2 / gov §4.1 / v2 §派单包 / ADR-0030 D2 | 待评审 |

## 处置追加记录（只追加；不改写上表既有行）

> 上表的「处置」列按 gov §7.2 在**阶段末评审**统一填写。在那之前，若某条已被人类裁决或已落地，
> 在这里追加一行说明，**不改写原行**（与 `LEDGER.md` 同一规矩）。

| 日期 | 关联 | 处置说明 |
|---|---|---|
| 2026-09-17 | PL-008 | **已落地关闭**：`origin` 已建立（HTTPS + 本机代理）；全部 7 个提交的 author/committer 由占位身份重写为 `snofin18 (via Codex) <snofin@gmail.com>`（人类裁决方案 B，保留 author date）；仓库本地 `user.name` 已同步。哈希映射与证据见 `MEMORY.md` §2 与 `LEDGER.md` 2026-09-17 各行 |
| 2026-09-17 | PL-016 | **建议 ①② 已落地**：① `ci.yml` 的 `EmbarkStudios/cargo-deny-action@v1` 钉到 **`@v2.1.1`**（该 release 打包的即 cargo-deny 0.20.2，与本机一致；该 action 无 `version` 输入，故钉 release tag 就是钉版本）；② 本地验收追加 `cargo deny check licenses bans sources`（离线可跑，专门证明 `deny.toml` 能被当前版本加载），并已建成 `gate-selftest.yml` canary 做正/负双向断言（ADR-0019 N3）。**建议 ③「与 PL-006 合并处置」仍待评审** |
| 2026-09-17 | PL-006 | **部分解除**：本机已装 `cargo-deny 0.20.2` + `cargo-llvm-cov 0.9.1`，本地验收与 CI 已等价（TASK-001 6/6，行覆盖 96.31%）。但"把安装方式写进开发环境准备清单"仍未做 → 归 **PL-017**（`docs/dev-env-setup.md`） |
| 2026-09-17 | PL-001 / PL-011 | **仍未裁决**。本次 DRIFT-001-2 的 a/b/c 落地时**刻意没有**顺带处理它们（避免范围蔓延）。提醒：若 PL-011 被采纳（新增第 12 项 CRLF 规则），`tasks/TASK-001-repo-skeleton.md` §5.1/§5.2 与卡面里的 `3/11` 需改述为 `3/12`；PL-001 若裁决，还需统一 gov §5.1 行数与 `plans/stage-1-pilots.md`「CI 14 项门禁」的表述 |
| 2026-09-17 | PL-020 | **实测数据补充**：全仓库扫描（排除 `.git`/`target`）发现 **17 个文件末尾缺换行**。其中 2 个（`docs/adr/0019-*.md`、`.github/workflows/gate-selftest.yml`）是本次新建、**已当场补齐**；其余 **15 个是 TASK-001 的既有文件**（`.gitattributes`、`.gitignore`、`Cargo.toml`、`CLAUDE.md`、`clippy.toml`、`deny.toml`、`LICENSE`、`README.md`、`rust-toolchain.toml`、`rustfmt.toml`、`ci.yml`、`overnight-automation-charter.md`、`tasks/TASK-001-repo-skeleton.md`、`xtask/Cargo.toml`、`xtask/README.md`），按 gov §3.3「禁止 drive-by refactor」**本次不动**，留给 TASK-015 在 `hygiene/missing-final-newline` 规则落地时一次性清扫（届时规则需先以 Warning 级上线、清扫完成后再升 Error 级，否则规则一上线就红 15 处） |
| 2026-09-18 | PL-019 | **已关闭（采纳方案 ①，ADR-0024 D2）**：`ci.yml` 新增 **`spike-deny`** job（gov §5.1 子编号 **#8b**），枚举 `spikes/*/Cargo.toml` 逐个跑 `cargo deny check licenses sources`（**只查这两项**：免联网、且避开 advisories/bans 对 spike 产生的永久噪声）；无 manifest 时**显式打印「无」再 exit 0**（铁律 1：不得静默通过）。`gate-selftest.yml` 新增 `spike-deny-gate` canary（本地 path 依赖 + 假 GPL fixture，断言 exit ≠ 0）；ADR-0019 登记表补 **#8b** 行。**双向实测（本机 2026-09-18）**：正向 `spikes/spike-a-notepad` → `licenses ok, sources ok` **exit 0**；负向 GPL fixture → **exit 4** + `error[rejected]`。方案 ②（hygiene 规则「spike 依赖必须在 DEPENDENCIES.md 有行」）**仍归 TASK-015**；方案 ③ 作废。依赖登记已补：`windows =0.62.2` → Approved for spikes；`uiautomation` → **Rejected**（ADR-0024 D1） |
| 2026-09-18 | PL-001 / PL-011 / PL-020 | **三条一并裁决关闭（ADR-0025，人类指示 #12「其他按最佳方案实行」授权）**：gov §5.4 = **13 项**（新增「禁 CRLF」**Error** 级 + 「必须末行换行」**先 Warning 后 Error**）；`plans/stage-1-pilots.md` 的「hygiene 12 项」确认为**笔误**、「CI 14 项门禁」确认为**过时表述**。`xtask` 已同步（`TOTAL_HYGIENE_RULE_COUNT` 13 / `DEFERRED` 10 / 单测断言同步），`cargo test --workspace` **99 passed**，`xtask hygiene` 打印「共 13 项，已实现 3 项，未实现 10 项」。**本次补全了 ADR-0025 首轮漏盘的 7 处**：`xtask/README.md`、`gov` §10 清单、`architecture-v2` ×2（**SSOT 自己**）、`charter` W2 ×2、`plans/stage-1-pilots.md` TASK-039 行。**末行换行的清扫仍留 TASK-015**（Warning → Error 路线不变；DoD 必须同时含「清扫」与「升 Error」两件事）—— 但待清扫数量已从 15 降为 **11**，且发现第二种违反形态，详见 **PL-027**。**顺带（纯空白改动，未改任何条目文字）**：删除了本表 PL-018/PL-019/PL-020 三行之间的两个空行 —— 它们会在 Markdown 里截断表格渲染，使后三条变成普通段落 |
| 2026-09-18 | PL-012 / PL-013 / PL-015 | **机制层面已关闭，验收层面仍开放**：ADR-0018 转为 **Accepted** —— 夜间自动化的投递机制 = **Windows 任务计划程序 + `codex exec`**，**放弃** Codex automation（heartbeat 与 cron 两条路都已被探针否决：投递机制产生畸形 `function_call_output`、缺 `call_id`）。章程 §11 已据此**全章重写为 v1.3**（11.1~11.9，含 10 条本机实测适配要点、退出码三层文件信号、4 步排查顺序）；PL-015 点名的两条过期记忆已随 ADR-0021 分层迁移一并纠偏。**仍开放的部分**：端到端冒烟一次都没跑过（`docs/memory/open.md` **N4/N5**）、`scripts/nightly-run.ps1` 未创建（PL-023）→ **在此之前不得注册正式任务计划**。验收断言清单已写好：`docs/nightly/scheduler-acceptance-test.md` |
| 2026-09-18 | PL-009 | **仍待评审，但风险已降低**：本次修正口径时**只改了 v2**（`cross-platform-ai-assistant-architecture-v2.md`），未动 v1 —— v1 仍含旧口径，但 AGENTS.md §1 的文档地图不指向 v1，v1 不在任何卡的 write scope 内。建议仍按原提案处理（移 `docs/history/` + 加「已被 v2 取代」横幅），归开源准备 |
| 2026-09-18 | PL-005 | **脱敏清单的实测输入（本次全仓扫描得到，供开源前直接使用）**：以 `api[_-]?key\|secret\|token\|password\|Bearer \|sk-\|ghp_\|AKIA` 扫全仓 → **83 处命中全部是技术上下文的 `token` 字面**（LLM token 计量 / IPC 鉴权 token / portal `restore_token` / `xtask` 的 Rust tokenizer），**无任何真实凭据** ✅。但仍有 **4 类需脱敏的非密钥信息**：① **本机绝对路径** `D:\csart\`（出现在 probe-03/04 的 `$WorkDir`、ADR-0023、SPIKE-A、MEMORY 里）——建议改为 `$env:TEMP` 或仓库内相对路径；② **Codex 会话/滚动 UUID**（如 ADR-0018 §证据表的 `01a0adcd-2b86-…`、`facts.md` 里的 rollout 文件名）；③ **本地代理地址** `http://127.0.0.1:30000`（MEMORY.md §1）——本身是 loopback 不泄露网络，但暴露了开发环境细节；④ **邮箱与账号**（`snofin@gmail.com` 已在全部提交的 author 里，属于已接受的公开信息，但写 `docs/OPEN_SOURCE_CHECKLIST.md` 时应明确列出以便复核）。→ 建议 `docs/OPEN_SOURCE_CHECKLIST.md` 直接以这四类作为首批检查项，并把① 做成可机器扫描的规则（禁止仓库内出现盘符绝对路径）　**归属**：开源准备（原第 4 个单元格并入本列 —— 本表只有 3 列，多出的那格会让 GitHub 破表，PL-031） |
| 2026-09-18 | PL-022 | **已关闭（ADR-0030 落地，人类指示 #4「避免今后再发生」授权）**：`xtask` 新增 `memory-counts` 与 `adr-index` 两个只读子命令，把「记忆规模计数」与「ADR 编号登记表」两处派生事实改为**机器校验**（共 **19** 条规则：memory 8 + adr 11），并接入 CI 硬门禁 **#12b**（`doc-consistency` job）与 gov §5.1 的 #12b 行。PL-022 提的三条里：① gov §5.4 表格行数派生 = 已由 ADR-0025 钉死为 13 且 `deferred.rs` 有数量自洽测试；② gov §5.1 清单行数 ↔ `ci.yml` 步骤数 = **本轮未实现**（gov §10 的口径已手工同步为「18 行清单 ↔ 17 步骤（8 硬 + 9 软）」），与 PL-032 同批归 TASK-015；③ `MEMORY.md` 规模表 = **已实现**，且额外按 ADR-0030 **D2 消除了重复书写**（删掉正文里「L1 现合计 207 条」这个派生合计，规则 `memory/derived-total-in-prose` 禁止它复发）。**负向实证已跑**：把规模表里 `facts.md` 的行数改成 `1` → 退出码 **1** 且输出 `memory/line-count-mismatch … 实测 101`；复原后 PASSED |
| 2026-09-18 | PL-029 | **已关闭（ADR-0026 D4 执行，章程 v1.4）**：章程 §13 W4 的 write scope 由「用范围写法表示的 0016 至 0020（新建 5 个文件）」改为**逐个列出**的 **0016 / 0017 / 0020 / 0027** 四个号；「禁止」项由「新建 0021 及以后的编号」改为「新建 **0028 及以后**的编号」，并新增「不得复用**已退役编号 0019**」；验收由「5 个文件」改为「**4 个文件**」，并新增两条机器验收（`xtask hygiene` 与 `xtask adr-index` 均须 0 Error）。**write scope 额外含 `docs/adr/README.md`（仅回填）** —— 这是 ADR-0026 D1「新建后必回填」与 ADR-0030 机器校验的必然要求，不回填就会以 `adr/file-not-in-registry` 直接红。**并新增一条通用要求：ADR 编号禁止写范围形式**（`0016~0020` 这种写法本身就是本条 PL 的成因之一） |
| 2026-09-18 | PL-030 | **已关闭（ADR-0030 D3 落地）**：`xtask adr-index` 已实现并接入 CI 硬门禁 **#12b**。三方交叉校验 `docs/adr/README.md` ↔ `docs/adr/NNNN-*.md` ↔ `docs/memory/decisions.md`，共 **11** 条规则；0019 号双重占用那类事故的机器判据是 `adr/number-collision`（§1 表 ∩ §2 表 ≠ ∅）。**负向实证已跑**：往登记表 §2 临时注入一行 `0019` → 退出码 **1**，且同时命中 `adr/number-collision` 与 `adr/retired-number-reallocated`；复原后 PASSED。ADR-0026 D1 的「登记表由人工维护」据此被取代（ADR-0026 仍是**规则来源**）。PL-030 里「顺带扫裸引用」的那一半**没有**实现 → 拆出为 **PL-032** |
| 2026-09-18 | PL-031 | **已关闭（人类指示 #3「这个去改掉」）**：3 处未转义竖线全部改为 `\|` —— ① `LEDGER.md` 开源脱敏预检行里的密钥扫描正则（8 个裸竖线）；② 本文件 PL-005 处置行里的同一条正则（9 个）；③ `docs/nightly/scheduler-acceptance-test.md` 断言 C1 的 PowerShell 管道符（1 个）。**修 ② 时发现它还有第二个缺陷**：该行末尾多出一个 `\| 开源准备 \|` 单元格，使这一行有 **4** 格而表头只有 **3** 列 —— 之前被大量裸竖线掩盖了，转义之后才暴露 → 已把「开源准备」并入第 3 列。**复扫判据**：按「表格行的未转义竖线切分单元格数 ↔ 该表分隔行的列数」逐表比对，全仓 45 个 `.md` → **破表 0 处**；同时复扫「`---` 前必须空行」→ **setext 风险 0 处** |
| 2026-09-18 | PL-028 | **违规范围比原记录更大：不是 2 处，是 6 处**（本轮全仓复扫发现；判据 =「裸写形式的 ADR 编号出现在 `docs/adr/` 之外，且该号属登记表 §2 的待建号集合」）。原记录的 2 处在 ADR 正文里（`0021-*.md:4` 引 0017、`0023-*.md:179` 引 0016）；**新发现的 4 处全在 `docs/governance-ai-agent-execution.md`**：0007（element 不跨进程的关联引用）、0003（变更历史表的「依据」列）、0011 两处（一处是 commit message 示例、一处是 ADR 草稿模板的标题示例）。**处置不变（本轮仍不改写）**：gov 不是「只增不改」的 ADR，理论上可改，但 ① 改它属于 drive-by refactor；② 4 处里有 2 处是**示例文本**，改成待建写法反而会让示例失真。→ 归 **PL-032** 一并处理：豁免机制必须同时覆盖「ADR 正文（只增不改）」与「示例 / 模板文本」两类，否则规则一上线就是 **6** 条永久红灯。**注**：本行与 PL-028 原行都**故意**写出了违规形态（否则无法说明它长什么样），因此这两行本身也必须进豁免清单 —— 这正是「先定豁免机制、再实现规则」的理由 |
| 2026-09-18 | PL-002 | **部分推进（ADR-0031 D6）**：`card-check` 的**四条判据已写死**，将来实现时不需要重新辩论 —— ① 卡片文件**正文区**（分界线以上）的 `git diff` 非空 → Error；② 状态非 `Ready` 的卡必须有对应 `tasks/TASK-NNN-*.md` → 缺失即 Error；③ 记录区 9 节标题必须齐全 → 缺节即 Warning（`Ready` 豁免，因为还没开工）；④ `plans/*.md` 里出现卡片正文形态的二级 TASK 标题 → Error（防形态回退造成两处正文）。判据原文同时落在 gov §3.4，规则来源是 ADR-0031。**本条不关闭**：`check-comments` / `check-ledger` / `card-check` 三个子命令**仍然没有任何任务卡认领**（gov §5.1 的 #15 #16 门禁因此仍无人负责）→ 继续归 TASK-015，或补一张专门的护栏卡 |
| 2026-09-18 | PL-032 | **已关闭（ADR-0032 Accepted）**：机器规则与历史违规的冲突被**豁免机制**解决。机器检查仍未实现—— 本 ADR 只规定机制不实现代码，读入逻辑归 PL-002 / PL-015。现在的“先制定豁免机制再上线规则”顺序与 PL-032 原提一致。例外：PL-028 的 6 处裈引用全部登记在 `docs/adr/0032-doc-rule-exemption-registry.md` E-001 至 E-010（gov §4.1 793 / 805 / 487 / 171 + xtask/src 4 个测试夹具）。 |
| 2026-09-18 | PL-034 | **本条与 PL-032 是同一件事**：机器规则（禁范围写法） vs 历史违规（9 处存量）。豁免机制封闭同上。全部 9 处在 `docs/adr/0032-doc-rule-exemption-registry.md` E-011 至 E-020（LEDGER.md 3 行 / PARKING_LOT.md 1 行 / 架构与章程 2 行 / ADR-0026 两行 / decisions 一行 / wbs、plans/stage-0-spikes 各一行）。**本轮两条合起来裁决、未来一同上线**。 |
| 2026-09-18 | PL-033 | **已关闭（ADR-0033 Accepted）**：两套阈值「写作规范 400/600」 vs 「CI 门禆 600/900」各管一摊，不是冲突是双层护栏。阈值数字本身**一个未动**，改的是§5.4 表行末尾、§6.2 (Rust) 表行末尾、`AGENTS.md §5.3` 同行末尾的同步标注。§6.2 保留原位置不拆到新节（Rust 语言专项规范的一部分，独立成节会造成谁是写作规范的难以回答）。本轮不动 CI 门禆语义（PL-033 原提「本轮不改门禆语义」继承）；`xtask/src/hygiene.rs` 的 `FILE_LINES_WARN/ERROR` 仍是 600/900。 |
| 2026-09-18 | PL-017 | **已落地**：docs/dev-env-setup.md 已创建（130 行，包括 Rust 工具链、cargo-deny / cargo-llvm-cov 的下载与校验、inspect.exe 位置、代理配置、验收命令、常见坑）。**PL-006 随之完全解除**：本机已装 cargo-deny 0.20.2 + cargo-llvm-cov 0.9.1，本地与 CI 已等价；下次重置机器只需按本清单重走 |
| 2026-09-18 | PL-027 | **已落地**：`hygiene` 第 13 项「末行换行」清扫完毕 —— `docscan` 报 encoding **11 → 0**（10 个缺末行 LF + 1 个问题形 `target-apps-feasibility.md` 双尾空行）。**全部 11 个文件现在都以恰好一个 `\n` 结尾**（已用 `open(p, 'rb').read()[-1] == 10` 逐个字节验证）。**为什么本轮能一次性清零**（而不是像以前 PL-022 / PL-032 那样要靠豁免机制）：清单是**短而全**（11 个文件，全部是仓库根配置 + xtask/Cargo.toml 这种工程元层小文件），**不涉及只追加文件**（不像 PL-034 的 LEDGER / PARKING_LOT 历史违规），所以不需要豁免 = 不需要新规则 = 清零即可升 Error。**剩一件事**：按 PL-027 原始建议把这条 `hygiene` 规则从 Warning 升 Error —— 但**本轮没升**，原因：① ADR-0025 D4 已把这条规则定为「先 Warning 后 Error」两段式（升 Error 是它的**第二阶段**）；② 升 Error 改门禁语义 = 漂移触发器 ④，需要新 ADR → 归 TASK-015，与 PL-022 / PL-033 / PL-035 同批护栏升级。**关闭判定**：PL-027 原 11 文件清单已扫零（docscan 复验确认）；升 Error 是 TASK-015 的事，PL-027 这一条本身 = 落地。 |
| 2026-09-18 | PL-026 | **已落地**：`spikes/spike-a-notepad/probe-01-tree-survey.ps1`（788 → 10 non-ASCII bytes）+ `probe-02-text-and-timing.ps1`（941 → 35）= **1729 字节 → 45 字节** 翻译掉 1684 字节（**97% 减量**），**剩余 45 字节全部是 STR-LIT 字符串**（故意的非 ASCII 测试数据，翻译它 = 改掉被测对象）。**关键设计选择：只翻译 `#` 行注释，保留 `"..."` 字符串字面量**。原 PL-026 提的担忧「改编码可能改语义」通过 `strip_ps_comments` 函数证明：去除所有注释后，剩余代码与翻译前**逐字节相等**。**剩余 3 处 STR-LIT 全部以 ADR-0032 豁免登记**（E-021 / E-022 / E-023），移除触发 = **永不**（测试数据的语义就是非 ASCII）。**未改 .ps1 的 PowerShell 行为**，只是把注释从中文换成英文（语义对等、字面更易读）；下次重跑探针的 evidence 一致性在「代码文本」而非「注释文本」。**关闭判定**：ADR-0024 D4「仓库内 .ps1 一律纯 ASCII」现在 0 违规，**仅** 3 条已知豁免，**机器检查实现归 PL-015**（与 PL-022 / PL-033 / PL-035 同批护栏升级） |
| 2026-09-18 | M5 | **已关闭（人类裁决 A = MIT OR Apache-2.0）**：`LICENSE` 改为双许可证标准文本（MIT + Apache-2.0 全文 = 39 行）；`NOTICE` 新增（10 行，Apache-2.0 §4(d) 硬要求）。`deny.toml` **未改**（其 `licenses.allow` 列表已含 `MIT` + `Apache-2.0`）。**为什么不改 deny.toml**：机械化的「许可证名单」理应多留余地而非「项目用哪个就列哪个」—— 若只列 MIT，将来要换 Apache-2.0 ONLY 还得改 deny.toml。**为什么不选单 MIT**：用户主动选了 A；MIT OR Apache-2.0 是 Rust 生态默认（tokio/serde/hyper），保留 Apache-2.0 给下游 = 给专利授权（Apache-2.0 独有 §3「patent grant」，MIT 没这条）。**为什么不选 Apache-2.0 ONLY**：与 M5 open 条目候选清单「`MIT OR Apache-2.0`（建议）」不符，强行收紧会拒一部分潜在贡献者。**`NOTICE` 是 Apache-2.0 §4(d) 的硬要求而非可选项**——任何派生物只要带 LICENSE 的任一部分必须同时带 NOTICE。**下游提醒**：「任选其一」机制 = 用户可以**单选 MIT**（更宽松），也可以**单选 Apache-2.0**（带专利授权）—— 选哪个 = 用户自己，不是项目强制 |
| 2026-09-18 | 人类会话（非任务卡） | **PL-036** stage-1 46 张卡片展开落地（已补号）：按 ADR-0031 · gov §3.2 · gov §3.4 模板生成 `tasks/TASK-012…058-*.md`（46 个新文件；011 与 035 是阶段 0 期间已迁出，不重复迁）。plans/stage-1-pilots.md §「卡片正文的位置」表从 2 行扩到 48 行；MEMORY.md §1 快照已同步。**原则：阶段 1 未开工 不预先拓展正文** —— 本批仅生成卡片文件骨架 + Ready 状态，个别卡的正文由 Orchestrator 在开工前按 gov §3.2 模板实际补全。· 选 slugs 原则：10–30 字笰 kebab-case，从标题推导；不造名词。 · canonical 分界线与 9 节骨架都从 gov §3.4 现场读取、逐字符复制。 |
| 2026-09-24 | PL-037 | **新提（Orchestrator 代行，人类授权）**：stage-1 A1 顺序矛盾 + **TASK-015 无法 Done**。`plans/stage-1-pilots.md` 批次表头写「必须串行」，但依赖列里只有 `013 ← 012` 是真串行（`014 ← 011` / `015 ← 011` 在 011 Done 后即解锁，三者 write scope 不重叠）。更硬的问题：015 的 DoD 含「arch test 能拦住 core→platform/windows 的依赖」，而 `crates/core` 要到 **TASK-028** 才存在 → **015 在 028 之前无法 Done**，与「015 必须早做（护栏晚于代码 = 漂移已经发生）」直接冲突。**影响**：A1 批次的「串行」与「早做」两条约束不能同时满足，派单时会撞墙。**建议（需人类裁决）**：① 把 015 的 arch test 项拆到后续卡（015 只做 hygiene 13 项 + verify-schemas + replay 骨架）；或 ② 015 保持现状但标「部分 Done」，arch test 移到 028 之后的新卡；或 ③ 把 `crates/core` 骨架（仅 trait + 目录）提前到 015 之前。**已停工作**：未改批次表的依赖列（属计划层裁决，agent 不动）。 |
| 2026-09-24 | PL-038 | **新提（Orchestrator 代行，人类授权）**：`docs/spec/*` 7 份契约草案被同一外来模板块污染（TASK-072 批量生成事故）。7/7 都有**空 `## 1. 目标` / `## 2. 范围`** + **两套 `## 4. 不变量` / `## 5. 与其他 spec 的关系`**；第二套是通用 schema 口径（`schema_version` 单调 / ISO-8601 / `id` u64 / ErrorCode），且 `### 字段` 标题**连着出现两次**，末尾 7 行关系表**自引用 testing.md**（在 tool-schema.md / audit-event.md 里也出现 = 从别的文件复制来的）。`naming.md`（更早手写）未受影响。**影响**：这 7 份是契约层文件（文件头明写「违反即 CI 失败或 Reviewer 拒绝合并」），stage-1 的 TASK-011 / 020 / 021 都会按它们实现 → 下个 agent 读到的是残契约；`PL-003`（`xtask/src/report.rs` 引用 `testing.md` §4.3 而该节不存在）是同源症状。**处置**：已建卡 `tasks/TASK-200-fix-spec-contract-drafts.md`（治理池 200~299）。**已停工作**：未改 `docs/spec/*` 一个字（属 spec 只读区）。**机器检查缺口**：`xtask docscan` 抓不到「节号重复 / 空节」这类结构缺陷 → 提案给护栏卡（未开卡）。 |
| 2026-09-24 | PL-037 | **已闭环（人类裁决选项 ③）**：`crates/core` 骨架**提前落地** = 新卡 `tasks/TASK-201-core-crate-skeleton.md`（Done）。新增 `crates/core/{Cargo.toml,README.md,src/lib.rs}`：`assistant-core`、**零第三方依赖**（`[dependencies]` 为空）、`#![deny(unsafe_code)]`、**零 `pub` 项**（骨架期不发明接口 = 铁律 10 的体现）。**为什么选 ③ 而不是 ①/②**：① 护栏继续晚于代码（`core` 一旦在 028 落地就没人回来补门禁）、② 制造长期 `partial` 状态（下个 agent 无法判断门禁到底存不存在 = 静默失败）。**效果**：`cargo test -p assistant-core arch::`（CI 软门禁 #5）不再落空，TASK-015 的 write scope `crates/core/tests/arch*` 有了宿主 crate → **015 现在可开工**。**⚠ 语义边界（重要）**：该门禁现在是「**0 个测试通过**」而**不是**「arch 规则已生效」；真正的分层断言归 **015**，CI 的 `[SOFT #5]` **仍是软门禁**（转硬 = 015 的动作）。**未做**：`crates/core/tests/**` 刻意不创建（属 015 的 write scope，避免两卡 scope 重叠）。**号段**：落 200~299 治理池（TASK-201）—— 理由与可裁决点见该卡 §9。 |
| 2026-09-24 | PL-039 | **新提（TASK-201 附带）**：`AGENTS.md` §11.1（「阶段切换 → 必更新 README 状态行」）与 §11.3（「改了 README 状态**以外**的章节 → 漂移」）**在文本层仍然冲突**。人类 chat 2026-09-24 已就 DRIFT-200-2 裁决「授权状态行 + `## 当前阶段` 章节一起改」（本轮已按此落地 README），但**规则原文没改** → 下次阶段切换会再撞一次同样的墙（只改状态行 = README 自相矛盾；连章节一起改 = 命中 §11.3 的漂移判据）。**处置建议**：把 §11.1 的口径明确为「状态行 **+ `## 当前阶段` 章节**」（两者是同一件事的两半）。**为什么本轮不改**：`AGENTS.md` 属只读区（「本文件变更需 ADR」）→ 归下次 `AGENTS.md` 修订或开治理卡。 |
| 2026-09-24 | PL-038 | **已闭环（TASK-200）**：`docs/spec/` 7 份契约草案的结构缺陷全部修完 —— 每份的**外来模板块**（重复 `### 字段` + 通用 §4 + 自引用 `testing.md` 的 §5 表）整段删除；`## 1. 目标` / `## 2. 范围` 补为非空（范围含明确**不做清单**）；第一套 `## 5.` 的一行括注扩成该文件真正的「与其他 spec 的关系」表。**逐份自检**：`## 4.` / `## 5.` 各只出现一次、`### 字段` = 0（7/7 命中）。**顺手处置 PL-003**：`xtask/src/report.rs` 引用 `testing.md` §4.3 而该节不存在 → 已在 `testing.md` 补出 `### 4.3 输出可注入`（改 Rust 注释超出本卡 scope，故走"补节"路线）。**未改**：任何契约字段 / 类型 / 枚举（Out of scope 硬线）、`naming.md`、`protocol/*.json`、任何 Rust 代码、任何 ADR。**遗留**：见 PL-040 / PL-041。 |
| 2026-09-24 | PL-040 | **新提（TASK-200 附带）**：`docs/spec/` **没有 `README.md`**（缺索引 / 阅读顺序 / 各 spec 的"上位与强制性"总览），8 份 spec 只能靠文件名猜阅读顺序。TASK-200 的 In scope 明确「若不存在，**不要新建**（新增顶层文件 = 漂移触发器 ②）→ 记 PL」→ 故记在此。**建议**：由 Orchestrator 在下一轮文档治理里新建，或在既有入口（`README.md` 的「查契约细节」行 / `docs/spec/naming.md`）补一句阅读顺序。 |
| 2026-09-24 | PL-041 | **新提（TASK-200 附带）**：**`id` 字段类型口径不一致**。被删除的外来模板块声称「**ID 用 u64**：所有 `id` 字段类型 = unsigned 64-bit」，但 `docs/spec/envelope.md` 的 `envelope_id` / `correlation_id` 与 `docs/spec/audit-event.md` 的 `event_id` 都是 **`Uuid`**（envelope 还明确要求 UUID v7 时间有序）。→ 该"通用不变量"**不成立**，故按本卡「逐行核对是否适用，不适用的删掉」**未把它移入任何文件**。**需要 ADR**：`id` 到底统一为 u64（DB / 行主键？）还是 Uuid（跨进程标识？）属契约口径，本卡不改契约。 |
| 2026-09-24 | PL-046 | **新提（TASK-013 附带，对应 DRIFT-013-1）**：`crates/storage` 的迁移链是 `crates/storage/src/schema.rs` 里的**编译期内嵌常量**（`MIGRATIONS` + `include_str!`），**没有**"别的 crate 往迁移链里加自己的表"的入口 → 任何跨 crate 加表都必须改 storage 的源码（TASK-013 原 write scope 只有 `crates/audit/**`，因此**根本建不出表**）。**建议**：后续护栏 / 存储卡引入**迁移注册表**（各 crate 声明自己的迁移，框架按版本排序执行）—— 这属于存储层公共接口变更，**需 ADR**。 |
| 2026-09-24 | PL-046 | **已关闭（ADR-0038 + TASK-202）**：迁移链从「storage 私有常量」改为**注册表 + 唯一装配点** —— `crates/storage` 只提供机制（`Migration` / `MigrationSet` / `MigrationSetError` + `Database::open(paths, clock, &MigrationSet)` 的**必填**迁移集参数），每个**拥有表**的 crate 用 `include_str!` 内嵌自己的迁移 SQL 并公开 `pub const MIGRATIONS`（0001 → `crates/storage`、0002 → `crates/audit`；DDL 与拥有者同处）。**效果**：① 「加一张表 = 只改自己那个 crate」（不再改 storage 的源码与测试 —— 正是 PL-046 的原始症状）；② 漏注册**不是静默**：`Database::open` 对「库里有本集合不认识的版本」报 `schema_version_mismatch`；③ 重号 / 缺号在装配时硬拦（`register()` / `validate()`）。**删除** `pub const SCHEMA_VERSION`（注册表下它必然说谎）→ 替代 = `MigrationSet::expected_version()`（删公共 API = 漂移 ⑫，由 **ADR-0038 D5** 授权）。**迁移文件移动**：`crates/storage/migrations/0002_audit_logs.sql` → `crates/audit/migrations/0002_audit_logs.sql`，**内容一字不改**（checksum 记内容不记路径 → 已有库仍可打开）。**遗留**：PL-047。 |
| 2026-09-24 | PL-047 | **新提（TASK-202 附带）**：**迁移版本号登记表仍是手工回填**。ADR-0038 D4 把 `docs/storage-design.md` §3.4 定为版本号分配的 SSOT，装配时 `register()` / `validate()` 只拦得住「已经装配进集合」的重号与缺号 —— **拦不住「写了迁移文件却忘了在 §3.4 占号」**（那正是 ADR-0030 的教训：靠记得回填的护栏会失效）。**建议的机器化**：`xtask` 扫描 `crates/*/migrations/*.sql`，校验「文件名号段唯一」「与 §3.4 登记表逐行一致」「每个含迁移的 crate 都公开 `MIGRATIONS`」。**为什么本轮不做**：新增 hygiene 规则要动 gov §5.4 的规则计数（当前 13 项）+ ADR-0025 / ADR-0030 的口径 → 归 **TASK-015**（它拥有 xtask 护栏与 `crates/core/tests/arch*`）。**注**：在它落地前，「登记表 + 装配硬拦」是**降级但不静默**的护栏（缺号会被 `validate()` 拦成红灯，只是报错点比 CI 晚）。 |
| 2026-09-24 | PL-042 | **复核结论（TASK-203）**：`audit.durability = separate_db_full` 仍未落地，**本卡不实现**。复核口径：真落地需要「第二个 DB 文件 + 独立连接 + `synchronous=FULL` + 与主库的迁移/校验协同」，而 `Database::open` 现在是**单库单连接**的所有者（ADR-0038）—— 加第二个库要改 `crates/storage` 的公共接口（漂移触发器 ③）+ 定「两个库的迁移集怎么装配 / 谁保证一致」的口径，属**独立 ADR + 独立卡**。**当前行为已合规**：请求该档位得到 `AuditError::UnsupportedDurability`（**显式失败**，绝不静默降级成 batched —— 铁律 1）。**触发条件** = 出现「每条审计不可丢」的硬要求（合规场景）。**处置：待评审（接受为「显式失败」现状；落地归未来独立卡 + 独立 ADR）** |
| 2026-09-24 | PL-043 | **已关闭（ADR-0040 D2 + TASK-203）**：链尾查询与读取顺序不再依赖 `rowid`。迁移 `0003_audit_logs_semantics` 引入 `sequence INTEGER PRIMARY KEY AUTOINCREMENT`（单调递增 + **永不复用**已用号），`SELECT_CHAIN_TAIL_SQL` 改 `ORDER BY sequence DESC LIMIT 1`、`SELECT_RECORDS_SQL` / `SELECT_RAW_ROWS_SQL` 改 `ORDER BY sequence`。**回归断言**：`crates/audit/tests/audit_tamper.rs::test_vacuum_does_not_change_chain_tail`（VACUUM 后链尾不变 + 新行仍接得上旧链尾 + 整链自洽）。`0002_audit_logs.sql` **一字不改**（checksum 记账）。**处置：✅ 已关闭（TASK-203）** |
| 2026-09-24 | PL-044 | **复核结论（TASK-203）**：**接受为残余风险，本卡不实现**。链的判据全在 `audit_logs` 内 —— 删掉**全部**行后「没有行」与「链自洽」不可区分（删尾部若干行同理：剩下的前缀仍自洽）。**为什么不在当前架构内修**：要检出它必须引入**外部锚点**（把链尾定期写到只追加的外部介质 / 另一台机器 / 签名的日志服务），而本项目的架构里**没有**这个落点（架构 v2 §15.3 的「加密与保留」只覆盖本地库），引它 = 新组件 + 新依赖 + 新信任模型（漂移 ① + ②）。**已在 `crates/audit/README.md` 的「已知限制」写明**（不静默）。**触发条件** = 合规 / 无人值守场景（阶段 7「无人值守·交易闸门」）。**处置：待评审（接受为残余风险；落地归未来独立卡 + 独立 ADR）** |
| 2026-09-24 | PL-045 | **已关闭（ADR-0040 D3 + TASK-203）**：`audit_logs` 只留**一个**语义明显的 self_hash 落点 —— 删掉与 `id` 完全同义的 `hash` 列（迁移 0003 重建表；`id TEXT NOT NULL UNIQUE` = 本条 `self_hash`）。「两列必须一致」那条**同义反复**的校验随之消失；权威判据只剩「重算 == `id`」（`verify.rs`）。**未引 `uuid`**：真代理键（UUIDv7）方案被 ADR-0040 选项 4 否决（时间有序 ≠ 链有序）。**处置：✅ 已关闭（TASK-203）** |
| 2026-09-24 | PL-048 | **新提（TASK-203 附带，与 TASK-203 的改动无关）**：`xtask/src/arch.rs` 的 `arch::tests::cargo_toml_with_deps_detected` 是**既有 flaky 测试**。**症状**：`assert_eq!(f.len(), 1)` 偶发 `left: 0` —— `check_third_party_deps` 在 `read_to_string` 失败时**静默返回空 Vec**（`let Ok(content) = ... else { return findings; }`），于是「文件读不到」与「没有三方依赖」不可区分。**证据**：① CI run 35964821459（push，commit `ab328b1`）的 `check (windows-latest)` 在 `[HARD #4] cargo test` 上红，`left: 0 / right: 1`；**同一 commit** 的 run 35964857696（pull_request）8/8 job 全绿 → 同一 SHA 两次结果不同 = flake；② 本机 Windows 连跑 25 次复现 1 次（约 4%）。**疑似根因（未完全定位）**：`tempdir()` 的唯一性只靠 `std::process::id()` + `SystemTime::now().as_nanos()`，而同文件里 `cargo_toml_with_deps_detected` 与 `cargo_toml_without_deps_passes` 会往**同一个** `xtask/Cargo.toml` 相对路径写内容 —— 并行执行下若两者撞进同一临时目录，就会互相覆盖（读到的正是对方那份「空 `[dependencies]`」→ `left: 0`）。**建议修法**：① 临时目录唯一性改用 `AtomicU64` 计数器（与 `crates/audit/tests/common/mod.rs::TestDir` 同款）；② `check_third_party_deps` 读不到文件时**不得**静默返回空 Vec（铁律 1）。**为什么本卡不改**：`xtask/**` 不在 TASK-203 的 write scope 内，属 drive-by refactor（AGENTS.md §3 禁止）→ 归 **TASK-015**（它拥有 `xtask/**`）。**处置：待评审（归 TASK-015）** |
**[supersedes:2026-09-20]** PL-029 主表（line 38）已同步为「已关闭（ADR-0026 D4）」= 无需单独 supersede 标注。本卡 TASK-069 处理。

- **[supersedes:2026-09-20]** PL-NEW（**TASK-051 → TASK-052 入口**）workspace `[lints.clippy] indexing_slicing = "deny"` 无 `priority = -1` —— per-item / per-function `#[allow]` 在 clippy 1.98 实测均无法 override（restriction 类 lint 不响应）；后果：要清掉本卡唯一保留的 refscan.rs 40-lint `#![allow(...)]` 块，需二选一 —— ① ADR 把 `indexing_slicing` 加 `priority = -1`（per-item 即可 override → 后续机械重构）+ ② TASK-052 用 `.first()` / `.get(n).expect(...)` 全替换 32 处 indexing_slicing（`.expect()` 由 test wrapper 允许）。**状态：✅ 已关闭（TASK-052 完成）。人类裁决 B 路 = 纯重构；refscan.rs 40-lint 块 + 其他 3 模块窄块全部清掉，32 处 indexing_slicing 用 `while let Some` + `.get(n)` 全替换，per-line 注释替代模块级 allow。workspace `[lints.clippy]` 配置**未动**（= 漂移触发器 ⑥ 不可忍受，按人类要求）。**详见 tasks/TASK-052-...md §5 + commit 收尾。**
